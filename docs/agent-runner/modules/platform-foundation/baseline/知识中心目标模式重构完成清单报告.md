# 知识中心目标模式重构完成清单报告

> 报告日期：2026-09-09 11:32:13 +0800
> 执行范围：P0 基线 + P1 资料加工统一契约、响应校验与流式/文件协议快照批次
> 结论：本批次已完成并可回归；目标模式总体收口尚未宣布完成。

## 已完成

- [x] 生成可重复执行的 P0 机器基线：`p0-capability-baseline.json`。
- [x] 形成入口、服务、真实路由、持久化源、对象归属、风险和验证计划的人工基线：`p0-capability-baseline.md`。
- [x] 更新平台基础模块 README 和总体架构设计，记录适配层边界与当前缺口。
- [x] 保留 `/knowledge-center/`、`/prompt-generator.html`、`/pingcode-materials/`、`/pingcode-api/*` 和 `/api/*` 既有路由。
- [x] 增加受保护的资料清洗查询适配层：`/knowledge-center/api/cleaning/workbench/summary`、`/material-batches`、`/datasets`、`/training/tasks`。
- [x] 资料清洗页面改为读取 Python 资料加工服务的真实批次、知识库版本和任务统计，不再用“资料清洗待建设”掩盖已存在能力。
- [x] 适配层只允许登记的方法/路径；查询、下载和事件要求 `knowledge:read`，写命令要求 `knowledge:write`；未登录请求真实返回 `401 AUTH_REQUIRED`。
- [x] 旧资料加工 API 未改写，统一入口仍是同源代理，Node/Python 服务边界保持不变。
- [x] 文档服务、资料加工服务、认证服务健康接口和统一入口均完成真实 HTTP 采样。
- [x] 建立 P1 统一接口契约 v1，冻结错误、分页、任务、事件和兼容规则的机器可读定义。
- [x] 定向 Jest：2 个套件、19 项测试通过；Node 语法检查和变更范围 `git diff --check` 通过。

## P1 端点适配批次（2026-09-09）

- [x] 新增 `packages/platform-contracts/knowledge-center/v1/endpoint-mapping.json`，登记资料加工上传、批次、预处理、训练、任务事件、文件下载/预览和数据集发布端点。
- [x] 新增 `code/packages/agent-runner-core/lib/knowledge-center-cleaning-contract.js` 显式方法/路径白名单；未知内部路径和未登记 HTTP 方法返回 `CLEANING_ROUTE_NOT_ALLOWED`。
- [x] 统一入口写请求要求 `knowledge:write`，查询/下载/事件要求 `knowledge:read`；写请求透传幂等键、请求关联并注入脱敏操作者标识。
- [x] 旧 `/api/*`、`/pingcode-api/*` 未修改，适配层可通过回退应用版本撤销。
- [x] 新增 `knowledge-center-cleaning-contract.test.js`：3 项契约测试通过。
- [x] 运行时白名单改为直接加载 `packages/platform-contracts/knowledge-center/v1/endpoint-mapping.json`，不再以内嵌 JS 数组作为契约事实源。
- [x] 新增 `tools/knowledge-processing/generate-knowledge-center-endpoint-mapping.js`，可从 Python FastAPI 路由重复生成端点登记；当前登记 117 个端点，运行时暴露 115 个，删除类端点默认 `expose=false`。
- [x] 增加端点契约结构校验：ID、方法、路径、操作类型和权限动作不合法时启动即失败；测试验证版本化 JSON 与运行时白名单一致。
- [x] 新增统一响应运行时校验，仅作用于知识中心清洗适配层：成功响应校验 `success/requestId/correlationId/data|items`，错误响应校验标准错误码、中文消息、`retryable` 和 `requestId`；保留下游任务状态和业务字段。
- [x] 新增 `packages/platform-contracts/knowledge-center/v1/stream-download-contract.json`，登记 4 条 SSE、3 条文件预览/内容/下载响应约束和资源 ID 深链规则。
- [x] 兼容性测试校验 SSE `text/event-stream`、缓存/代理缓冲头、文件深链源码路径和版本化协议快照。
- [x] 增加 Python FastAPI 路由与版本化端点登记的防漂移测试，当前覆盖 117 个 `/api/*` 路由。
- [x] 定向 Jest：4 个套件、24 项测试通过；端点契约单套件追加防漂移测试后为 5 项通过；`node --check` 和 `git diff --check` 通过。
- [x] 统一响应校验后定向 Jest：4 个套件、26 项测试通过；Node 语法检查和 `git diff --check` 通过。
- [x] 流式/文件协议快照加入后定向 Jest：4 个套件、27 项测试通过；Node 语法检查和 `git diff --check` 通过。
- [x] 非 JSON 透传契约补强：统一代理对 SSE、预览和下载响应追加 `X-Request-Id`/`X-Correlation-Id`，且不缓冲响应体；资料加工 PDF/图片预览显式返回 `Content-Disposition: inline`，下载继续为 `attachment`。
- [x] 新增 `tests/agent-runner/proxy-streaming-contract.test.js` 和 Python `test_file_response_contract.py`，分别真实验证代理 SSE/二进制透传及预览/下载处置。
- [x] 认证与 GitLab 统一入口浏览器回归：Chromium 15 项通过，覆盖匿名登录面板、管理员登录/错误恢复、OAuth 降级与知识编辑者仓库读取。
- [x] 审核与发布统一入口真实只读回归：页面读取真实审核任务/发布状态并展示工作区或可恢复空态，不展示演示数据；Chromium 场景通过。
- [x] 文档生产增量工作区真实浏览器回归：2 项 Chromium 场景通过，覆盖真实状态/范围/草稿/检查结果展示及服务失败降级、移动端无横向溢出。
- [x] 文档生产入口新增真实任务列表回归：不拦截后端请求时读取真实增量任务或真实空态，禁止“演示数据”文案；该工作区 Chromium 回归共 3 项通过。
- [x] 统一入口与文档生产回归合并执行：`knowledge-center-entry.spec.js` + `knowledge-center-incremental.spec.js` 共 17 项 Chromium 场景通过。
- [x] 增加 Jest/Playwright 测试边界配置：Jest 排除 `tests/e2e/`，避免把 Playwright 场景误当 Node 测试执行；E2E 继续使用 Playwright 命令单独验证。
- [x] 全量 Jest 复核确认剩余失败来自既有外部前置：检索集成依赖未运行的 4100 文档服务，`document-api.test.js` 为历史脚本型文件且无 Jest `test()`；未发现本轮契约、代理或资料加工改动导致的新增失败。
- [x] 为统一资料清洗工作区补齐真实上传会话客户端契约：创建会话、登记文件、分块上传、完成文件/会话及创建批次，写请求带幂等键；新增契约测试。
- [x] 资料清洗页面已接入有 `knowledge:write` 权限用户的“上传资料”对话框，按服务端块大小调用真实上传会话链路并在完成后刷新工作台；Chromium 13 项入口回归通过（含上传入口可达性）。
- [x] 在隔离临时数据目录真实执行上传黄金 API 流程：管理员登录、创建会话 201、登记文件 201、分块上传 200、完成文件/会话 200、创建批次 201；测试结束销毁临时服务和数据目录，未污染共享数据。
- [x] 在隔离临时数据目录进一步真实执行统一入口资料加工链：上传批次后依次调用扫描、准备、元数据构建和预处理流水线，响应为 `200/200/200/202`，轮询任务最终 `completed`；临时进程和数据已清理。
- [x] 上述隔离资料加工链由统一入口真实代理完成，并生成候选数据集；已覆盖资料接入、扫描、转换/准备、清洗分块和数据集候选生成，未执行发布或共享数据切换。
- [x] 执行文件源到 YashanDB 的只读迁移预演：发现 9 个已登记 JSON 源，输出数量和 SHA-256 摘要到 `p0-migration-dry-run.json`；未连接数据库、未写入目标、未删除源文件。
- [x] 真实读取候选数据集 `dataset_1d7438983dc34062` 的质量状态并调用正式发布接口；因 `qualityPassed=false`、`publishable=false`，服务返回 `409 QUALITY_GATE_FAILED`，确认质量门禁阻止发布且未改变数据集状态。
- [x] 新增隔离环境统一入口真实上传 E2E：Chromium 通过管理员登录、打开“上传资料”、选择浏览器文件、真实分块上传、完成会话并创建批次；最终以资料清洗工作区重新读取的真实批次名称和关闭的上传对话框验收。
- [x] 资料清洗工作区接入 `/index/stats` 真实只读投影，新增“索引与图谱”统计面板及服务不可用降级状态；未在前端重算或生成演示数据。
- [x] 隔离真实后端浏览器门禁补充匿名写入拒绝场景：匿名用户对上传会话请求返回 `401 AUTH_REQUIRED`，页面不授予任何资料清洗写权限；上传成功黄金流程与匿名拒绝场景共 2 项 Chromium 测试通过。
- [x] 在隔离认证服务中将内置测试用户临时调整为 `REVIEWER`（仅读权限）并真实调用上传端点，后端返回 `403 CLEANING_PERMISSION_DENIED`；验证后测试进程退出，未影响共享权限数据。
- [x] 真实验证不存在数据集的图谱摘要查询：统一入口返回 `404` 及资源不存在错误码，不返回空图谱或演示数据。
- [x] 隔离真实后端上传不可预览二进制文件并调用预览接口，返回 `415 FILE_PREVIEW_UNSUPPORTED`；确认不以空白内容代替能力边界。
- [x] 真实验证取消上传会话后的恢复边界：取消成功后继续登记文件返回 `400 UPLOAD_FILE_INVALID`，不会继续写入已取消会话。
- [x] 资料清洗隔离 Chromium/API 门禁现累计覆盖 6 个场景：管理员上传批次、匿名 `401`、只读角色 `403`、不存在数据集 `404`、不可预览文件 `415`、取消会话后续写入拒绝；各场景均使用临时认证/资料加工/网关拓扑，未污染共享数据。
- [x] 文档生产服务真实生成失败门禁：隔离启动文档服务调用 `/api/document/generate`，外部模型因区域限制返回 `500`，失败发生在写产物前，未生成“重构门禁测试”文档；该结果登记为外部模型凭证/区域前置，不冒充生成流程通过。
- [x] 审核发布真实写闭环复跑：隔离环境补齐合成正式基线后，真实 Chromium 场景通过阅读、差异、行级意见、回复、处理/重开、审核通过和确认发布；审核依据使用默认折叠证据抽屉，意见提交定位当前可见表单。临时认证、服务和状态文件均在进程退出后清理，未修改共享正式数据或外部 GitLab。
- [x] 下载任务服务层补齐页面边界取消：队列态和运行态登记取消信号，页面处理前后检查信号，进入 `cancelling -> cancelled`；保留未完成页面并允许 `resume`，批次保持 `downloading`，不会因取消误标记完成。
- [x] 新增下载取消服务测试和真实 FastAPI 取消路由契约测试；取消接口返回完整 `TaskSnapshot`，取消语义未改变旧下载入口。

## 未完成与后续门禁

- [ ] 资料清洗写操作目前仅通过统一入口薄适配转发，尚未迁入统一工作区的领域命令和数据库事实源；继续由 Python 资料加工服务执行。
- [ ] 尚未完成全量历史 JSON/目录到 YashanDB 的数量、哈希和业务回读对账，不能切换单主写。
- [ ] 已形成 SSE、下载和预览的机器可读协议快照，并完成代理透传、登录态 SSE、请求头/查询参数续接、文件基础权限与后端 TestClient 契约验证；无权限/资源不存在/范围失败场景、真实浏览器断线重连/取消恢复组合和所有响应字段级 Schema 仍未完成。当前取消仅覆盖下载任务页面边界，不代表页面内部强制中断或所有任务通用取消。
- [x] Socket.IO 已通过真实 `socket.io-client` 连接、`subscribe`/`unsubscribe` ack 和 `task_id` 房间语义回归（`socket-io-compatibility.test.js`）。
- [ ] v1 契约已完成 Python FastAPI 路由登记、运行时白名单、JSON 外壳校验及 SSE/文件协议快照；字段级映射和全量真实样本仍未完成。
- [ ] 尚未完成资料接入加工、文档生产、审核发布三类黄金流程的统一入口浏览器 E2E 全流程；资料上传写入和审核发布主写闭环已在隔离环境打通，但仍需索引/图谱、数据集发布、文档产物生成、审核退回和失败恢复全量门禁。
- [ ] 不得删除旧入口、改变保留期、修改已发布数据或处理事实源冲突；这些事项仍需人工决策。

## 可回退点

- 资料清洗适配层为新增白名单路由，旧 `/pingcode-api/*` 未修改；回退应用版本即可恢复占位页面，资料加工数据不被改写。
- 本批次未执行数据删除、迁移切换或不可逆配置变更。

## 证据命令

```bash
node tools/knowledge-processing/generate-knowledge-center-p0-baseline.js
node --check code/code/code/apps/knowledge-center-web/frontend-server.js
node --check code/packages/agent-runner-core/routes/knowledge-center.js
npx jest tests/agent-runner/knowledge-center-cleaning-contract.test.js tests/agent-runner/knowledge-platform-compatibility.test.js tests/agent-runner/platform-context-gateway.test.js tests/agent-runner/socket-io-compatibility.test.js --runInBand
curl http://127.0.0.1:14110/api/health
curl http://127.0.0.1:18010/api/health
curl http://127.0.0.1:13510/knowledge-center/api/cleaning/workbench/summary
```

最后一条在未登录状态应返回 `401 AUTH_REQUIRED`；登录态验证需使用受控测试账号并纳入后续浏览器门禁。

P1 适配批次的真实 HTTP 采样（2026-09-09）：

- 登录态 `GET /knowledge-center/api/cleaning/workbench/summary` 返回 `200`，读取 Python 资料加工真实工作台数据。
- 登录态 `POST /knowledge-center/api/cleaning/upload-sessions` 使用空参数触发上游校验，网关返回 `422 VALIDATION_ERROR`，并保留 `requestId=smoke-write-2`、`correlationId=smoke-correlation-2`。
- 未登记路径 `/knowledge-center/api/cleaning/internal/debug` 返回 `405 CLEANING_ROUTE_NOT_ALLOWED`。

P1 端点登记收口批次的真实 HTTP 采样（2026-09-09 11:16:29 +0800）：

- 文档服务 `GET http://127.0.0.1:14110/api/health` 返回 `200`、`status=ok`。
- 资料加工服务 `GET http://127.0.0.1:18010/api/health` 返回 `200`、`status=ok`。
- 统一入口 `GET http://127.0.0.1:13510/knowledge-center/` 返回 `200`，页面为中文知识中心外壳。
- 未登录清洗查询 `GET /knowledge-center/api/cleaning/workbench/summary` 返回 `401 AUTH_REQUIRED`。
- 未登记路径和未暴露删除路径均返回 `405 CLEANING_ROUTE_NOT_ALLOWED`；没有因端点登记扩展而绕过权限或开放删除操作。

P1 统一入口浏览器回归（2026-09-09 10:30:00 +0800）：

- 资料清洗视图在加载、成功和降级状态均保留可见的“资料清洗”页面标题；全局隐藏 `h1` 仅作技术标题。
- `tests/e2e/knowledge-center-entry.spec.js` 中的资料清洗/文档生产场景改为断言当前真实工作区语义，不再引用早期 demo 的“来源选择/生成策略/生产任务”三标签。
- 使用隔离服务真实执行 `KPG_BASE_URL=http://127.0.0.1:13510 npx playwright test tests/e2e/knowledge-center-entry.spec.js --project=chromium --workers=1`：12 个 Chromium 场景全部通过，无重试。
- 增量工作区 E2E 登录钩子在跳转后增加真实 URL 断言，避免未建立管理员会话就访问深链。

本次证据只证明统一入口的导航、回退、降级、四视口和两个工作区语义可用，不证明资料加工上传至数据集发布的全流程已完成。

本批次新增证据只证明端点登记已从代码内嵌数组迁移到版本化契约，并完成结构校验和受保护路由回归；不证明资料加工全流程已迁入统一领域服务或 YashanDB 单主写。

P1 流式/文件协议快照批次（2026-09-09 11:32:13 +0800）：

- 新增 `packages/platform-contracts/knowledge-center/v1/stream-download-contract.json`，将 SSE、文件预览、文件内容和文件下载契约纳入版本控制。
- `knowledge-platform-compatibility.test.js` 新增协议快照测试，确认 Python 实现包含 4 条 `text/event-stream` 路径及 `X-Accel-Buffering: no` 语义，并确认 3 条文件深链路径存在。
- 定向 Jest 4 个套件、27 项测试通过；该批次尚未包含登录态流式证据，后续批次已补充真实连接和请求关联头验证。

P1 统一响应契约校验与浏览器回归（2026-09-09 11:26:39 +0800）：

- 运行时契约模块 `code/packages/agent-runner-core/lib/knowledge-center-runtime-contract.js` 已接入资料加工适配代理；下游返回不符合 v1 外壳时统一转为 `502 KNOWLEDGE_CONTRACT_INVALID`，不改变旧 `/api/*` 行为。
- 使用 `KPG_BASE_URL=http://127.0.0.1:13510 npx playwright test tests/e2e/knowledge-center-entry.spec.js --project=chromium --workers=1`，12 个 Chromium 场景全部通过。
- 已确认 Python 任务模型存在 `completed/paused` 等旧状态；当前仍只校验传输外壳，不强制套用尚未完成兼容映射的 v1 任务状态枚举，避免功能退化。

P1 非 JSON 透传契约补强批次（2026-09-09）：

- 定向 Jest 5 个套件、29 项测试通过，包含真实 Node 上游/代理 SSE 与二进制响应测试；Python `unittest` 6 项通过（SSE 5 项、文件处置 2 项）。
- 已完成 `node --check`、`python3 -m py_compile` 和 `git diff --check`。
- 该批次未执行真实数据库迁移、发布数据变更或文件清理。

P1 统一入口登录态 SSE 实测批次（2026-09-09）：

- 使用隔离认证服务、统一前端网关和真实资料加工服务启动拓扑，管理员登录返回 `200` 并取得会话 Cookie。
- 登录态 `GET /knowledge-center/api/cleaning/workbench/summary` 返回 `200`，读取真实资料加工工作台数据。
- 登录态 `GET /knowledge-center/api/cleaning/tasks/task_a9bf21af05c14d11/events` 返回 `200`，响应头为 `text/event-stream; charset=utf-8`、`Cache-Control: no-cache`、`X-Accel-Buffering: no`，并回传请求关联头；首个数据块为 `: connected`。
- 四条 SSE 实现路径均在事件生产前发送 `: connected` 连接前导，避免无事件任务首字节等待轮询间隔。
- 新增 broker 级回归：连接前导不占用业务序号，`lastEventId=1` 只重放 `id > 1` 的事件，验证断线续接游标语义。
- SSE 续接兼容补强：`/api/tasks/{taskId}/events` 与 `/api/training/tasks/{taskId}/events` 在未提供 `lastEventId` 查询参数时读取 `Last-Event-ID` 请求头；查询参数优先。Python SSE 回归 7 项通过，定向 Jest 3 套件 17 项通过。
- 任务 ID 来自真实已完成下载任务查询结果；本次只读订阅并主动断开，没有创建、修改或删除业务任务。

P1 统一入口文件权限与深链实测批次（2026-09-09）：

- 使用真实资料加工批次 `batch_31583e4c802a4b83` 的授权资源 `4a10dce7bc4220630b561f03`，管理员登录态通过统一入口读取 metadata、preview、content、download，均返回 200 且内容长度与资源记录一致。
- 预览/内容响应保持 inline 语义，下载响应保持 attachment 语义，并回传请求关联头。
- 未登录访问同一文件预览真实返回 `401 AUTH_REQUIRED`；本次未执行删除、路径写入或业务数据变更。
- 文件服务现在落实资源级 `downloadable=false` 拒绝规则，返回 `403 FILE_DOWNLOAD_FORBIDDEN`，并有 Python 回归测试覆盖。
- 生成并纳入 `packages/platform-contracts/knowledge-center/v1/openapi.json` 字段级契约快照；端点契约测试已验证 117 条登记端点均能映射到 FastAPI OpenAPI 操作。

P1 后端流式/文件契约复核批次（2026-09-09 13:48:37 +0800）：

- `PYTHONPATH=apps/pingcode-api python3 -m unittest tests/pingcode/web-backend/test_sse_compatibility.py tests/pingcode/web-backend/test_file_response_contract.py`：9 项通过，覆盖 SSE 续接游标、连接前导、查询参数优先级、文件预览 inline、下载 attachment 和不可下载资源拒绝。
- `npx jest tests/agent-runner/knowledge-center-cleaning-contract.test.js tests/agent-runner/knowledge-platform-compatibility.test.js tests/agent-runner/proxy-streaming-contract.test.js tests/agent-runner/socket-io-compatibility.test.js --runInBand`：4 个套件、18 项通过。
- `python3 -m py_compile apps/pingcode-api/app/main.py apps/pingcode-api/app/services.py` 与 `git diff --check` 通过。
- 本批次只复核协议与回归证据，未执行 YashanDB 导入/单主写切换、已发布数据修改、文件清理或外部模型凭证配置。

P1 文件范围响应批次（2026-09-09 13:54:37 +0800）：

- 三个文件深链（预览、内容、下载）增加单一 `bytes` 范围支持：有效请求返回 `206`、`Content-Range`、`Accept-Ranges` 和准确 `Content-Length`；不支持多范围或越界范围返回 `416 FILE_RANGE_INVALID`，不读取越界数据。
- 无 `Range` 请求继续使用原有 `inline`/`attachment` 处置语义，资源级预览/下载权限和 `FILE_NOT_FOUND` 行为不变。
- `test_file_response_contract.py` 11 项（含 SSE）和 Node 3 个契约套件 17 项通过；`py_compile` 与 `git diff --check` 通过。
- 已将范围语义写入 `packages/platform-contracts/knowledge-center/v1/stream-download-contract.json`；本批次未执行数据库迁移、发布数据变更或文件清理。
