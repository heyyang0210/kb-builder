# 提示词与认知复盘记录

> 生效日期：2026-08-20
>
> 状态：强制执行
>
> 单一入口：本文件是生效日期之后重要提示词、决策和认知复盘的唯一记录入口。`prompt-log.md` 仅作为历史档案保留，不再新增记录。

## 一、建设目的

记录提示词不是为了堆积聊天历史，而是为了建立一条可审计的改进链：

`原始问题 → 当时理解 → 隐含假设 → 证据与反例 → 认知或逻辑问题 → 修正动作 → 验证结果 → 可复用经验`

通过周期分析这些记录，识别需求理解、概念定义、架构判断、任务拆解、验证方法和沟通方式中的重复性问题，持续提升个人认知、团队决策质量和工程化交付能力。

## 二、强制记录范围

以下提示词必须记录：

1. 引发需求、设计、代码、配置、测试、数据或目录变更的用户指令。
2. 确认或改变产品边界、架构、安全、性能、公共接口、外部依赖和发布策略的决策。
3. 用户指出理解错误、逻辑不清、遗漏、执行过慢、表达歧义或结果不符合预期的纠偏意见。
4. 导致返工、测试失败、人工接管、范围变化或重要方案取舍的提示词。
5. 对后续任务具有复用价值的成功约束、反例和验收方法。

简单问候、无决策影响的状态查询和重复确认可以不记录。若无法判断是否重要，默认记录。

## 三、完成门禁

- 重要任务开始时，先检查本文件中是否已有相同问题或约束。
- 发生关键确认或纠偏时，在继续实施前补充决策记录。
- 任务结束前必须补齐执行结果、验证证据、认知问题和后续改进；未记录不得将任务标记为“已完成”。
- 同一提示词只维护一条主记录；后续变化以“补充记录”追加，不覆盖原始判断。
- 提示词涉及多个连续确认时，可以合并为同一主题，但必须保留每次改变结论的关键信息和时间顺序。

## 四、记录内容规范

每条记录至少包含：

| 字段 | 要求 |
|---|---|
| 记录编号 | 使用 `提示词-日期-序号`，例如 `提示词-20260820-001` |
| 时间与角色 | 记录日期、时间和提示词发起角色 |
| 原始提示词 | 原文记录；内容过长时保留完整文件引用和不失真的核心摘要 |
| 目标与期望结果 | 用可验证语言说明用户真正要解决的问题 |
| 当时的关键理解 | 只记录可审计的判断摘要，不记录模型隐藏思维链 |
| 假设与不确定项 | 区分已确认事实、合理假设和待确认决策 |
| 认知与逻辑检查 | 检查概念混淆、先入为主、因果跳跃、范围膨胀、证据不足和替代方案遗漏 |
| 执行动作 | 记录实际修改、查询、测试或决策，不把计划写成完成 |
| 验证证据 | 提供文件、测试、接口结果或人工确认位置 |
| 结果与残余问题 | 说明完成、条件完成、失败或阻塞，以及仍需谁确认 |
| 可复用经验 | 提炼能约束后续任务的规则，不写空泛感想 |

## 五、认知与逻辑分析清单

复盘时至少回答以下问题：

1. **问题定义**：解决的是根因、表象，还是用户提出的某个既定方案？
2. **概念边界**：关键术语是否定义清楚，是否把不同对象或状态混为一谈？
3. **事实与假设**：哪些来自代码、文档、运行证据或用户确认，哪些只是推测？
4. **因果完整性**：方案为什么能解决问题，中间是否缺少必要条件？
5. **一致性**：需求、设计、任务、实现、测试和汇报是否使用同一口径？
6. **替代方案**：是否存在更小、可逆、低成本或更早验证价值的做法？
7. **失败边界**：方案在什么条件下失效，如何停止、回退和人工接管？
8. **验证有效性**：现有证据验证了真实目标，还是只验证了容易通过的代理指标？
9. **认知偏差**：是否出现过早定论、技术方案先行、术语自明、过度设计或只确认支持性证据？
10. **工程改进**：下次怎样通过模板、检查、自动化或确认门禁避免重复犯错？

## 六、安全与隐私约束

- 不记录密码、令牌、私钥、个人敏感信息和未授权业务数据；必须使用脱敏摘要。
- 不记录模型隐藏思维链，只记录对协作和审计有价值的判断摘要、证据、反例与结论。
- 引用外部内容时记录来源和版本，不复制无必要的大段受限内容。
- 提示词本身包含错误或敏感内容时保留问题性质，但应脱敏并注明处理方式。

## 七、记录模板

```markdown
## 提示词-YYYYMMDD-NNN：主题

- 时间：YYYY-MM-DD HH:mm
- 发起角色：用户 / 产品负责人 / 开发人员 / 测试人员 / 智能代理
- 类型：需求 / 设计 / 实现 / 测试 / 决策 / 纠偏 / 复盘

### 原始提示词

> 原文或不失真的核心摘要

### 目标与期望结果

### 关键理解、假设与待确认项

### 认知与逻辑检查

### 执行动作与证据

### 结果与残余问题

### 可复用经验
```

## 八、首批复盘记录

## 提示词-20260901-001：启动大纲管理模块多角色开发

- 时间：2026-09-01
- 发起角色：用户
- 类型：实现 / 决策

### 原始提示词

> 大纲文件统一放在 outlines 目录进行管理，结合设计评审启动多角色开发；不确定、风险项记录到知识中心建设平台相应目录。

### 目标与期望结果

在不改变大纲文件统一存储边界的前提下，先交付可回退的候选版本垂直切片，并让未冻结的权限、并发、稳定 ID 迁移和 Agent 能力进入风险清单。

### 关键理解、假设与待确认项

- 已确认：`outlines` 是当前阶段文件适配器目录；上传或导入不得直接形成生产发布基线。
- 合理假设：先实现创建、导入、候选编辑、预检、发布和双入口读取，再逐步接入责任、模板和 Agent。
- 待确认：YashanDB 仓储切换时机、正式权限矩阵、稳定 ID 迁移规则和幂等/基线冲突契约。

### 认知与逻辑检查

设计文档完整不等于实现契约完整。本轮采用垂直切片，避免同时开发所有联动导致状态机和事实源不一致；旧上传接口保留兼容，但行为改为生成草稿。

### 执行动作与证据

- 后端新增候选创建、导入、版本查询、节点编辑、预检和发布接口，文件仍写入 `agent-runner/outlines`。
- 前端支持按 `handbookId` 跳转详情、真实文件导入和候选提示。
- `tests/outline-management.test.js` 与 `tests/knowledge-center-outlines-view.test.js` 共 9 项通过；`git diff --check`（本轮文件）通过。

### 结果与残余问题

最小候选版本闭环已完成。责任映射写入、模板推荐、逆向提取、权限校验、幂等和稳定 ID 迁移仍未完成，不得标记为大纲管理整体完成。

### 可复用经验

先冻结“候选不污染发布基线”的不变量，再扩展页面和 Agent；每个新增写接口必须有状态、版本和事实源说明。

### 提示词-20260820-001：先理顺构建思路再写汇报正文

- 时间：2026-08-20
- 发起角色：用户
- 类型：纠偏

#### 原始提示词

> 感觉直接生成的这轮文档，其实没有把整体思路理顺，可以先生成一个构建的思路文档。

#### 目标与期望结果

先明确汇报目标、听众、叙事主线、模块依赖和风险边界，再据此生成正式汇报稿，避免局部内容齐全但整体逻辑松散。

#### 关键理解、假设与待确认项

- 已确认：现有正文不能靠逐段润色解决结构问题。
- 修正方向：将“结构化、生成、增量、治理”从四个并列功能改为前后依赖的知识生命周期。

#### 认知与逻辑检查

- 问题：过早进入正文生成，把“覆盖所有输入点”等同于“整体逻辑成立”。
- 根因：缺少成稿前的受众目标、核心判断和因果主线设计。
- 反例：即使每个功能都写到，听众仍可能无法理解为什么需要这些能力以及它们如何形成闭环。

#### 执行动作与证据

- 新增 `agent-runner/docs/overview/知识平台工程化技术汇报构建思路.md`。
- 文档先定义三个听众判断，再定义总体叙事主线和各模块统一讲法。

#### 结果与残余问题

- 构建思路已形成；正式汇报稿仍需依据新主线重写，不能直接把旧稿视为定稿。

#### 可复用经验

复杂汇报先写“听众最终应形成什么判断”和“模块之间为什么有这个顺序”，再写正文。内容覆盖率不能替代逻辑完整性。

### 提示词-20260820-002：从文档生成演示工具升级为知识中心

- 时间：2026-08-20
- 发起角色：用户
- 类型：需求

#### 原始提示词

> YashanDB 知识库文档生成器目前只是文档生成的演示级平台工具，需要支持多人并行文档生产，并升级为包含大纲、模板、权限、版本、审核等能力的知识中心管理平台；还需要头脑风暴其他能力，形成逻辑自洽的需求文档，不确定项交互确认。

#### 目标与期望结果

形成知识中心平台总体需求，不是简单增加功能菜单，而是建立从规划、生产、审核、发布、检索到反馈演进的完整生命周期。

#### 关键理解、假设与待确认项

- 已确认：单企业多知识空间；首期章节级异步协作并为实时协同预留；审核流程由空间配置一级或多级。
- 已确认：PingCode/GitLab 首期只读获取来源快照，不双向回写。
- 待确认：哪些文档版本允许进入正式知识检索范围。

#### 认知与逻辑检查

- 风险：把“知识中心”理解为大纲、模板、权限、版本、审核等菜单的集合。
- 修正：以知识资产生命周期组织能力，并将权限、审计、质量、反馈和运营作为跨生命周期治理能力。
- 范围控制：实时同段协作、外部发布和多企业租户不应未经验证直接进入首期。

#### 执行动作与证据

- 总体需求归档到 `.codex/知识中心建设平台/01-需求/知识中心-平台-总体需求-需求文档.md`。
- 建立 `.codex/知识中心建设平台/` 中文工程档案目录，分区承载需求、设计、任务、测试、验收、风险、进度和复盘。

#### 结果与残余问题

- 总体需求处于评审中；正式知识检索准入、身份来源、密级、外部发布等决策仍需确认。

#### 可复用经验

平台级需求必须先定义稳定业务对象、生命周期和不变量，再进行模块拆分；模块名称不能替代端到端验收场景。

### 提示词-20260820-003：区分文档版本与正式知识检索

- 时间：2026-08-20
- 发起角色：用户
- 类型：概念纠偏

#### 原始提示词

> D-11 正式知识库准入中的“索引”是什么意思？是否需要文档历史版本能力，至于接入数据库还是 GitLab，后续在具体模块实现时再设计？

#### 目标与期望结果

澄清产品能力和技术实现的层次，避免用“索引”这一技术词混淆文档历史、发布状态和知识检索准入。

#### 关键理解、假设与待确认项

- 文档版本库保存草稿、审核候选、发布版和退役版历史，是必备产品能力。
- 正式知识检索范围决定哪些版本可被搜索、问答引用和后续生成复用。
- 数据库、文件仓库、GitLab 或组合存储属于后续架构选型。

#### 认知与逻辑检查

- 问题：默认听众理解“索引”，并一度把发布状态、正式知识和检索准备状态写在同一语义中。
- 修正：在需求中增加概念对照表，并拆分文档资产状态和检索准备状态。

#### 执行动作与证据

- 已在知识中心总体需求的“文档版本与检索数据的区别”章节中明确边界。

#### 结果与残余问题

- 技术方案保持开放；“仅审核发布版本进入正式知识检索范围”仍待产品确认。

#### 可复用经验

需求文档优先使用业务语言。技术术语首次出现必须定义，并说明它不决定哪些后续技术选型。

### 提示词-20260820-004：文档正文和文件名均需中文化

- 时间：2026-08-20
- 发起角色：用户
- 类型：纠偏

#### 原始提示词

> `.codex` 下面的需求、任务等文档需要使用中文描述；`REQ-KNOWLEDGE-CENTER-PLATFORM.md` 这类文件也需要改成中文，并建立知识中心建设平台专用目录承载后续中间文档。

#### 目标与期望结果

让中文化覆盖正文、状态、角色、业务文件名和归档入口，而不只是翻译正文内容。

#### 认知与逻辑检查

- 问题：第一次只把正文和状态改成中文，仍保留英文业务文件名和分散归档路径。
- 根因：把“中文描述”狭义理解为正文语言，没有确认文件命名和项目档案边界。
- 修正：使用中文业务文件名；`README.md` 仅作为工具可识别的目录入口例外；稳定编号放入正文元信息。

#### 执行动作与证据

- 需求改名为 `.codex/知识中心建设平台/01-需求/知识中心-平台-总体需求-需求文档.md`。
- 任务改名为 `.codex/知识中心建设平台/04-开发任务/知识中心-平台-建设需求规划-开发任务文档.md`。
- 建立九类中文工程档案目录并清理旧英文路径。

#### 结果与残余问题

- 当前知识中心业务文档无 `REQ-*`、`TASK-*` 英文文件名残留。

#### 可复用经验

用户提出“中文化”时，应明确检查界面、正文、元数据、文件名、目录名和索引链接六个层次。

### 提示词-20260820-005：建立提示词记录与认知复盘强制约束

- 时间：2026-08-20
- 发起角色：用户
- 类型：治理要求

#### 原始提示词

> 需要将提示词的信息记录到当前项目的 prompt.md 文档中，作为一个强制约束，以便后续能够通过提示词进行分析，找出自己的认知和逻辑上的问题，以此提升自己的认知和工程化能力。

#### 目标与期望结果

建立持续、可审计、可分析的提示词记录机制，使纠偏和失败能够转化为后续工程约束。

#### 认知与逻辑检查

- 现状问题：`prompt-log.md` 侧重历史执行结果，缺少强制范围、完成门禁、认知分析字段和安全边界。
- 风险：只保存原始提示词会形成信息堆积；只写经验总结又会失去原始上下文和反证价值。
- 修正：同时记录原始信息、假设、证据、问题、行动、验证和可复用经验。

#### 执行动作与证据

- 新建本文件作为单一入口。
- 在 `AGENTS.md` 中增加强制记录和完成门禁。
- 将 `prompt-log.md` 标记为只读历史档案。

#### 结果与残余问题

- 规则已建立；后续还需通过周期复盘观察是否真正减少重复性认知错误，不能以“记录数量”代替改进效果。

#### 可复用经验

知识沉淀必须同时包含原始输入、判断变化和验证结果；日志存在不等于学习发生，必须把复盘结论转成下一次任务的检查项。

### 提示词-20260820-006：执行过程为什么过慢

- 时间：2026-08-20
- 发起角色：用户
- 类型：执行纠偏

#### 原始提示词

> 为什么执行得这么慢？

#### 目标与期望结果

在遵守仓库规划、审查和验证约束的同时，减少无效等待、重复读取和低价值流程开销，让用户及时获得可验证产物。

#### 关键理解、假设与待确认项

- 必要成本：复杂需求需由规划角色拆解，涉及权限和安全时需独立审查，补丁前需重新读取文件。
- 可避免成本：重复扫描大范围文件、等待非关键审查才反馈阶段结果、同一检查多次执行以及没有及时收敛待确认项。

#### 认知与逻辑检查

- 问题：把“流程完整”放在了“尽快形成最小可评审产物”之前。
- 因果缺口：更多检查不自动等于更高质量；只有能发现具体风险或提供验收证据的检查才有价值。
- 修正方向：先交付最小纵向成果，再并行做互不冲突的只读核对；状态更新说明已完成、正在做和剩余门禁。

#### 执行动作与证据

- 后续限定搜索和格式检查到本次目标文件，避免用全仓既有问题阻塞当前交付。
- 将知识中心 P0 收敛为真实样板空间的纵向薄切，而非等待所有平台能力一次完成。

#### 结果与残余问题

- 本轮后续操作减少了无关扫描，但仍需持续观察复杂任务的首次可评审产物耗时。

#### 可复用经验

强制流程应服务风险控制，而不是成为仪式。每个步骤都要回答“它会改变什么决策或补充什么证据”；不能回答时，应删除、合并或延后该步骤。

### 提示词-20260820-007：精简强制约束在 AGENTS.md 中的篇幅

- 时间：2026-08-20
- 发起角色：用户
- 类型：治理纠偏

#### 原始提示词

> AGENTS.md 中“提示词记录与认知复盘强制约束”的内容需要精简；AGENTS.md 已经足够长，不适合继续大段加入。

#### 目标与期望结果

保留提示词治理的强制性，同时减少工作空间总指引中的重复细节，让执行者能快速识别关键门禁。

#### 关键理解、假设与待确认项

- 已确认：`AGENTS.md` 只保留规则入口和强制门禁。
- 已确认：记录字段、分析清单、隐私和模板等详细规范继续由 `prompt.md` 单独维护。
- 待确认项：无。

#### 认知与逻辑检查

- 问题：首次把详细执行规范完整复制到总指引，造成信息重复和注意力稀释。
- 根因：把“强制约束”误解为“必须在入口文件完整展开”，忽略了入口规则与专项规范可以分层维护。
- 替代方案：完全删除会削弱约束；保留简短门禁并链接单一详细规范更合适。

#### 执行动作与证据

- 将 `AGENTS.md` 中 7 条详细规则压缩为 3 条，保留单一入口、触发与完成门禁、安全和追加原则。
- 详细字段、分析清单和模板仍保留在本文件。

#### 结果与残余问题

- 约束入口更短，详细规范没有丢失；需要通过后续任务观察执行者是否仍能稳定找到并遵守本文件。

#### 可复用经验

总指引只放高频、强制、可立即执行的规则；解释、模板和长清单下沉到单一专项文档，通过链接建立分层治理。

### 提示词-20260820-008：九个团队角色不等于每个任务启动九个 Agent

- 时间：2026-08-20
- 发起角色：用户
- 类型：编排纠偏

#### 原始提示词

> 这些角色是项目开发团队成员；需要找到适合任务的实施路径，不是每次都启动多角色。简单独立任务大可不必启动多角色模式。

#### 目标与期望结果

在保留九个团队职责边界的同时，为任务选择最小充分执行路径，让简单任务直接闭环，高风险或需要职责分离的任务再增量启用角色。

#### 关键理解、假设与待确认项

- 已确认：九个 Role 是团队职责目录，不是每次运行的固定实例集合。
- 已确认：简单、局部、低风险、可逆且验收确定的任务允许单执行者完成和自检。
- 已确认：架构、公共 API、外部依赖、安全、性能、生产数据和破坏性变更不能走直接路径。
- 待确认：自动路由运行时、版本化 Policy 的存放位置和校准阈值需另行设计审批。

#### 认知与逻辑检查

- 问题：原编排协议从收到需求直接进入固定多角色流水线，把职责完整性错误映射为运行实例完整性。
- 根因：缺少位于任务和 Role Contract 之间的路由层，无法表达直接处理、单执行者、分离交付和治理交付的差异。
- 修正：采用最小充分闭环；每增加一个角色都必须补充独立决策、验证、反证、审批、协调或投影价值。

#### 执行动作与证据

- 新增 Task Routing Contract，定义 L0/L1 直接路径、L2/L3 角色路径、路由接口、红线和动态升级伪代码。
- 将 `AGENTS.md` 的固定自动编排改为人工按需路由，并补齐 Architect 和 Project Manager。
- 更新 Role Contract 与设计文档导航，明确正式状态机只适用于 L2/L3。

#### 结果与残余问题

- 文档契约已形成；自动分类、路由执行和数据校准仍未实现，不能宣称已具备自动路由能力。

#### 可复用经验

Role 定义“谁有权负责”，Route 决定“本次是否需要他负责”。流程完整性的判断标准是责任和证据是否充分，不是启动角色的数量。

### 提示词-20260821-001：完全自动化开发目标缺少成立条件

- 时间：2026-08-21
- 发起角色：用户
- 类型：目标纠偏

#### 原始提示词

> 之前提出的能够完全自动化进行需求开发，在逻辑层面上是有漏洞的，这一块你为什么没有反驳我？给出逻辑说明和整改意见。

#### 目标与期望结果

修正顶层自治目标，避免用更多角色、工具或 Skill 掩盖需求真实性、独立验收、组织授权和残余风险无法完全自动决定的问题。

#### 关键理解、假设与待确认项

- 已确认：目标改为“人类治理下的边界自治”，不再宣称完全自动化开发。
- 已确认：AI 自治执行需要目标明确、范围有界、可机器验收、可回退且失败影响在授权范围内。
- 已确认：人类保留产品范围、架构、公共 API、外部依赖、安全、生产数据、不可逆操作、性能承诺和残余风险接受权。
- 待确认：具体审批人、审批粒度、等待行为和运行时 Policy 仍需后续交互确定。

#### 认知与逻辑检查

- 问题：此前过早接受“补齐角色、工具和 Skill 即可实现完全自治”的前提，没有先质疑目标是否可验证、可授权和可承担责任。
- 根因：把技术编排能力等同于业务决策权，把多 Agent 的意见分离误当成组织责任分离。
- 修正：AI 负责准备方案、证据、反例和授权范围内的执行；受限决策与残余风险由人承担。

#### 执行动作与证据

- Task Routing Contract 增加自治目标和五项直接执行前提。
- 跨模块从 L3 充分条件中移除，契约稳定、可回退且可验证时允许走 L2。
- 增加最小治理交付流程，明确自动阻断只来自已批准的确定性红线。
- 同步 `AGENTS.md`、Architect、Project Manager 和角色公共契约的人机边界。

#### 结果与残余问题

- 原则层逻辑已修正；自动路由、审批、Policy 和阻断仍未实现，不能声称治理交付已自动化。

#### 可复用经验

在设计“如何自动化”之前，必须先证明目标可机器验收、操作已获授权、失败责任有归属；无法证明时，正确方案是边界自治和人工裁决，而不是继续增加 Agent。

### 提示词-20260821-002：人工审批清单需要单一边界和分轮落地

- 时间：2026-08-21
- 发起角色：用户
- 类型：治理配置

#### 原始提示词

> 明确给出不需要审批和需要审批的清单，快速指导大模型判断；一次交互不足，本轮说明清单逻辑并给出下一步优化方案。

#### 目标与期望结果

让低风险工作在默认授权内快速执行，将业务权责、不可逆影响和重大风险接受交给人，并避免各角色维护不同审批口径。

#### 关键理解、假设与待确认项

- 已确认：审批按实际影响判断，不按“安全”“性能”“配置”或跨模块等关键词判断。
- 已确认：事实未知或批准范围不足时返回 `needs_decision`，不得由模型猜测放行。
- 待确认：具体审批人、审批粒度、有效期、跨任务复用、撤销、超时和运行时实现。

#### 认知与逻辑检查

- 过宽清单会把普通修复和性能优化都升级，破坏最小路径；过窄清单会让模型越权接受风险。
- Role 只能定义识别、记录、核验和停机责任，不能通过角色或 Skill 获得业务批准权。

#### 执行动作与证据

- 新增 `agent-runner/docs/41-Approval-Boundary-v1人工审批边界.md` 作为单一事实源。
- `AGENTS.md` 只保留放行、停机和不确定项三条规则；Architect、Planner、Project Manager、Worker、Test Engineer 和 Reviewer 按职责引用同一边界。
- 移除 Planner 和 Reviewer 中按性能/安全关键词一律升级的旧口径。

#### 结果与残余问题

- V1 已形成可人工执行的判断规范，但审批身份、授权生命周期和运行时强制尚未实现，不能宣称已自动化。

#### 可复用经验

审批治理先确定权责和影响边界，再定义记录生命周期，最后实现自动门禁；不能先写运行时或靠增加角色替代授权设计。

### 提示词-20260821-008：规则过多可能损害模型自治

- 时间：2026-08-21
- 发起角色：用户
- 类型：治理纠偏

#### 原始提示词

> 第一次配置不要把约束定义得过死；每次启动任务都加载大量规则，可能引入噪音、降低自主性并影响实际效果。

#### 关键理解与修正

- 该担心合理。规则的价值取决于降低高风险错误的收益，不能按文档完整度或约束数量衡量。
- 将上下文装载纳入路由：默认只加载任务目标、相关文件、当前约束和最小验收信息；角色、审批全文和独立审查规范按责任或升级信号加载。
- 低风险任务只需保留简短的审批判断；不要求每个角色重复生成完整审批字段。
- 安全、性能、配置和跨模块仅作为影响信号，不自动升级路径。

#### 结果与残余问题

- 已同步 `AGENTS.md`、Task Routing、Role README 和 Approval Boundary 的按需装载规则。
- 尚未有真实任务样本验证上下文长度、交接成本和质量变化，后续应通过代表性任务记录误升级、漏升级、耗时和返工，而不是继续增加静态规则。

### 提示词-20260821-006：将四级路由精简为三条执行路径

- 时间：2026-08-21
- 发起角色：用户
- 类型：治理决策

#### 已确认决策

- 路由采用“直接处理 / 标准开发 / 治理任务”。
- Bugfix 不新增路径：明确局部问题直接处理；需要补设计或独立验证时走标准开发；命中人工边界才治理。
- 用户明确提出的功能默认视为产品范围已授权；实现中扩大范围、改变业务规则或出现重大取舍时再确认。
- G0-G4 和完整状态机只用于项目级、高风险或明确要求正式验收的治理任务；普通代码开发只走“设计检查 -> 实施 -> 验证”。

#### 执行动作与证据

- 重写 Task Routing Contract 为三条路径和三个顺序判断。
- 同步 `AGENTS.md`、Role Contract、角色公共契约、Worker、Planner、PM、Test 和 Reviewer 的适用范围。
- 删除低风险任务默认 Reviewer 抽样及固定比例验收要求。

#### 残余问题

- 三条路径尚未经过真实任务对照验证；下一步应先回放代表性任务，观察耗时、上下文、返工和风险逃逸，不继续增加静态规则。

### 提示词-20260821-007：收窄全局开发约束和 PM 启用范围

- 时间：2026-08-21
- 发起角色：用户
- 类型：治理决策

#### 已确认决策

- “先设计后实现”只强制用于功能开发、公共契约和架构变更；明确根因的局部 Bugfix 与维护任务只需确认范围和验收方式。
- 真实后端 API 验证只强制用于涉及后端行为、接口或端到端流程的任务；其他任务采用与风险匹配的验证方式。
- 标准开发不默认启动 Project Manager；PM 仅用于治理任务、项目级计划/里程碑或用户明确要求。

#### 执行动作与残余问题

- 已同步 `AGENTS.md` 和 Project Manager 角色入口，不新增角色、状态或运行时机制。
- 仍需通过真实任务观察是否存在误升级或验证不足，再决定是否调整，不能据此继续增加静态规则。

### 提示词-20260821-003：按事实汇总本周项目进展

- 时间：2026-08-21 14:20:42
- 发起角色：用户
- 类型：汇报 / 事实核对

#### 原始提示词

> 汇总本周当前项目的进展，展示给领导查看，不要过度优化，按事实说话；不明确的内容交互式确认。

#### 目标与期望结果

按 2026-08-17 至 2026-08-21 的实际证据形成领导可读周报，区分已完成、进行中、阻塞和待确认事项，不把局部测试或需求文档扩大为生产完成。

#### 关键理解、假设与待确认项

- 已确认：本周提交、进度看板、日报、风险清单和知识中心需求文档是本次汇总的证据范围。
- 已确认：质量分析三项治理需求已实现并完成组合回归，但当前等待用户验收；M2、GraphStore 仍有明确阻塞。
- 已确认：知识中心专项本周完成需求和工程档案落盘，尚未进入代码开发。
- 待确认：本周各事项的业务负责人、领导关注的优先级、下一周承诺日期和是否同步回写总看板。

#### 认知与逻辑检查

- 防止把“组件回归通过”误写为“生产端到端通过”。
- 防止把旧看板“汇报正文尚未开始”误读为知识中心专项没有新增事实；周报同时标注看板状态和本周新增文档事实。
- 防止用提交数量代替交付价值；采用能力结果、测试数字和阻塞条件表达。

#### 执行动作与证据

- 新建 `.codex/知识中心建设平台/08-进度与报告/知识中心-平台-2026年第34周项目进展-进展报告文档.md`。
- 汇总质量分析 `3/3、10/10、36/36`，GraphStore 本地前置 `6/10、42/42`，M2 `187 项（185 passed、2 skipped）`、API-A `7/7` 等证据。
- 明确 ACL、真实训练出口、G2.5、外部图数据库和知识中心需求评审等未完成或待确认事项。

#### 结果与残余问题

- 周报按事实形成；未补写负责人、交付日期或生产上线结论。
- 领导仍需确认关注优先级、下一步承诺日期和是否将周报事实同步到项目总看板。

#### 可复用经验

周报先固定时间范围和证据清单，再按“结果—证据—边界—下一步”组织；没有验收证据的事项使用“设计完成、局部通过、等待确认或阻塞”等准确状态。

### 提示词-20260821-004：将周报压缩为业务可感知的三条进展

- 时间：2026-08-21 14:30:06
- 发起角色：用户
- 类型：汇报 / 表达调整

#### 原始提示词

> 将本周项目进展总结成对外业务可以感知的 3 条进展。

#### 目标与期望结果

将面向技术的周报内容转换为业务能够理解的价值变化，保留完成边界，不堆砌内部模块名、测试项和提交记录。

#### 关键理解、假设与待确认项

- 已确认：三条进展分别覆盖知识质量可追溯、知识生产可控、知识中心平台规划三个业务层面。
- 合理假设：此处“对外业务可以感知”指业务用户或管理者能理解的能力变化，不代表已经对外发布或正式商用。
- 待确认：后续对外材料是否需要补充业务负责人、客户案例或可量化业务收益；当前证据不足，不自行补写。

#### 认知与逻辑检查

- 不能把 GraphStore 等内部技术实现直接表述为业务能力；需说明其对知识可信、可追溯或后续平台能力的间接价值。
- 不能把“完成回归”写成“业务已正式使用”；保留用户验收、生产出口和安全门禁边界。
- 不能以提交数量替代业务成果；每条进展必须回答业务能看到什么变化。

#### 执行动作与证据

- 在 `知识中心-平台-2026年第34周项目进展-进展报告文档.md` 新增“面向业务的三条进展”章节。
- 三条内容分别对应质量分析治理、M2 知识治理链路和知识中心需求基线，并保留等待验收、真实生产出口未闭环和尚未上线等限制。

#### 结果与残余问题

- 已形成三条业务可读口径；未新增业务收益、客户反馈或上线结论。
- 如用于正式对外宣传，仍需业务负责人确认措辞和可公开范围。

#### 可复用经验

技术周报转业务汇报时，应按“业务能看到的变化—当前证据—尚未承诺的边界”表达；内部技术组件只有在能解释业务影响时才进入摘要。

### 提示词-20260821-005：移出当前阶段的统一知识资产目录并开展总体概要设计

- 时间：2026-08-21 15:21:17
- 发起角色：用户
- 类型：需求边界 / 架构设计 / 前端信息架构

#### 原始提示词

> “将分散的大纲、模板、素材、文档和知识关系纳入统一的知识资产目录”不是现阶段内容；总体需求方向基本没有问题，下一步需要构建总体概要设计，确定和划分模块，以此为基础进行前端模块标签设计；不明确或逻辑不正确的内容反馈出来。

#### 目标与期望结果

在不否定知识中心长期方向的前提下，收敛当前阶段边界，输出可供详细设计和前端信息架构使用的模块划分，并显式列出逻辑风险和待确认项。

#### 关键理解、假设与待确认项

- 已确认：现阶段不建设企业级统一知识资产目录、跨空间统一分类和知识关系目录。
- 合理假设：统一资产目录是后续可能建设的能力，而非永久取消；因此保留为后续候选，不作为 P0/P1 隐含前置。
- 合理假设：“前端模块标签”指一级业务导航标签；若用户指文档元数据标签，需要另行设计。
- 待确认：统一资产目录是延后还是永久不建设；发布是否拆成独立一级标签；普通阅读者是否需要独立知识门户；企业身份来源和首期角色矩阵。

#### 认知与逻辑检查

- 不能只删除产品目标中的一句话，仍保留统一分类、跨空间目录、统一门户等首期表述，否则需求自相矛盾。
- “知识中心”不必等于“统一资产目录”；当前可以先建设受控多人文档生产和发布闭环。
- 前端标签不能机械映射后端模块；来源、版本、权限和审计应有责任边界，但不应全部成为一级导航。
- 现有资料加工工作台、文档生成器和发布数据集不是目标知识中心对象，必须通过适配和迁移设计建立边界。

#### 执行动作与证据

- 将总体需求版本更新为 0.2.0，增加 D-13“现阶段不建设统一知识资产目录”，同步修正产品目标、非目标、生命周期、能力地图和空间模块表述。
- 新增 `02-概要设计/知识中心-平台-总体架构-概要设计文档.md`，定义 9 个责任模块、P0 主流程、单一事实源、现有能力复用边界、逻辑接口和风险。
- 新增 `02-概要设计/知识中心前端模块标签与信息架构设计.md`，定义 7 个一级业务标签、二级入口、角色可见性和路由草案。
- 同步更新需求、概要设计和工程档案 README 索引。

#### 结果与残余问题

- 已形成可进入评审的总体概要设计和前端标签设计；未修改前端代码、后端接口或数据库方案。
- 设计状态仍为“评审中”；O-01 至 O-05 未全部确认前，不进入详细设计和功能实现。

#### 可复用经验

范围收敛必须同时修正目标、非目标、生命周期、能力地图、模块需求、分期和验收，不只删除单句表述；前端导航应按用户任务组织，模块责任通过页面和状态边界体现。

### 提示词-20260821-006：纠正知识资产目录范围并评审概要设计

- 时间：2026-08-21 15:36:16
- 发起角色：用户
- 类型：需求纠偏 / 设计评审

#### 原始提示词

> 前一轮关于“知识资产目录不是现阶段内容”的意见有误，平台仍需承载知识资产目录；首期支持多部门但当前实际只有一个部门，前端保留扩展能力；模板可共享，一个大纲可选择一批模板，只有大纲管理员（手册负责人）可修改；同时询问审核管理、后端存储管理和右侧预览的边界，要求给出评审意见、逻辑说明和修改判断。

#### 目标与期望结果

逐条判断评审意见是否成立，识别连带影响和概念歧义；明确可直接修改项，并在涉及产品入口或权限所有权的事项上先请求确认。

#### 关键理解、假设与待确认项

- 已确认：知识资产目录属于现阶段范围，上一轮 D-13“不建设”的结论失效，必须以补充记录纠正而不是覆盖历史记录。
- 已确认：数据和权限模型首期支持多部门；当前实际只配置一个部门，前端不能强迫用户进行无意义切换，但需保留动态扩展能力。
- 已确认：模板可共享，一个大纲可绑定多个模板；大纲与模板的绑定关系需要受控修改。
- 待确认：知识资产目录是否作为一级导航；大纲负责人可修改的是“大纲与模板绑定关系”，还是也可修改共享模板正文。

#### 认知与逻辑检查

- 上一轮错误来自把用户一次未准确表达的范围意见直接固化为“已确认 D-13”，缺少与原始平台目标和其他能力的反向一致性确认。
- 知识资产目录是跨资产的统一浏览和组织视图，不等同于每类资产的编辑模块；是否作为一级导航属于信息架构决策。
- 共享模板正文与某个大纲选择哪些模板是两种不同权限；若由手册负责人直接修改共享模板，会影响其他大纲，必须拆开授权。
- 存储管理不应成为普通用户业务标签，但总体概要设计必须包含存储抽象、一致性、备份恢复和数据生命周期，当前设计确有遗漏。

#### 执行动作与证据

- 已复核总体需求、总体概要设计和前端信息架构中知识资产目录、空间、模板、审核、存储及预览相关表述。
- 本轮先形成评审结论，没有在产品入口和权限语义未确认前继续修改需求或设计正文。

#### 结果与残余问题

- 评论 1、2、4、5、6 均应修改；评论 3 的“模板绑定关系修改权”和“共享模板正文修改权”需拆分确认。
- 知识资产目录是否进入一级导航仍需用户确认；确认后统一修正三份文档和 D-13 状态。

#### 可复用经验

用户纠正范围时，应先检查其与既有目标、对象模型和信息架构的连带影响；涉及共享资产时必须区分“选择/绑定权限”和“资产内容修改权限”，不能用一个“可修改”覆盖两种责任。

#### 补充记录：2026-08-21 15:44:00

- 用户确认知识资产目录作为一级业务导航。
- 用户确认手册负责人既可调整大纲的模板绑定，也可修改共享模板正文；保存需记录版本并支持恢复，删除仅管理员可执行。
- 设计修正：将“前端模块标签”统一为“一级业务导航”，并补充其必要性：稳定工作域、角色边界、状态归属、深链恢复和渐进扩展。
- 设计修正：首期支持多部门但当前单部门；单部门隐藏无意义的部门选择器，多部门时自动开放切换。
- 设计修正：知识资产目录成为一级导航；“文档管理”并入知识资产视图；“审核管理”明确为“审核与发布”。
- 设计修正：总体概要设计新增 M10 知识资产目录和 M11 数据存储与资产仓储，补齐后端存储、一致性、备份恢复及数据生命周期边界。
- 安全边界：管理员拥有删除入口不等于可破坏历史引用；已发布或被引用模板只能停用或归档，只有未发布且未被引用的草稿可物理删除。
- 验证证据：总体需求更新为 0.3.0，总体概要设计与前端信息架构更新为 0.2.0，并同步需求、概要设计、工程档案和任务目录索引。

#### 验证补充：2026-08-21 15:54:52

- 前端设计文档已更名为 `知识中心-前端-一级导航与信息架构-概要设计文档.md`，消除“模块标签”和“一级导航”的术语混用。
- 需求、总体概要设计、前端信息架构、设计任务和各级 README 已完成链接与口径同步。
- `git diff --check`、新增文档独立空白检查、Markdown 代码围栏检查和 README 内部链接检查通过。
- 本轮仅修改需求与设计文档，未修改前后端代码、公共 API、数据库或生产配置。

### 提示词-20260821-007：补充 Excel 手册目录编排与前端表格维护

- 时间：2026-08-21 16:10:09
- 发起角色：用户
- 类型：设计评审 / 需求纠偏

#### 原始提示词

> 分派章节和具体的文档，是通过上传一个 Excel 表格进行更新，在平台前端有一个专门表格进行维护，这个表格的设计后续在详细设计中体现，并展示整个手册文档的目录结构。根据 Review 意见进行修改。

#### 目标与期望结果

将 Excel 批量导入、前端专用表格维护和整本手册目录展示纳入需求与总体概要设计，并明确它们与生产项目、章节任务的关系；不在概要阶段擅自冻结 Excel 字段和具体交互。

#### 关键理解、假设与待确认项

- 已确认：手册负责人通过 Excel 批量创建或更新目录节点、具体文档和负责人的编排关系。
- 已确认：平台前端提供专用表格维护同一份编排数据，并展示整本手册目录结构。
- 已确认：任务由工作台依据已确认编排版本生成，Excel 文件及行号不是任务或目录节点的稳定身份。
- 合理假设：Excel 是批量维护入口，平台表格是可视化维护入口，两者不能形成两套业务状态。
- 待详细设计：Excel 列定义、必填规则、稳定身份匹配、重复行处理、导入幂等、表格列和具体交互。

#### 认知与逻辑检查

- 原概要设计虽已在范围和模块职责中提到 Excel，但 P0 时序和逻辑接口仍沿用“负责人直接分派章节”，造成主流程口径断裂。
- 若上传即覆盖，会导致解析错误或误操作直接污染当前目录；因此需要“解析—差异预览—人工确认—原子应用”的受控流程。
- 若 Excel 和前端表格分别存储状态，会出现互相覆盖和责任不清；必须以版本化手册目录编排作为单一事实。
- 若使用 Excel 行号关联历史任务，插入、排序或移动行会造成引用失真；稳定身份和匹配规则必须在详细设计中专门处理。

#### 修正动作与证据

- 总体需求更新为 0.3.1，补充手册目录编排表、目录导入变更集、确认前不写入及整本手册目录展示要求。
- 总体概要设计更新为 0.2.1，补充模块关系、P0 时序、对象关系、逻辑接口、事件、伪代码、存储一致性、风险和详细设计顺序。
- 前端一级导航与信息架构设计更新为 0.2.1，在“大纲管理”下增加整本手册目录、目录编排表、Excel 导入、导入记录和差异确认，并定义目录树、主表格与按需预览的职责。
- 同步更新需求、概要设计和开发任务目录 README 及概要设计任务记录。

#### 结果与残余问题

- 需求和概要设计已统一为“Excel/平台表格维护编排，确认后生成任务”的主链路，本轮未修改代码、接口或数据库。
- Excel 字段和页面详细交互仍待详细设计，不应依据当前概要设计直接编码。

#### 可复用经验

业务补充不能只写入模块说明，还要反向检查主流程、对象模型、接口、事件、风险、验收和索引是否一致；对于批量导入，必须明确单一事实源、预检确认、幂等和稳定身份边界。

### 提示词-20260821-008：补充 CAS、文本评论、生产报表与审核分析接口

- 时间：2026-08-21 16:26:17
- 发起角色：用户
- 类型：需求补充 / 概要设计联动

#### 原始提示词

> 文档支持鼠标选择一段话后评论，类似 Word 审核；用户认证接入内部统一 CAS；文档生成需要展示整体进展和手册更新的报表；预留审核和修改记录分析接口，后续结合大模型和上传 Skill 分析。

#### 目标与期望结果

将四项产品事实纳入总体需求和概要设计，明确业务归属、数据边界、前端入口、接口意图、风险与验收，同时避免把 CAS 认证误写成平台授权、把报表或 AI 分析写成新的业务事实来源。

#### 关键理解、假设与待确认项

- 已确认：企业内部 CAS 是平台常规用户登录认证入口。
- 已确认：审核者可像 Word 一样用鼠标选择连续文本添加评论。
- 已确认：工作台需要展示整本手册的生产进展、更新和异常，并支持权限内下钻。
- 已确认：审核意见、回复、版本差异和修改结果需要结构化保存并预留授权分析接口；后续可结合大模型与上传 Skill 分析。
- 合理假设：CAS 只提供身份依据，平台继续负责用户映射、空间角色和资源授权。
- 合理假设：生产报表放入工作台，不新增一级导航；报表是源事实的只读投影。
- 待详细设计：CAS 属性映射、单点退出、会话与应急访问；报表刷新、导出与性能；文本锚点定位；分析 API、数据脱敏和上传 Skill 安全边界。

#### 认知与逻辑检查

- 原需求把行级评论增强放到后续阶段，与本次明确要求的 Word 式审核冲突，已调整为 P0 文本选区评论。
- 原 D-01 和 O-05 仍将身份来源列为待确认，已根据 CAS 决策关闭选型问题，仅保留实现参数待详细设计。
- CAS 登录成功不代表拥有文档权限，否则内部任意用户可能越权访问；认证和授权必须分开。
- 报表若自行保存可编辑状态会与任务、审核、版本产生冲突；只能保留口径化、可重建的只读投影。
- 大模型分析若覆盖原意见或自动改变审核状态，会破坏审核证据；原始记录与派生分析必须分离。
- 文本评论不能只保存字符偏移，正文修改后可能漂移；必须保存固定版本和原引用，无法可靠重定位时显式待确认。

#### 修正动作与证据

- 总体需求更新为 0.4.0，新增 CAS 决策、P0 文本选区评论、文档生产进展报表、审核记录分析接口及 D-14、D-15。
- 总体概要设计更新为 0.3.0，补充 CAS 与授权边界、M01 报表投影、M07 评论与查询契约、M09 分析运行、对象关系、逻辑接口、事件、存储和风险。
- 前端信息架构更新为 0.3.0，在工作台增加生产进展和手册更新，在审核页增加文本选区评论；审核分析接口暂不新增一级导航。
- 同步更新需求、概要设计和任务索引及版本记录。

#### 结果与残余问题

- 本轮完成需求与概要设计联动，未实现 CAS、页面、公共 API、数据表、模型调用或 Skill 执行。
- O-07 至 O-09 仍需在对应详细设计中确认；审核智能分析执行可后置，但结构化记录、授权接口与追溯边界应在 P0 保留。

#### 可复用经验

引入统一认证时必须分开认证与授权；引入报表时必须确定源事实和统计口径；引入 AI 分析时必须保留不可变原始证据、版本化执行输入和不可自动决策边界。

### 提示词-20260821-009：确定 YashanDB 目标存储与 Linux 文件渐进迁移路线

- 时间：2026-08-21 16:56:36
- 发起角色：用户
- 类型：架构决策 / 数据迁移演进

#### 原始提示词

> 后台文件存储需要考虑数据库，数据库选型确定为 YashanDB；现有文件采用 Linux 文件管理方式存储。给出逐步替换路线和逻辑，并根据 Review 意见补充和演进。

#### 目标与期望结果

将 YashanDB 作为目标业务数据库写入需求和概要设计，基于当前 Linux 文件目录与 `metadata.json` 现状形成可分批验证、可暂停、可回退且不会长期双写分叉的替换路线。

#### 关键理解、假设与待确认项

- 已确认：知识中心目标业务数据库为 YashanDB，不再把 SQLite 或 GitLab 作为文档业务事实的目标主库候选。
- 已确认：当前演示工具主要通过 Linux 文件目录、`metadata.json` 和路径引用保存大纲、文档及运行产物。
- 合理假设：先迁结构化元数据和版本关系，再迁正文，最后迁附件与运行产物，可更早建立权限、审计和事务边界，并降低大文件不确定性。
- 合理假设：迁移按空间、手册或资产批次执行，每批在线业务只能有一个写入主源；旧文件在切换后只读。
- 待详细设计：YashanDB 表结构、分区索引、普通正文与大对象边界、容量性能基线、备份恢复目标、迁移批次大小和运维审批方式。

#### 认知与逻辑检查

- 原需求仍称数据库、文件仓库或 GitLab 组合待选，与用户已确认的 YashanDB 选型冲突，必须纠正。
- 历史 `agent-runner/docs/16-数据库方案设计.md` 曾建议未来迁 SQLite，只能视为演示阶段历史方案；其仓储抽象思想可复用，但目标数据库结论已失效。
- “目标数据库是 YashanDB”不自动等于所有图片、附件和大型运行产物都必须直接入表；大对象方式仍需真实容量、备份恢复和访问性能验证。
- 长期在线双写会产生先后顺序、部分失败和冲突裁决问题；渐进迁移应采用批次唯一主写、旧源只读、幂等导入和摘要对账。
- 切换后产生的新数据库事实无法保证无损反写旧文件模型，因此回退应暂停新写、恢复兼容读取并输出差异清单，而不是承诺自动双向回滚。

#### 修正动作与证据

- 总体需求更新为 0.5.0，增加 YashanDB 持久化决策、迁移非功能要求、十项迁移要求、五阶段替换路线、双写与大对象风险及 D-16。
- 总体概要设计更新为 0.4.0，将 M11 明确为 YashanDB 数据存储与资产仓储，补充现状复用边界、迁移架构图、批次门禁、回退语义和详细设计顺序。
- 前端信息架构更新为 0.3.1，在平台管理中增加只读的“存储与迁移”摘要视图，不开放直接改库或删除旧目录能力。
- 将历史 `agent-runner/docs/16-数据库方案设计.md` 标记为演示阶段历史设计，明确 SQLite 目标方案失效并链接到新的 YashanDB 迁移设计，避免双重架构口径。
- 同步更新需求、概要设计和开发任务索引及版本记录。

#### 结果与残余问题

- 已确定从 Linux 文件主写逐步演进到 YashanDB 唯一业务事实主源的路线，本轮未创建数据表、迁移脚本、连接配置或执行实际迁移。
- 大对象方式、容量与性能基线、迁移批次和备份恢复目标仍需在 M11 详细设计与真实样本验证后冻结。

#### 可复用经验

数据存储替换应先分清目标业务事实库和大对象字节存放方式，再以仓储抽象隔离业务代码；迁移必须为每个批次定义唯一主写、内容摘要、关系权限校验、切换门禁和真实可执行的回退语义。

### 提示词-20260824-001：补充通用前端设计 Skill

- 时间：2026-08-24 11:51:55
- 发起角色：用户
- 类型：能力扩展 / 外部 Skill 评估与安装

#### 原始提示词

> 当前项目缺少足够的前端设计 Skill 和 Agent 角色，需要从 GitHub 找 1 到 2 个专业前端设计 Skill 补充能力。这是通用请求，不使用当前仓库多角色能力；不合理则指出并改进。

#### 目标与期望结果

从公开 GitHub 仓库筛选、审计并安装 1 到 2 个互补的通用前端设计 Skill，增强视觉方向、信息层级、UX、无障碍、响应式和 Vue 等技术栈设计能力，同时避免把第三方推荐直接当作产品结论。

#### 关键理解、假设与待确认项

- 已确认：本次安装到用户级 Codex Skill 目录，不写入项目仓库，也不实例化仓库内多角色。
- 合理判断：单独增加“设计审美”不够，需要一个偏视觉方向的 Skill 和一个偏 UX 规则、响应式、无障碍及实现检查的 Skill。
- 已选择：Anthropic `frontend-design`，Apache-2.0；上游提交 `3b3fad96af16a10759d930941b4520ba0c40edae`。
- 已选择：Next Level Builder `ui-ux-pro-max`，MIT；上游提交 `bc826e2267a36d98a2dcf5231e16c30ff546770f`。
- 待后续验证：真实知识中心页面设计时，需要结合现有业务流程、中文内容、Vue 组件和浏览器截图验证 Skill 的实际收益。

#### 认知与逻辑检查

- Skill 可以增强方法和检查清单，但不能替代用户研究、企业品牌规范、交互原型评审或真实可用性测试。
- `frontend-design` 偏视觉个性，部分关于 hero 和大胆视觉的建议不适合后台管理平台，必须服从业务工具应安静、密集、可扫描的产品约束。
- `ui-ux-pro-max` 的自动设计系统把知识中心误匹配为运营落地页，并推荐视频集成、靛紫色和 Soft UI；说明其数据库结果只能作为候选，必须由业务语境和人工判断复核。
- 两个 Skill 内容互补，但存在颜色、布局和设计流程建议重叠；使用时应以项目约束和现有设计系统优先，不叠加所有建议。
- GitHub 子目录安装不会自动带入仓库根许可证；已补入 `ui-ux-pro-max/LICENSE` 并记录上游提交，保持来源可审计。

#### 执行动作与证据

- 使用官方 `skill-installer` 安装 `frontend-design` 到 `/home/hey/.codex/skills/frontend-design`。
- 使用官方 `skill-installer` 安装 `ui-ux-pro-max` 到 `/home/hey/.codex/skills/ui-ux-pro-max`。
- 完整读取两个 `SKILL.md` 和许可证；检查仓库活跃度、目录树以及脚本的网络、依赖和系统写入行为。
- `ui-ux-pro-max` 本地搜索命令成功，数据校验覆盖 12 个领域文件、22 个技术栈文件并通过。
- 可独立运行的 130 项测试全部通过；完整测试中的 2 项仓库维护测试因子目录安装缺少完整仓库根结构而无法导入，不影响核心搜索，但已作为安装形态限制记录。
- 两个 Skill 均记录上游仓库、路径和固定提交；未向当前项目新增依赖、代码或配置。

#### 结果与残余问题

- 两个通用 Skill 已安装，本轮没有创建新 Agent，也没有修改当前前端功能。
- 新安装 Skill 按安装器约束从下一轮开始被会话发现；本轮只能通过直接脚本验证安装内容。
- 后续前端设计不能直接持久化 `ui-ux-pro-max` 自动生成的设计系统，需要先针对知识中心后台场景评审其匹配度。

#### 可复用经验

选择外部设计 Skill 时应优先采用互补组合，并同时审查来源、许可证、脚本行为、数据质量和实际查询偏差；自动推荐是设计输入，不是设计决策，必须用业务语境、现有约束和截图验收形成闭环。

### 提示词-20260824-002：纠正前端设计 Skill 的绝对优劣判断

- 时间：2026-08-24 11:55:28
- 发起角色：用户
- 类型：选型质疑 / 认知纠偏

#### 原始提示词

> 这两个 Skill 比 tasting-skill 更好吗？怎么网上的评论是这种 Skill 更好呢？

#### 结论与修正

- 已确认事实：用户所指应为 `Leonxlnx/taste-skill`，其主 Skill 名为 `design-taste-frontend`；它对落地页、作品集、品牌展示页和“去 AI 模板感”的规则更强。
- 已确认事实：`taste-skill` 的上游说明明确将后台、高密度仪表盘、数据表格、多步骤产品界面和实时协作界面列为非适用范围。
- 合理判断：网上评价更高主要反映其展示型页面效果明显、规则易传播和社区热度高，不等于它对企业知识中心后台更适配。
- 修正原有表述：不再宣称已安装的两个 Skill 绝对更好；它们是在当前后台任务上覆盖更广、互补性更好，但仍不能代替 Carbon、Fluent 或 Atlassian 等企业后台设计系统基线。
- 待确认项：是否安装 `taste-skill` 作为第三个审美检查补充；未经用户确认不执行安装。

#### 认知与逻辑检查

- 概念混淆：把 GitHub Stars、网络评论和视觉案例传播度当成通用设计能力证据。
- 证据不足：当前三个 Skill 尚未在同一份知识中心页面任务上进行盲测，因此不能声称有经验证的综合排名。
- 范围检查：知识中心是多角色、多权限、表格密集、审核和版本流程明显的企业工具，选型权重应优先信息架构、任务效率、状态清晰度、无障碍和实现可行性，不应以营销页视觉冲击力为主指标。

#### 可复用经验

外部 Skill 选型必须先做“任务类型—适用范围—失败边界”映射，再看热度和案例；最终优劣应由同任务、同约束、同验收表的 A/B 产出确定。

### 提示词-20260824-003：为知识中心预留外部平台集成管理模块

- 时间：2026-08-24 15:04:28
- 发起角色：用户
- 类型：需求 / 架构边界 / 前端设计

#### 原始提示词

> 还需要预留出来一个与其他平台对接的管理模块，这一块用于与其他平台进行对接，后续演进这一部分，增强整个平台的集成和管理能力，借助于其他平台的能力，补充本平台的功能短板。

#### 目标与期望结果

在知识中心前端和总体设计中明确一个可进入、可演进的外部平台集成管理模块，使 PingCode、GitLab、GitHub 及后续平台的连接、能力、授权、同步任务和健康状态有统一承载位置；同时不把知识中心膨胀成外部平台的替代品。

#### 关键理解、假设与待确认项

- 已确认：采用策略 B；PingCode 首期只读获取并保存来源快照，GitLab 首期允许知识发布后异步创建文档分支、提交文档并创建 MR，GitHub 暂不进入 P0，仅预留同一连接器契约。
- 已确认：集成管理作为“平台管理”下的独立二级模块，固定路由 `/admin/integrations`；当前不提升为一级导航。
- 合理假设：M12 负责集成提供方目录、连接实例、能力授权、同步策略、事件/开放能力预留和健康投影；M04 负责连接器实际 API 调用、来源快照和外部动作执行；M09 负责长任务状态和审计。
- 待确认：后续优先补齐项目引用、事件订阅、通知、开放接口还是更细粒度同步；外部平台状态是否需要双向同步；GitLab MR 粒度、目标映射和失败升级时限仍由 D-18 详细设计确认。

#### 认知与逻辑检查

- 概念边界：不能把“集成管理”与“连接器运行”混为一个模块；前者管理能否接、允许做什么、在哪生效，后者按策略执行确定性 API 动作。
- 范围风险：用户提出“补充本平台功能短板”容易跳变为复制外部平台功能；修正为保存稳定引用、来源快照、同步结果和受控事件，不复制外部业务事实。
- 因果完整性：预留模块只有入口没有能力声明、授权、失败和审计，会形成空壳；因此需求增加连接实例、能力白名单、策略版本、任务恢复和审计验收。
- 失败边界：连接停用只阻止新任务，不删除历史快照和审计；GitLab 回写失败不回滚知识发布，进入重试或人工接管。
- 证据边界：Demo 不能把 GitHub 或事件订阅显示为已接入，所有后续能力必须标注“预留/未接入”。

#### 执行动作与证据

- 总体需求更新为 0.7.0：新增“集成管理与开放能力”能力域、7.13 集成管理模块、集成策略与能力授权对象、集成可演进性非功能要求、P0 管理壳和 D-21/D-22。
- 总体概要设计更新为 0.6.0：新增 M12，拆分 M12 配置治理、M04 连接器执行、M09 长任务状态，补充对象关系、逻辑接口、事件和风险不变量。
- 前端一级导航更新为 0.5.0：平台管理下增加集成管理语义、`/admin/integrations` 入口、角色可见性、上下文预览和验收项，并删除重复的旧入口描述。
- 外部连接器详细设计更新为 0.2.0：增加 M12 边界、集成管理页面结构、策略版本和集成管理验收。
- 新增《知识中心前端评审 Demo 详细设计》，说明需求足够性判断、页面范围、集成管理交互、演示假设和验收清单；Demo 页面新增集成管理子导航及 P0/P1 演进卡片。
- 修正工程档案索引、概要设计任务、需求规划任务和第 34 周周报中的旧“GitLab 全部只读”表述。

#### 验证结果

- HTML 内嵌脚本通过 `node --check`。
- Playwright 本地 Demo 验证通过：`1440×900`、`1024×768`、`768×1024`、`375×812` 四个视口均无页面级横向溢出，控制台无错误。
- 交互验证通过：集成管理标题和演进卡片、预留能力提示、PingCode 连接测试、GitLab 失败重试并生成模拟 MR、Word 式选区评论新增。
- 截图证据：`/tmp/knowledge-center-demo-platform.png`、`/tmp/knowledge-center-demo-desktop-wide.png`、`/tmp/knowledge-center-demo-mobile.png`。
- `git diff --check -- '.codex/知识中心建设平台'` 通过。

#### 结果与残余问题

- 已完成：集成管理模块在需求、概要设计、前端信息架构、详细设计和评审 Demo 中形成一致边界。
- 未完成：未连接真实 CAS、YashanDB、PingCode、GitLab 或 GitHub；未实现连接器代码、后端 API、凭证托管、真实同步和事件订阅。
- 残余决策：D-18、D-19、D-22 仍需在真实集成详细设计和安全评审前确认；Demo 的“每批次一个 MR”只是演示默认，不是最终生产规则。

#### 可复用经验

外部集成能力应按“管理配置 → 授权执行 → 异步任务 → 审计恢复”拆层，先建立统一契约和能力白名单，再按真实业务短板接入平台；预留入口必须同时标出未实现状态和失败边界，避免把规划能力误写成已上线能力。

### 提示词-20260824-004：修复核心对象关系图无法阅读

- 时间：2026-08-24 16:28:00
- 发起角色：用户
- 类型：Bugfix / 前端表现设计

#### 原始提示词

> 《知识中心-平台-总体架构-概要设计文档.md》第 8 节核心对象关系在 YashanDB 知识库文档生成器前端展示不出来，疑似图内容过于复杂；需要修复渲染，并考虑更好的表现维度。

#### 目标与期望结果

真实打开该文档时，第 8 节不出现 Mermaid 错误；读者可按业务域理解核心对象关系，同时仍能查看原始关系源。

#### 关键理解、假设与待确认项

- 已确认事实：39 条中文实体关系的 `erDiagram` 被 Mermaid 10.9.8 渲染为 `Syntax error in text`；同一文档其他 Mermaid 图可渲染。
- 设计判断：单纯放大或横向滚动不能解决高密度 ER 图的认知负担。
- 范围边界：本次只改文档阅读器的 Mermaid 表现，不修改业务对象模型、后端接口或数据库设计。

#### 认知与逻辑检查

- 根因与表象分离：存在真实语法兼容问题，但即便修复语法，单图关系密度仍不利于阅读。
- 替代方案比较：保留原图并增加缩放不能提供对象域层次；专用关系阅读器可以同时解决渲染失败和信息过载。
- 失败边界：只有 `erDiagram` 走专用解析；其他图继续 Mermaid；单个 Mermaid 失败只显示局部中文降级，不阻断整篇文档。

#### 执行动作与证据

- 前端新增关系阅读器：四个业务域、39 条关系、关系方向和可展开原始 Mermaid 源码。
- 新增复杂关系预览设计文档和 Playwright 回归测试。
- 实际浏览器验证：关系阅读器 1 个、业务分组 4 个、关系项 39 个、其他 SVG 图 3 个、无页面错误；600px 窄屏无横向溢出。

#### 结果与残余问题

- 已完成：第 8 节不再显示 Mermaid 语法错误，核心关系可以分组阅读。
- 残余问题：业务域关键词是当前阅读投影配置；若未来实体命名体系变化，应根据新增对象域调整关键词，不应把它误当作数据库分组规则。

#### 可复用经验

复杂图形的修复不能只看渲染成功率；对知识文档应同时验证语法兼容、信息密度、渐进披露、窄屏布局和原始证据可回查。

### 提示词-20260824-005：修正知识中心 Demo 预览、来源与责任映射边界

- 时间：2026-08-24 17:10:00
- 发起角色：用户
- 类型：Review 纠偏 / 前端 Demo / 预览接口 Bugfix

#### 原始反馈

> 每个平台要有具体接入配置和说明，平台统一使用 CAS；大纲是 Markdown，Excel 只维护手册、具体文档和责任人的映射且通常静态；全量生成可按规则确定来源但需人工确认，增量更新必须人工选择来源；需要完整抓取内容预览并作为原始素材；修复知识中心前端评审 Demo 的 Forbidden 展示错误。

#### 根因与判断

- 真实 API 对该 HTML 的 `/api/document/list` 和 `/content` 返回成功，但 `/api/document/raw/knowledge-center/...` 返回 403。
- 根因是 Express `sendFile` 的 `dotfiles: 'deny'` 与已登记的 `.codex` 根目录冲突；路径边界和符号链接校验本身有效，不能因此开放全局静态目录。
- 原有设计把 Markdown 大纲与责任映射 Excel 混成“目录导入”，并把全量/增量都简化为人工选源，造成业务对象和人工决策边界错误。

#### 执行动作

- 原样预览仅将已通过登记根目录、词法边界和 realpath 边界校验的单文件改为 `dotfiles: 'allow'`；新增知识中心 HTML 回归断言，保留路径穿越和符号链接拒绝测试。
- 需求、总体概要、前端信息架构和详细设计统一为：Markdown 负责结构版本；责任映射 Excel/平台表格负责稳定映射版本；全量生成先确认规则/范围，增量更新人工选择具体来源；抓取后先完整预览，人工固化来源快照后才可被生产引用。
- 详细设计增加 PingCode、GitLab、GitHub 及后续平台的接入配置字段、能力和说明；明确 CAS 只负责知识中心登录，外部 API 仍需独立连接实例授权。
- Demo 增加全量/增量模式、原始素材预览/快照状态、平台接入配置详情弹窗、Markdown/责任映射文案和黄金分割附近的适中中高密度布局。

#### 验证证据

- `node agent-runner/tests/document-raw-preview-api.test.js` 通过；知识中心 Demo 原样预览返回 HTTP 200、`text/html; charset=utf-8`，AI 体系认知 HTML 和安全拒绝用例仍通过。
- Demo 内嵌脚本通过 `node --check`，`git diff --check` 通过。
- Playwright 真实浏览器验证 1440×900、1024×768、768×1024、375×812：无页面级横向溢出、无控制台错误；平台详情弹窗、全量/增量模式、来源预览、责任映射页面均可见。

#### 残余问题

- 尚未接入真实 CAS、YashanDB、PingCode、GitLab 或 GitHub；当前平台详情、预览和固化动作仍是 Demo 模拟。
- Markdown 解析契约、责任映射字段、来源完整预览的内容上限/脱敏规则和真实连接凭证范围仍需详细接口和安全评审冻结。

### 提示词-20260824-006：补齐文档生产来源定位与预览交互

- 时间：2026-08-24 17:35:00
- 发起角色：用户
- 类型：前端设计评价 / 交互补强

#### 决策

- `taste-skill` 当前环境不可用，采用已安装的 `frontend-design` 与 `ui-ux-pro-max` 进行等价评价；不虚构 taste-skill 的调用结果。
- 来源控件采用“平台切换 + 最近来源下拉 + 网页/对象地址输入 + 获取预览 + 固化快照”组合。下拉降低定位成本，地址输入覆盖具体页面、文件、提交和 MR，预览与固化分离以保留人工确认边界。

#### 执行动作

- Demo 文档生产面板新增 PingCode/GitLab 来源选择器、最近来源快捷项、地址输入、抓取状态、远端版本/提交号、更新时间、完整性摘要和快照固化按钮。
- 详细设计新增来源选择交互表，明确全量模式确认规则与范围、增量模式人工选定具体对象、后端权限复核和 `snapshotId` 引用约束。

#### 验证要求

- 内嵌脚本通过 `node --check`。Playwright 已验证 GitLab 平台切换、地址输入、素材预览和快照固化；1440×900、1024×768、768×1024、375×812 均无页面级横向溢出，控制台无错误。

### 提示词-20260824-007：Taste-Skill 评审、手册完整度页面与 Demo 改名

- 时间：2026-08-24 17:52:22
- 决策：安装 `Leonxlnx/taste-skill/skills/taste-skill` 固定提交 `72e299530e2eb31ed8da06181bc19f6c18a00821`，MIT 许可。该 Skill 明确不适合 dashboard、复杂数据表格和企业产品 UI，本轮只采用其现状审计、反模板化、视觉一致性和交付预检规则。
- 设计参数：`DESIGN_VARIANCE 3 / MOTION_INTENSITY 2 / VISUAL_DENSITY 7`；维持中文后台、中高密度、低动效和约 1.6:1 主次布局。
- 执行动作：将 `知识中心前端评审Demo.html` 移动为 `知识中心管理-Demo.html`；增加手册详情四维完整度、统计上下文、目录缺口筛选、更新发布摘要和责任模块跳转；从工作台与知识资产进入同一只读视图。
- 权限演示：新增平台管理员角色，并对作者、审核者、发布者、管理员的来源、审核、发布、集成和重试按钮实施前端禁用提示；真实权限仍由 M08 后端校验。
- 文档边界：概要设计补充 M01/M06/M10/M08 的完整度投影接口和只读边界；详细设计增加 P0/P1/P2 用户视角问题清单。旧 HTML 文件不保留，历史记录中的旧名称仅作为改名前事实。

### 提示词-20260825-001：手册总览与关键状态泳道改造

- 时间：2026-08-25 10:41:11
- 发起角色：用户
- 类型：前端产品改造 / 指标口径治理

#### 原始目标

> 将工作台调整为总负责人视角的全局进度与风险总览；在手册详情中增加只包含“未开始、生产中、审核发布中、已完成”的关键状态泳道，风险作为正交标签。

#### 关键决策与口径

- 泳道是手册详情的“生产进展”页签，不新增一级导航；桌面端四列、移动端纵向阶段列表。
- 默认按一级章节或工作包聚合，卡片点击后进入“目录与缺口”，携带章节、阶段和风险筛选。
- 同一节点只属于一个关键阶段，但可同时有多个风险标签；风险筛选不改变节点阶段。
- 生产完成度与泳道“已完成”共用同一口径：`43 / 72（59.7%）`。`53 / 72` 仅表示已纳入目标发布 v2.3 的候选范围，包含生产或审核中节点，不等同于已完成数。
- 手册完整情况分别展示结构、内容、责任和质量门禁事实，不合成单一分数。知识点基线未冻结前显示“指标暂未定义”，不使用发布数或目录节点伪造覆盖率。

#### 设计与实现结果

- 工作台改为“手册生产总览”，集中展示生产完成度、四类完整情况、优先风险和未定义的知识点覆盖度。
- 手册详情形成“总览、生产进展、目录与缺口、更新与发布”四页签；泳道聚合数为 `8 + 9 + 12 + 43 = 72`。
- 同步更新前端信息架构、总体概要设计、前端详细设计和详细设计 README；状态映射由服务端版本化投影产生，前端不自行归类。

#### 验证证据

- HTML 内嵌脚本语法检查通过；原始文档预览 API 回归通过，新 HTML 路径可正常返回。
- Playwright 验证 `1440×900`、`1024×768`、`768×1024`、`375×812` 四视口：无页面级横向溢出、无控制台错误；四泳道合计 72，风险筛选前后卡片所属阶段不变，下钻到目录后条件与代表节点一致。
- 最终泳道截图：`/tmp/kc-progress-final-1440.png`、`/tmp/kc-progress-final-375.png`。
- GitLab 同步恢复回归通过：平台管理员重试后任务转为“成功 / MR !185 已创建”，工作台外部同步待处理数从 `1` 更新为 `0`。
- 本轮目标文件的 `git diff --check` 通过。

### 提示词-20260825-002：移除工作台的大纲上传动作

- 时间：2026-08-25 11:09:31
- 发起角色：用户
- 类型：前端信息架构纠偏

#### 决策与边界

- 工作台是总负责人的全局进度、风险和待办入口，不承担 Markdown 大纲上传或结构维护。
- “上传 Markdown 大纲”仅属于“大纲管理”责任模块。工作台仍可通过风险或目录投影下钻到大纲管理处理问题，但不在首屏提供直接上传动作。

#### 执行结果

- 移除 `知识中心管理-Demo.html` 工作台页头的“上传 Markdown 大纲”按钮，保留“查看待审核”待办下钻。
- 前端详细设计同步补充工作台与大纲管理的操作归属边界。

### 提示词-20260825-003：补齐 Agent/Skill 文档生成与前端治理能力

- 时间：2026-08-25 12:01:19
- 发起角色：用户
- 类型：功能补强 / 架构边界 / 前端 Demo

#### 原始反馈与根因

> 文章生成和编写需要借助 Agent、Skill 能力，现有 Demo 没有展示对应设计和前端配置。

- 原有信息架构正确避免将 Agent/Skill 作为一级技术导航，但被过度执行为隐藏生成能力、实际版本和运行链路。
- 原设计集中强调“Skill 不能充当连接器”的禁止边界，但没有同等详细地定义 Skill 在素材清洗、结构映射、章节撰写和引用校验中的正向作用。
- 把“AI 与规则治理”整体排在 P1，忽略了基础生成、版本冻结和运行追溯是 P0 文档生产闭环的必要部分。

#### 底层逻辑与决策

- Agent 是受控的生产流程编排者；Skill 是版本化的单项语义能力；模型负责推理和生成；人工保留来源、规则、草稿提交、审核和发布的最终确认权。
- “生成方案版本”是稳定的业务配置单元，冻结 Agent、Skill 集合、模型策略、提示词包、质量规则和人工门禁；生成运行再冻结大纲、模板、来源快照、操作人和步骤输入输出摘要。
- 采用“AI 能力治理 → 生产批次方案绑定 → 任务步骤执行 → 审核追溯”四层结构。不新增 Agent/Skill 一级导航，平台管理下增加 `/admin/ai-capabilities` 二级模块。
- 前端渐进披露：作者默认看生成方案、业务步骤、风险和恢复动作；展开后才看 Agent/Skill/模型/提示词/规则精确版本。不向作者显示完整系统提示词、密钥或任意 Skill 上传入口。

#### 执行结果

- 需求中增加 `7.5.1 Agent 与 Skill 辅助生成`，将基础生成和追溯定为 P0，复杂评测与成本优化保留在 P1。
- 概要设计补充 M05/M09 责任、生成方案/Agent/Skill/生成运行对象关系、单一事实源和 `9.2.2 Agent/Skill 生成能力边界`。
- Demo 的文档生产增加已锁定生成方案、四个业务步骤、可展开运行版本和继续执行反馈；审核页增加 AI/人工差异归因和只读生成链路。
- 平台管理增加“AI 能力管理”，展示生成方案、Agent、Skill、模型/提示词/规则版本、评测和空间授权；非管理员不能创建新版本。

#### 验证证据

- HTML 内嵌脚本语法检查通过；目标文件 `git diff --check` 通过。
- Playwright 验证 `1440×900`、`1024×768`、`768×1024`、`375×812`：文档生产运行详情可展开，审核生成链路可查看，平台管理员可进入 AI 能力管理，手册负责人无权修改；四视口无页面级横向溢出和控制台错误。
- 截图：`/tmp/kc-agent-production-final-1440.png`、`/tmp/kc-agent-production-final-375.png`、`/tmp/kc-ai-capabilities-final-1440.png`、`/tmp/kc-ai-capabilities-final-375.png`。

### 提示词-20260827-001：统一仓库级 Git 提交规范

- 时间：2026-08-27 16:22:01
- 发起角色：用户
- 类型：仓库规范维护

#### 原始目标

> 将 `CONTRIBUTING.md` 改名为 `git提交规范.md`，并从偏向 Agent Runner 的说明调整为当前仓库全局规范。

#### 决策与执行结果

- 保留 Conventional Commits 作为统一格式，提交摘要使用中文；作用域改为覆盖知识中心、Agent Runner、PingCode、前端、检索、LLM、Skill、提示词、文档、脚本、配置和治理角色等仓库主要区域。
- 将写死的 Node.js 检查调整为风险匹配的全仓检查规则，基础检查统一为 `git status`、`git diff --check` 和 `git diff --stat`。
- 删除正文必须逐项列出修改文件的冗余要求，强调单一主题、设计与测试同步、敏感信息保护和破坏性变更说明。
- 根据提交类型增加正文要求：`docs` 说明文档范围和主要结论，`feat` 说明新增能力与影响，`fix` 说明问题根因和修复方式，`refactor` 说明职责调整与保持不变的外部行为；非微小提交至少包含“修改内容”，并按实际情况记录“验证结果”。
- 文件移动为 `git提交规范.md`，根 `README.md` 和 `AGENTS.md` 统一引用该文件；不保留第二份提交规范。
- 当前规范依靠人工检查，未将尚未配置的 commitlint、Husky 或 Commitizen 描述为已有门禁。

### 提示词-20260827-002：将知识平台通用化改造拆为中文任务卡

- 时间：2026-08-27 17:43:25
- 发起角色：用户
- 类型：任务拆解 / 纠偏 / 项目治理

#### 原始提示词

> 文档生成器和清洗工具通用化改造内容较多，需要拆分任务逐个执行，任务放入 `.codex` 任务目录，并建立改造进展详情；任务卡改用通过文件名即可识别内容的中文描述。

#### 目标与关键决策

- 避免单轮同时装载设计、双后端、双前端和全量验证上下文，降低上下文溢出及跨模块误改风险。
- 使用 `TASK-KPG-00` 至 `TASK-KPG-11` 作为稳定编号，文件名使用中文业务描述。
- 任务卡统一放在现行规范目录 `.codex/workflow/tasks/knowledge-platform-generalization/`，不扩展旧 `.codex/tasks/`。
- 专项 `PROGRESS.md` 作为跨任务状态、证据、迁移矩阵和风险的单一入口；任务按门禁逐项推进。

#### 执行动作与证据

- 新增 12 张中文任务卡，覆盖分支审计、设计、配置契约、双端加载、YashanDB 能力包、两条业务迁移、运行接口、统一前端、兼容、端到端验收和文档收口。
- 新增专项 `README.md`、`PLAN.md`、`PROGRESS.md`，并同步模块和任务总索引。
- 所有任务初始状态为“待开始”，没有将任务拆解误记为业务改造已完成。

#### 结果与残余问题

- 任务结构和进展入口已建立。
- 2026-08-27 17:56:46 已从 `dev@7595a71` 创建 `feat/knowledge-platform-generalization`。切换前后工作区状态、已跟踪差异和未跟踪路径清单指纹一致，用户修改未丢失。
- 工作区审计识别出 13 项已跟踪变化和 3,202 个未跟踪文件；专项文件、用户既有修改、运行产物/禁止提交三类边界已记录在专项 `PROGRESS.md`。
- TASK-KPG-00 已完成，下一步只执行 TASK-KPG-01；总体架构和迁移设计冻结前不实施业务代码改造。

#### 可复用经验

- 跨语言、跨前端的大型改造应拆成带退出门禁的小任务，并用一个进展文档维护累计上下文。
- 中文文件名负责可发现性，稳定编号负责机器检索和跨文档追踪，两者不应互相替代。

### 提示词-20260827-003：按五阶段实施知识中心平台通用化改造

- 时间：2026-08-27 18:12:10
- 发起角色：用户
- 类型：架构 / 实施 / 测试 / 项目治理

#### 原始提示词

> 按照专项 PLAN 的五个阶段逐步完成任务；复杂任务启用多角色协作；最终明确输出风险项和不明确项。

#### 目标与期望结果

依次完成 TASK-KPG-00 至 TASK-KPG-11，每项达到退出门禁后再进入下一项，通过独立提交和专项进展记录控制上下文、工作区污染和跨模块回归风险。

#### 关键理解、假设与待确认项

- 已确认：平台采用单实例、单企业配置化，首个且本轮唯一能力包为 YashanDB。
- 已确认：3500、Node 4100、Python 8001 保持独立；统一入口为 `/knowledge-center/`，旧入口和既有 API 保持兼容。
- 决策：未设置能力包环境变量时允许选择登记的默认 YashanDB 包；显式选择为空、缺失或非法时必须启动失败。
- 决策：生产配置只接受受控 profile ID；配置指纹包含规范化配置和引用资源内容摘要，不包含秘密、绝对路径或易变运行字段。
- 待后续冻结：Profile 字段格式、允许资源根、历史实体别名、连接器 `configured` 口径和真实模型验收环境。

#### 认知与逻辑检查

- 风险：把“通用化”误解为机械替换所有 YashanDB/PingCode 文案，会破坏领域事实和连接器专名。
- 修正：区分平台品牌、企业领域事实、连接器专名和历史兼容常量，只迁移真正的企业差异。
- 风险：只验证新页面可打开无法证明旧 API 兼容，必须冻结方法、状态码、字段、Socket.IO、SSE 和深链快照。
- 失败边界：显式非法配置不得静默回退；单个运行模块离线可由统一入口降级展示，双模块不可用则聚合接口返回 503。

#### 执行动作与证据

- Architect、Backend Worker、Test Engineer 对 TASK-KPG-01 分别完成产品架构、现有代码边界和可执行测试矩阵审查。
- 新增知识中心建设平台通用化总体架构设计及 `enterprise-profile/v1` 配置与运行上下文开发设计基线。
- 真实只读探测确认 4100、8001 健康接口及 3500 旧入口/代理返回 200；尚未实施的 `/knowledge-center/` 返回 404。
- 同步平台基础模块 README、设计文档总索引、TASK-KPG-01 和专项进展。

#### 结果与残余问题

- TASK-KPG-01 完成，阶段 A 达到退出门禁；下一步进入 TASK-KPG-02，不提前实现加载器。
- 已识别但未在本任务擅自整改：本地 PingCode 明文凭证、8000/8001 端口漂移、服务器绝对路径配置和旧配置管理弱密钥兼容逻辑。
- 凭证轮换与 Git 历史处置需要人工决策；真实模型链最终验收依赖外部环境和授权凭证。

#### 可复用经验

- 跨语言配置必须先冻结规范化、指纹、错误语义和共享黄金向量，再分别实现加载器。
- 默认配置选择与加载失败回退是不同语义；只有前者可以用于兼容，后者必须暴露部署错误。

#### 补充记录：TASK-KPG-02 企业能力包契约

- 时间：2026-08-27 18:25:52
- 契约决策：首版只支持 JSON；生产通过受控 profile ID 选择；资源引用以仓库根为基准并受允许目录限制；密钥只保存 `env:` 或 `secret:` 引用。
- 错误决策：公共 `code` 保持架构级稳定，使用 `issueCode` 表达 Schema、重复引用、未知引用、绝对路径、越界和敏感字段等具体原因，同时返回 JSON Pointer `path`。
- 执行证据：新增 Draft 2020-12 Schema、一个最小 YashanDB 有效样例和 11 个单故障无效样例；Node 12 项 Jest 与 Python 双组 unittest 全部通过。
- 风险复盘：Node 的 Schema 库来自传递依赖，不能直接成为生产加载器基础；TASK-KPG-03 必须消除这一隐式依赖。Fixture 的 mutation 描述必须先展开，不能被误当成可直接交给 Schema 的企业能力包。

#### 补充记录：TASK-KPG-03 双端加载器与运行上下文

- 时间：2026-08-27 18:37:06
- 实现决策：Node 使用仓库自有的契约关键字校验器，Python 使用锁定的 `jsonschema`；两端按 Schema、安全、引用语义、资源解析、规范化和指纹顺序加载。
- 执行证据：Node 19 项、Python 6 项测试通过；两端默认包上下文和 `sha256:d676cbe5dd461814571825b1e9a6cf6c7679f13d8b4f9113359dc01b387fedac` 指纹一致。
- 真实验证：Node 4199、Python 8091 在隔离环境启动且健康接口返回 200；显式空 profile 两端均非零退出。
- 审查修正：Schema 先于引用语义；畸形连接器不再产生语言运行时异常；资源错误返回具体 JSON Pointer；Node 校验器不宣称通用 Draft 2020-12 实现。
- 后续输入：TASK-KPG-04 按真实资源类型冻结允许根及连接器最低密钥注册表，不能整体放开可能含凭证的配置目录。

#### 补充记录：TASK-KPG-04 YashanDB 企业能力包

- 时间：2026-08-27 18:47:00
- 组合结果：建立正式注册表和 YashanDB 完整能力包，引用两个工作区、领域入口、4 个 Agent、2 个加工 Skill、5 个 Prompt、7 个模板和两类规则入口。
- 安全决策：只对白名单资源目录和 `quality-config.json` 单文件开放引用；不引用含凭证、模型密钥或服务器绝对路径的配置。
- 连接器决策：`local-upload` 无密钥；PingCode 和 MCP 分别要求逻辑密钥引用。`configured` 只判断引用可解析，不代表健康。
- 验证证据：双端加载 21 个资源并产生相同完整包指纹；Node 19 项、Python 6 项测试以及 JSON、敏感信息和绝对路径扫描通过。
- 残余判断：缺少证据证明历史供应商错误类型可以无损合并为通用 `ErrorCode`，因此别名映射保持空，不擅自重写或合并历史语义。

#### 补充记录：TASK-KPG-05 文档生成链配置驱动

- 时间：2026-08-27 19:04:01
- 审查纠偏：仅替换品牌文案和在 Workflow 状态追加指纹不足以满足配置驱动；多角色审查识别出 Agent Prompt 实际未消费登记资源、直写链无追溯、7 类生成 Skill 未登记及质量规则仍固定路径等缺口。
- 实现决策：启动时保留公开脱敏上下文和后端深度只读资源注册表两个边界；业务按集合、ID 或规范相对引用读取资源，绝对路径不进入响应、日志摘要和配置指纹。
- 兼容决策：旧生成 API、任务状态字段以及 `../skills`、`../templates` 返回路径不变，只追加非敏感 `profile` 字段；普通 Workflow 与直写任务冻结相同配置指纹。
- 执行证据：能力包资源由 21 增至 29；Node 聚焦测试 33/33、Python 6/6 通过；隔离端口 4198 的健康、任务创建和状态查询均返回 200，任务实际加载登记的 Planner Prompt。
- 残余边界：类型识别、YashanDB 手册输出目录和固定适用版本属于企业生成策略，但当前契约没有独立策略资源类型，本轮保持兼容并登记后续扩展，不机械提升为平台公共常量。
- 未验证项：真实模型调用已发起但未等待完整文档生成；外部 LLM、MCP 和黄金文档质量需在具备可控凭证与测试数据的 TASK-KPG-10 环境验证。

#### 补充记录：TASK-KPG-06 资料加工链配置驱动

- 时间：2026-08-27 19:18:00
- 边界决策：品牌、领域默认值、领域版本、Skill 根和追溯字段由企业能力包注入；`YashanDBErrorCode`、`OracleErrorCode`、`yashandb.error.*` 是历史数据事实，必须保留兼容。
- 实现结果：Python 新增运行访问器，清洗说明、模型配置提示、知识提取信封、候选记录及标准化产物接入 profile；Vue 顶栏从既有运行配置的脱敏品牌投影生成。
- 验证证据：资料加工 profile 测试 3/3，组合领域/错误码/追溯/预览测试 14/14，Vue 构建通过，运行配置响应不含路径、密钥或完整 profile。
- 基线区分：完整旧回归存在结构分块长度断言和实体/关系-only 错误码断言差异，与本轮配置迁移无关，未扩大范围修复。
- 风险：Python 默认端口 8000 与部署使用 8001 的漂移仍留到 TASK-KPG-10 真实启动核验；真实模型知识提取依赖外部模型网关。

#### 补充记录：TASK-KPG-07 平台运行上下文与统一网关

- 时间：2026-08-27 20:18:00
- 决策：沿用双端已冻结的脱敏 context DTO，不单方面增加领域版本、模块列表或 `loadedAt`；模块状态由 3500 聚合外壳表达。
- 实现：Node/Python 提供 `/api/platform/context`；3500 新增统一聚合入口，并行、总时限和响应大小受控，指纹不一致只降级，不重算或改写。
- 验证：网关 Jest 9/9、Python profile 4/4；隔离 HTTP 验证双端指纹一致、网关 ok、单模块停止后 degraded/MODULE_UNAVAILABLE。
- 风险：Python 8000/部署 8001 端口漂移和旧 API 全量兼容性留待 TASK-KPG-10；领域版本等扩展字段需先修订公共契约。

#### 补充记录：TASK-KPG-08 知识中心建设平台统一前端入口

- 时间：2026-08-27 20:26:00
- 设计判断：面向平台主管和内容生产者采用中高密度企业后台布局；Taste-Skill 的营销页/Hero/高动效规则不适用，保留其审计优先、一致性和交付预检原则，并结合 UI/UX Pro Max 的无障碍、触控、响应式与错误恢复规则。
- 实现：新增 `/knowledge-center/` 静态外壳，读取聚合运行上下文，展示企业能力包和两个独立工作区；单模块故障禁用对应入口，保留另一入口与刷新动作。
- 验证：隔离网关静态路由 200；Playwright Chromium 桌面、1024、平板、375px 及单模块降级共 5/5，无页面级横向溢出。
- 风险：旧入口迁移提示和完整兼容回归尚未实现，分别进入 TASK-KPG-09/10；Playwright 报告和截图不写入仓库。

#### 补充记录：TASK-KPG-09 旧入口迁移提示与兼容

- 时间：2026-08-27 20:41:00
- 实现：文档生成和资料加工旧入口均增加可关闭、非阻断的 `/knowledge-center/` 迁移提示；保留查询参数、深链和既有 API，不执行重定向。
- 纠偏：Playwright 发现文档生成页既有 `copyToast` 浮层拦截提示按钮，补充 `pointer-events: none`，恢复真实点击链路。
- 验证：Python 前端 `npm run build`；Playwright Chromium 兼容测试 3/3，旧入口 URL、迁移链接和 PingCode API 代理均通过。
- 边界：Socket.IO、SSE、完整旧 API 和四视口兼容在 TASK-KPG-10 继续验证；历史领域文案不作为运行时硬编码缺陷机械清理。

#### 补充记录：TASK-KPG-10 通用化端到端验收与响应式检查

- 时间：2026-08-27 21:02:00
- 验证：Node Jest 31/31，Python unittest 11/11，资料加工前端 npm run build；隔离 Node 14110/Python 18010/3500 13510 双端 API 与聚合真实返回 200、指纹一致、状态 ok。
- 浏览器：Playwright Chromium 统一入口四视口、单模块降级、旧入口深链和 API 代理 8/8；页面无横向溢出或控制台错误。
- 安全：源码、HTML/JS 和聚合响应未发现令牌、密码、secret 引用或服务器绝对路径；异常上下文、HTTP 错误、无效 JSON、超时和响应大小限制均有测试。
- 环境边界：4100/8001/3500 由用户旧进程占用，未强制重启；真实 PingCode/MCP、外部模型完整产出、生产黄金样例及旧 Socket.IO/SSE 全量基线仍未验证。
- 风险：Vite 大 chunk 警告和 Python 8000/8001 端口漂移记录为非阻塞后续事项。

#### 补充记录：阶段 E 默认部署与真实协议补验

- 时间：2026-08-27 20:48:00
- 纠偏：独立测试审阅指出隔离 HTTP/Playwright 不能证明默认部署、Socket.IO 与 SSE 全量兼容；因此撤回无条件完成声明并补充真实协议证据。
- 默认部署：确认旧服务无活动任务后受控重启 3500/4100/8001，当前版本统一入口、双端上下文、聚合、旧入口和 API 代理共 7 个 URL 均 200，指纹一致。
- 协议验证：真实 Socket.IO 客户端完成连接和 task_id 订阅/退订；Python 四条 SSE 流验证响应头与事件内容；兼容 Jest 5/5、SSE unittest 4/4。
- 依赖：新增 `socket.io-client` 仅用于开发测试；安装审计报告 6 个既有依赖漏洞（1 low、5 high），未执行自动升级以避免扩大范围。
- 人类门禁：真实 PingCode/MCP、外部模型完整生成、生产黄金样例、明文凭证轮换/历史清理和 8000/8001 文档统一仍需授权或决策。
## 2026-08-28 11:20:00 通用化专项：文档生成策略与技术债

- 决策：不修改 `document-paths.json`、PingCode 明文配置和旧前端私网/绝对路径示例，统一记录到 `.codex/workflow/todos/知识中心通用化待处理风险与技术债.md`。
- 实施：在企业能力包中新增可选 `generationPolicies`，按文档类型和生产模式绑定来源确认、Agent/Skill/Prompt/模板、质量规则与逻辑输出目录；生成器保留旧字段并附加策略追溯信息。
- 依据：策略选择必须确定性和可审计，模型不能选择外部目标或绕过人工确认；固定盐/默认密钥与 Vue 大 chunk 分别属于后续安全迁移和性能优化专项。
- 待确认：文档类型稳定 ID、全量候选来源回退、输出目录映射、质量门禁强度及何时升级为强制契约。

## 2026-08-28 16:55:00 知识中心融合与隔离验证

- 决策：采用“统一外壳 + 统一只读投影 + 专业模块”融合方式，不合并 Node/Python 进程，不迁移底层数据，保留旧入口和 API。
- 实施：新增 `/knowledge-center/api/platform/overview`，并将前端总览、手册详情泳道、来源预览/快照、知识库、文档生产、文档管理和独立生成策略模块接入统一投影；不可用时明确显示部分投影或评审示例。
- 隔离：增加 `agent-runner/restart-knowledge-center-isolated.sh`，默认前端 13510、文档 API 14110、资料加工 API 18010；PID、工作目录和命令校验确保不误杀 dev 服务。
- 性能：按路由和依赖域拆分资料加工前端大包，业务页由约 3.3 MB 降至约 46 KB；重依赖仍保留独立告警，不通过提高阈值隐藏。
- 验证：统一投影 Jest 11/11；隔离三服务健康与投影 `200/ok`；Playwright 四视口无横向溢出、来源预览与泳道可达、无新增控制台错误。
- 边界：GitLab 仅受控模拟；真实 CAS、外部模型、外部回写和服务端完整度/知识点覆盖口径继续由后端及后续人工门禁确认。

## 2026-08-28 17:20:00 知识中心 Playwright 巡检修复

- 问题：移除顶部面包屑后，前端仍更新不存在的 `#view-label`，导致 `Cannot set properties of null`，页面内容为空。
- 修复：删除过期 DOM 更新；保留正文标题作为唯一业务标题。同步确认错误面板在正常投影下隐藏。
- 验证：隔离地址四视口检查无横向溢出；桌面和移动端导航、手册泳道、来源预览、快照确认可达；控制台无新增错误。

## 2026-08-28 17:45:00 知识中心真实数据边界

- 决策：移除前端内嵌手册、风险、泳道、来源快照、文档和生成任务等 fake 数据，避免干扰后续联调与验收。
- 规则：统一投影实际返回的摘要才渲染；接口无数据时隐藏指标；尚无事实源的页面显示“待创建”框架，不伪造业务记录或预览正文。
- 验证：隔离地址 1440x900 与 375x812 无横向溢出、无控制台错误，页面不再出现 SN-2039、rev-20260828 等演示记录。

## 2026-08-31 知识中心统一入口与生成策略

- 决策：采用 Demo 的完整信息架构作为运行入口基准；一级导航固定为工作台、知识资产、资料清洗、大纲管理、模板管理、文档生产、审核与发布、平台管理。
- 生成策略边界：仅服务文档生成，负责参考文档分类优先级和 Agent/Skill/Prompt/质量门禁编排；模板正文由模板管理维护，连接器由平台管理维护。
- 来源分类：PingCode/GitLab 通过规则推荐类别，用户可修正后固化到快照；官方文档、特性设计、手册、其他数据库参考、模型参数知识按固定顺序参与，模型知识仅补充表达不新增事实。
- 交互：桌面侧栏支持展开、64px 图标态、完全隐藏和 200～360px 拖拽调宽，状态本地记忆；移动端使用抽屉导航。

## 2026-08-31 10:50:00 知识中心运行入口语义化命名

- Review 判断：`index.html` 符合 Web 默认入口约定，但在同仓库多前端场景下无法从文件名识别业务用途，意见合理。
- 决策：运行页改名为 `knowledge-center-management.html`；对外 URL 继续使用稳定的 `/knowledge-center/`，网关同时兼容历史显式 `/knowledge-center/index.html`。
- 故障纠偏：改名后若未同步默认入口映射，会使目录 URL 返回 404，浏览器可能停留在未加载样式的旧页面；入口文件与网关映射必须作为同一变更验证。

## 2026-08-31 11:20:00 知识中心第一、二阶段前端改造

- 用户判断：当前界面不具备合格对外产品完成度；先完成工作台信息重构和公共视觉系统，暂停其他模块页面扩展。
- 产品逻辑：工作台按“待处理事项、资产概况、风险与阻塞、生产进度”组织；只展示真实投影，移除技术 ID 和实现层术语。
- 视觉逻辑：采用低变化、低动效、中高密度企业后台设计；统一色彩、字体、间距、圆角、焦点和 Lucide 图标。
- 工程边界：新增 `common` 和 `modules/dashboard`；`common` 只承载跨模块稳定复用或平台级能力，不作为无法归类代码的收容目录。
- 第三阶段门槛：每个业务模块必须先冻结角色、事实源、状态、接口 DTO、权限、错误和跨模块路径，再进入独立目录实施；本轮不构造假状态或假接口。

## 2026-08-31 11:40:00 知识中心公开入口收敛

- 判断：`#main` 是浏览器端文档片段，不是服务端路由或第二页面；它由键盘跳过导航控件产生，没有作为公开入口展示的必要。
- 实施：保留跳过导航的无障碍能力，但改为直接聚焦正文，不修改地址栏；唯一公开入口保持 `/knowledge-center/`。
- 脚本：隔离重启脚本的内部健康检查只输出服务就绪状态，不打印内部 API URL；最终只展示知识中心前端入口 URL。

## 2026-08-31 12:17:26 知识中心前端多角色实施与冲突收敛

- 路由：按 Task Routing Contract 走标准开发；使用架构审计、前端范围实施和 Playwright 验收三个独立角色，主执行者负责集成、API 空值语义和文档对账。
- 冲突：七/八个导航、侧栏规则、模块路由、权限 DTO、顶部上下文、生成策略、GitLab 能力、品牌与第三阶段门槛统一记录为 `needs_decision`，未把推荐项当成已批准业务规则。
- 实施：完成八模块 ES Module 目录、`common` 分层、资料清洗独立渲染、研发术语清理、未接入操作禁用、策略硬编码移除、平板抽屉和移动焦点约束。
- 数据：统计字段缺失时保持缺失，只展示上游明确返回的零值；不展示内部数据集 ID。
- 视觉：Taste-Skill 只采用审计优先、一致性和反模板化规则；仪表盘的布局、触控、对比度和焦点以 UI/UX Pro Max 与项目详细设计为准。
- 验收：隔离环境 13510/14110/18010 重启成功；三个真实 HTTP 入口均 `200`；Jest 17/17；Playwright Chromium 7/7；三个最终截图无溢出、遮挡和控制台错误。

## 2026-08-31 13:00:57 知识中心前端九项决策批准与路由落地

- 人工决策：用户对 `DEC-KC-FE-001` 至 `DEC-KC-FE-009` 全部批准 B 方案；批准结论统一记录在 `07-决策与风险/知识中心-前端-改造决策-决策记录文档.md`。
- 路由：八个一级模块采用 `/knowledge-center/<module>` 正式路径，根路径为工作台；使用 History API 支持直接访问、刷新恢复和浏览器前进/后退。
- 受限边界：顶部只等待真实空间和用户 DTO；导航权限等待 `session`、`visibleModules`、`allowedActions`；生成策略等待已发布策略 API；GitLab 继续保持未接入或受控模拟。
- 开发门槛：完整业务页必须先补齐角色、事实源、DTO、状态、权限、错误和端到端设计；未具备事实源时只展示空状态或建设状态，不添加假数据。
- 验收结果：根入口、知识资产深链、上下文和总览接口均返回 `200`；Node 语法检查通过；Jest 18/18；Playwright Chromium 8/8；1440x900 与 375x812 截图无横向溢出、遮挡或控制台错误。

## 2026-08-31 13:30:00 知识中心建设平台文档目录对账

- 用户决策：`04-开发任务` 与 `08-进度与报告` 保留原目录，不移动或删除；历史任务和周报继续作为时点证据。
- 文档定位：`知识中心-前端-业务交互-详细设计文档.md` 不删除，改定位为后续模块详细设计输入基线；它集中保留手册完整度、来源快照、生成策略、Agent/Skill/Prompt、审核发布和集成管理等业务交互约束，不能由视觉模块设计替代。
- 当前口径：GitLab 分支、提交和 MR 是专项验收后的目标能力；当前前端与连接器仅允许未接入或受控模拟，不展示真实成功结果。
- 对账动作：同步更新需求、概要设计、详细设计索引及 04/08 目录说明，明确后续决策以 07-决策与风险为准；未删除历史正文或用户未提交文件。
- 残余问题：静态 `知识中心管理-Demo.html` 仍是历史评审载体，待业务模块详细设计和运行页面预览完全迁移后，再单独评估是否归档；当前不删除。

## 提示词-20260831-001：知识中心-认证-CAS登录与会话-概要设计文档

- 时间：2026-08-31 14:35:56
- 发起角色：用户
- 类型：需求 / 设计 / 安全决策

### 原始提示词

> 知识中心管理平台参考 `yas-ai-helper` 登录界面，通过 CAS 登录；先形成概要设计，说明基本原则和设计维度，全面考虑易用性，并对不明确内容交互确认。确认后要求按计划新增设计文档、同步概要设计 README 和本提示词记录。

### 目标与期望结果

形成可进入详细设计的登录与认证基线，明确 CAS 身份认证、平台用户建档、角色授权、平台会话、前端状态、错误恢复、安全审计和验收方式，同时不把参考系统的已有实现误写为知识中心当前能力。

### 关键理解、假设与待确认项

- 已确认：沿用同一企业 CAS，知识中心登记独立 service URL。
- 已确认：首次 CAS 用户自动建档，默认获得全部业务空间的知识业务读写权限，不获得审核、发布和平台管理权限。
- 已确认：本地 `admin` 用于平台管理和权限分配，登录页与 CAS 入口同页展示。
- 用户偏好：持续保留初始 `admin/admin` 且不增加访问限制。
- 待确认：CAS 稳定用户属性、认证职责的物理后端归属、平台会话细节、完整权限矩阵，以及默认管理员凭据安全例外的有效生产审批。

### 认知与逻辑检查

- 不能把“CAS 登录成功”等同于“拥有知识资源权限”；认证与授权必须由后端分层校验。
- `yas-ai-helper` 已具备 CAS 跳转、`serviceValidate`、用户建档、默认角色、JWT 和退出链路，但知识中心当前仍是静态前端和有限只读聚合接口，参考实现不等于目标能力已存在。
- “普通 CAS 用户具备业务读写”不能解释为通配管理权限；审核、发布、用户、角色、连接器、配置和审计必须独立授权。
- 同页暴露管理员入口并持续使用公开默认凭据会降低认证安全控制；用户偏好可以进入设计记录，但不能替代有范围、责任人和回退条件的生产安全审批。
- 登录体验不能只覆盖成功跳转，还必须覆盖票据失效、CAS 不可用、会话过期、权限不足、深链恢复、键盘和读屏操作。

### 执行动作与证据

- 新增 `.codex/知识中心建设平台/02-概要设计/知识中心-认证-CAS登录与会话-概要设计文档.md`，定义原则、边界、流程、逻辑接口、对象、交互、安全例外和验收设计。
- 同步 `.codex/知识中心建设平台/02-概要设计/README.md`，登记新设计。
- 设计证据来自 `yas-ai-helper/web/src/views/auth/Login.vue`、`yas-ai-helper/yah-api/internal/web/controller/v1/auth.go`、`yas-ai-helper/yah-api/internal/web/service/auth.go` 和 `agent-runner/frontend-server.js`。
- UI/UX 检查采用企业后台中高密度、低动效、清晰状态恢复、WCAG 2.2 AA、44px 触控区和四视口验证原则；未将不适用的营销页结构或字体建议写入设计。

### 结果与残余问题

- 概要设计已形成，普通 CAS 登录、自动建档、基础业务读写、管理权限隔离和前端体验具备后续详细设计输入。
- 管理员 `admin/admin` 无限制持续使用被明确标记为 `needs_decision`；当前文档交付不实施认证代码、默认凭据或生产配置。
- CAS 真实属性和单点退出能力、会话实现、认证服务归属、权限矩阵及安全例外仍需在详细设计或专项审批中冻结。

### 可复用经验

- 复用登录界面时应同时审查协议、会话、用户映射、授权和退出，不能只复制视觉页面。
- 用户确认高风险偏好后，设计仍需区分“偏好证据”和“有效生产审批”，并给出不阻塞安全主链路的回退方案。
- 认证页面的易用性来自状态可理解、失败可恢复和深链可恢复，而不只是视觉一致。

## 提示词-20260831-002：认证架构方案与管理员凭据决策补充

- 时间：2026-08-31 14:45:00
- 发起角色：用户
- 类型：架构决策 / 安全决策

### 原始提示词

> 普通 CAS 用户自动建档并具备全部业务空间读写权限已批准；管理员允许使用 `admin`，不强制修改密码；认证职责归属请给出 3 种方案。

### 目标与期望结果

在实施登录功能前比较认证服务归属的 3 种架构方案，明确网关、认证权威、业务服务、会话、用户权限仓储和实施风险，供用户选择后冻结方案。

### 关键理解、假设与待确认项

- 已确认：普通 CAS 用户首次登录自动建档，默认获得全部业务空间的知识业务读写权限，不自动获得管理、审核和发布权限。
- 已确认：本地 `admin` 允许继续使用，不强制首次修改密码；该方案增加已知安全风险，必须保留限流、审计、告警和后续凭据轮换入口。
- 待确认：认证权威采用现有 4100 服务、3500 网关，还是独立认证服务；正式用户、角色、会话和审计存储继续以 YashanDB 为目标事实源。

### 认知与逻辑检查

- “管理员允许使用 admin”只决定凭据兼容策略，不应扩大普通 CAS 用户管理权限，也不能省略后端逐请求授权。
- 认证服务位置会影响会话边界、跨服务身份传播、部署拓扑和公共 API，不能仅按代码改动量选择。
- 3500 是静态/BFF 入口，4100 是现有 Express 业务后端；二者都可承载逻辑，但持久化、代理和故障边界不同。

### 执行动作与证据

- 多角色审查建议比较：4100 认证权威 + 3500 同源代理；3500 直接承载认证；独立认证服务 + 两层代理。
- 方案证据来自 `agent-runner/server.js`、`agent-runner/frontend-server.js`、知识中心认证概要设计和审批边界文档。

### 结果与残余问题

- 已形成 3 种方案比较，待用户选择后更新认证概要设计并进入实现设计。
- 在方案冻结前不新增认证 API、CAS 外呼、会话存储或跨服务身份签名实现。

### 可复用经验

- 涉及认证、权限和外部身份系统时，先冻结权威归属和身份传播协议，再拆前端、网关和业务服务任务。
## 2026-08-31 15:00:19 知识中心专业治理型视觉与负责人视图决策

- 视觉方向：采用专业治理型，业务面板使用白色，不再使用深青作为品牌主色；页面画布、新强调色和深色模式尚未确认，进入 `07-决策与风险`，本轮不提前写入运行样式。
- 工作台：按风险与阻塞、生产完成度、四类完整度矩阵、默认展开泳道、最近活动排列；风险比完成度更醒目，最近活动用于接续未完成任务。
- 汇报：工作台和手册详情需要导出/打印视图；输出必须与当前权限和统计时点一致，具体文件格式、脱敏和大数据量生成方式待确认。
- 知识点覆盖：知识点基线由手册负责人维护，区分已覆盖、部分覆盖和待覆盖；多文档关系边不去重，但对外汇总以基线知识点逐点计数，防止关系数量放大覆盖率，完整证据链后续建设。
- 生产泳道：固定按一级章节聚合；卡片展示文档完成度和局部风险，单文档失败不将章节整体改为失败，审核退回回到生产中。
- 拖拽边界：支持拖拽发起受控状态变更，但不能由前端直接换列。批量范围、部分失败、撤销、权限和并发规则未冻结前不实现本地模拟成功。
- Taste-Skill 影响：仅采用审计优先、单一视觉语言、卡片克制、完整状态、对比度和响应式预检；营销页 Hero、高动效、图片优先和宣传式构图不适用于企业知识治理后台。
- 实施结果：运行界面已将深青品牌与导航色替换为中性黑白灰，保留现有画布色等待下一轮决策；工作台按已确认顺序展示风险、生产完成度、完整度矩阵、泳道和最近活动。打印汇报入口已可用，导出因格式和权限规则未冻结而明确禁用。
- 数据边界：真实任务和资产统计继续显示；完整度、泳道、续办活动缺少事实源时只显示暂不可用，不写入示例指标。当前失败任务数作为已接入风险事实显示，不推断其他风险。
- 验证证据：前端 ES Module 语法检查通过；平台兼容和投影 Jest 18/18；知识中心 Playwright Chromium 9/9，覆盖 1440、1024、768、375 四视口、风险优先层级、深链、错误状态和抽屉焦点；实际 1440×900 截图无横向溢出或遮挡。
- 已知限制：全仓 `git diff --check` 仍被本任务之外的 `prompt-generator.html.backup` 与 `processing_units.py` 既有尾随空格阻断；本轮涉及路径的 `git diff --check` 已通过，未修改无关文件。

### 2026-08-31 17:55:00 补充纠偏：浅色导航与事件刷新

- 用户纠偏：白色业务面板应以浅灰结构层级和浅色导航承载，导航文字需同步调整，界面使用天蓝/浅蓝增强活力；顶部数据状态/刷新框不需要；此前新增的导出/打印与原设计冲突，需要撤回。
- 决策更新：采用浅灰画布、白色面板、浅灰导航、深色导航文字和天空蓝单一交互强调色。风险红、警告橙、成功绿保持独立语义。原 `DEC-KC-FE-014` 被后续决策覆盖，本阶段不提供报表导出或打印。
- 刷新契约：页面首次进入自动加载；创建、修改、审核、发布等写操作只有收到后端成功响应后，才通过 `notifyKnowledgeDataChanged(scopes, source)` 派发事件。`context` 只重读平台上下文，`overview` 或业务作用域只重读总览；连续事件合并范围，刷新失败保留已有投影。
- 界面实施：删除常驻“数据已更新/刷新”区域、打印汇报和导出占位；错误状态仍保留“重新检查”作为失败恢复动作。浅色导航当前项同时使用浅蓝背景、深蓝文字、左侧指示和 `aria-current`。
- 异步边界：浏览器公共事件只覆盖同页面内已经确认成功的写操作。后台任务跨进程完成仍需真实 SSE/WebSocket 或等价接口，当前不使用轮询、本地计时器或模拟事件冒充实时更新，并登记 `RISK-KC-FE-017`。
- UI/UX Pro Max 影响：采用其白底深色文字、4.5:1 对比度、明确导航当前态、异步加载反馈和语义令牌规则；未采用自动推荐的橙色 CTA，因为会与警告/风险语义冲突。
- 验证：Node 语法检查通过；Jest 18/18；Playwright Chromium 10/10，覆盖四视口、负责人层级、无常驻刷新、公共事件触发真实投影请求、深链、错误恢复和抽屉焦点；1440×900 截图无溢出，导航为 `rgb(241,245,249)`，选中项为 `rgb(186,230,253)`。

## 提示词-20260831-003：采用独立认证服务完成登录功能

- 时间：2026-08-31 18:30:00
- 发起角色：用户
- 类型：架构决策 / 实现 / 测试

### 原始提示词

> 认证架构选择方案三：新增独立认证服务。

### 目标与期望结果

实现知识中心 CAS 与管理员登录、平台会话、首次用户建档、权限投影、深链恢复、退出和平台接口门禁，并通过真实后端 API 与浏览器验收。

### 关键理解、假设与待确认项

- 已批准：独立认证服务作为认证事实源；3500 作为同源入口/BFF；4100 和 8001 保持业务服务边界。
- 已批准：CAS 用户首次建档并获得全部业务空间的基础读写权限，不自动获得审核、发布或管理权限。
- 已批准：管理员允许使用 `admin`，不强制首次修改密码；当前兼容默认密码为 `admin`，同时启用失败限流、统一错误和审计。
- 当前限制：仓库没有已批准的 Node YashanDB 驱动和连接参数，运行实现使用可替换仓储接口与运行目录文件仓储；正式生产仍需实现并验收 YashanDB 适配器。
- 未完成边界：真实企业 CAS 环境未在本地完成登录，当前以一次性 ticket 协议桩验证 CAS 后端链路；4100/8001 的全部业务接口逐请求签名身份传播仍需后续分批接入。

### 认知与逻辑检查

- 独立认证服务不能只新增登录页；必须同时修正 3500 静态回退吞认证接口、未登录可读取平台聚合数据和 401/403 混淆问题。
- 测试不能通过写 `localStorage` 冒充认证；管理员 E2E 通过真实登录 API 设置 HttpOnly Cookie。
- CAS ticket 只有在外部 CAS 校验成功后才登记消费；外部临时失败不能永久阻止同一票据按 CAS 有效期重试。
- 浏览器深链恢复必须区分“没有记忆目标”和“根路径目标”，否则直接访问资产页会错误回到工作台。

### 执行动作与证据

- 新增 `agent-runner/auth-server.js` 和 `lib/knowledge-center-auth.js`，实现独立认证 HTTP 服务、CAS `serviceValidate`、管理员登录、scrypt 密码摘要、opaque 会话、HttpOnly Cookie、用户/角色/会话/审计仓储和票据重放拒绝。
- 3500 新增 `/knowledge-center/api/auth/*` 同源代理，并在平台上下文与总览接口前真实查询认证会话。
- 前端新增认证 API、内存认证状态、认证控制器和登录样式；应用启动改为先认证后加载业务页面，并增加回调清理、深链、权限、会话过期和退出处理。
- 隔离启动脚本新增认证服务运行单元；`.env.example` 和知识中心 README 补充配置与运行说明。
- 多角色同步更新认证概要设计、认证服务详细设计及其目录索引。

### 结果与残余问题

- Jest 认证契约使用真实 HTTP handler 和一次性 CAS 协议桩通过 4 项；认证与知识平台兼容聚焦测试合计 10 项通过。
- 隔离环境真实启动认证、3500、4100、8001 四个服务；通过网关完成管理员登录、会话查询、受保护平台上下文读取和退出后 401 验证。
- Chromium 登录专项覆盖四视口、管理员成功/失败共 6 项通过；知识中心业务入口回归覆盖四视口、深链、导航和错误状态。
- 生产上线前仍需：YashanDB 仓储适配器、内部身份签名传播、真实 CAS 联调、生产 HTTPS Secure Cookie、默认管理员密码风险复核。

### 可复用经验

- 认证功能的最小闭环是“身份验证、会话、授权、代理门禁、失败恢复、审计、真实 API 验收”，不是只有登录表单。
- 独立认证服务选择会新增运行单元，启动脚本、健康检查、配置、代理和故障恢复必须作为同一功能交付。

## 提示词-20260831-004：深化大纲管理结构治理设计

- 时间：2026-08-31 19:10:00
- 发起角色：用户
- 类型：需求澄清 / 概要设计 / 详细设计

### 关键决策与纠偏

- 大纲管理定位为手册结构治理模块，不再以文件上传或提示词生成为中心。
- 一份大纲绑定一个适用产品，可引用该产品目录中的多个已发布业务版本标签；业务版本标签集合不代表连续范围，且与大纲结构版本分别存储、展示和审计。
- 手册、章节、知识点采用分层稳定身份；编号、标题、路径、顺序和 Excel 行号均不是稳定 ID。
- 导入和编辑先形成候选版本，人工确认后才发布；生产任务锁定大纲、责任映射、模板及业务版本上下文，历史任务不自动漂移。
- 存量文档逆向提取允许使用 Codex、Claude Code 等通用执行器，但 Agent 仅生成带证据和置信度的候选结构，无权直接修改或发布正式大纲。

### 设计分层与风险

- 新增 M02 模块概要设计，并将结构与产品版本、责任映射与模板绑定、逆向提取 Agent、前端交互拆为独立详细设计，避免把尚未冻结的 Agent 能力伪装为已实现功能。
- 单负责人/单主文档的扩展兼容、旧数据稳定 ID 迁移、产品版本标签治理、Agent 沙箱与冲突规则、质量门禁阈值和进行中任务迁移仍需后续决策，统一登记到 `07-决策与风险`。

## 提示词-20260901-001：修复技术服务部下载批次卡住

- 时间：2026-09-01 13:04:28
- 原始问题：批次 `batch_31583e4c802a4b83` 长期显示 `running`，下载页面无进度。
- 证据：真实 API 显示任务停在 `page_content` 、5/35；下载账本记录 Playwright `WriteUnixTransport closed`；隔离服务日志显示 Playwright Node 驱动约 4GB 内存 OOM。
- 根因：图片、附件通过 `page.evaluate` 将二进制展开为 JavaScript 字节数组，大文件导致驱动内存膨胀；驱动崩溃后任务状态未收敛。
- 执行修复：二进制下载改为 Playwright request context 直接读取 response body；识别驱动传输关闭后使当前客户端失效，后续请求可重建会话；既有断点账本保留已完成页面。
- 运行恢复：仅重启 18010 资料加工后端，启动对账将任务改为 `interrupted`，通过真实 `resume` API 续传。最终为 `completed` 35/35，失败 0，待处理 0，告警 2。
- 验证：相关单元测试 54/54 通过；批次下载页和 13510 网关返回 HTTP 200；重启后日志无新的 OOM。
- 可复用经验：浏览器自动化链路不应通过 JS 数组传输大二进制；外部驱动失败必须同时更新任务可恢复状态，不能仅依赖进程重启。

## 提示词-20260901-002：修复知识中心刷新时登录界面闪现

- 时间：2026-09-01
- 问题：已登录用户在 `/knowledge-center/outlines` 按 F5 时，在会话检查完成前短暂看到登录面板。
- 根因：HTML 首屏默认展示 `auth-shell`，而 `bootstrapAuth()` 异步读取会话；首次绘制与会话结果之间存在可见窗口。
- 修复：新增轻量 `auth-loading` 占位，认证面板首屏隐藏；只有认证确认为未登录或发生错误时才显示登录表单。
- 验证：知识中心 Chromium/Firefox 入口回归 20/20 通过，包含大纲深链刷新恢复；Node 语法和差异检查通过。
## 提示词-20260901-文档命名规范统一

- 决策：知识中心建设平台下的业务文档统一采用“知识中心-模块名-具体功能特性-文档类型文档”命名。
- 已执行：需求、概要设计、详细设计、开发任务、决策/风险和进展报告文件完成语义化改名；各目录 README、设计文档引用和专项进展记录同步更新。
- 边界：README、静态 Demo、任务目录中的历史任务卡不改名；新文件必须遵循对应目录 README 的命名规则。

## 提示词-20260901-大纲管理前期资料梳理与详细设计

- 资料来源：`scripts/pingcode/runtime/web/spaces/positive/batches/batch_31583e4c802a4b83/pages/`，用于提炼手册资产、产品版本、A/B 角负责人、技术领域分工、生产生命周期和典型手册约束。
- 业务判断：历史批次仅作业务规则和初始化参考，不直接成为正式事实源；历史姓名不进入平台模型，运行时使用账号/角色引用；版本文本先作为候选标签并匹配产品目录。
- 已完成：将“前期资料分析与设计依据”合并进大纲管理模块概要设计，深化结构与产品版本、责任映射、逆向提取 Agent 三份详细设计，并同步大纲管理风险清单和目录索引。
- 边界：本轮优先冻结结构与责任映射闭环；模板管理不展开实体和接口；Agent 只能生成带证据的候选结果，不能修改正式大纲、创建正式用户或发布版本。
- 特别约束：无法匹配历史负责人时仅记录责任人待绑定状态，不创建用户占位、不授予权限；历史资料中的 URL、服务器路径、账号、密码和令牌不得进入 Agent 上下文、正式 DTO 或前端展示。
- Review 纠偏：前期资料分析内容不再单独维护，统一并入大纲管理模块概要设计；历史编号前缀 `DB` 明确表示 YashanDB，不表示数据库技术类型。
- 存储决策：当前阶段先冻结仓储契约、稳定 ID、版本指纹、审计和迁移边界；第二阶段在 DTO、权限和模型评审通过后接入 YashanDB，按结构化事实、正文版本、附件/运行产物分批迁移，迁移期旧文件只读兼容且禁止双向写入。

## 提示词-20260901-大纲管理详细设计合并

- 决策：将大纲管理的前端交互、结构与产品版本、责任映射、逆向提取 Agent 四份详细设计合并为《知识中心-大纲管理-结构治理与逆向提取-详细设计文档》；删除四个重复拆分入口。
- 产品模型：采用“产品族 → 产品 → 离散产品版本标签”三级模型；手册绑定一个产品和多个已发布离散标签，不自动推导版本范围。
- 责任模型：大纲类似书籍目录，页面文章是最小生产单元；手册 A/B 角、技术领域主责/协作和模板到文章的引用关系在同一综合设计中冻结。
- Agent 流程：上传手册或选择快照，选择提取策略和多个 Skill（可在线编辑排序、启停和参数），再由 Codex/Claude Code 生成候选大纲；前端可编辑、预检、评审并发布终稿，Agent 不得直接发布。
- Review 纠偏：不保留历史姓名；仅记录责任待绑定状态和来源定位，运行时统一使用平台账号/角色引用。手册列表中的手册名称/整行明确跳转手册详情并保留筛选上下文。
- 交互决策：大纲管理采用双入口关联模式。知识资产提供手册业务入口，大纲管理提供大纲工作台入口，二者通过同一 `handbookId` 和统一事实源关联。
- Review 纠偏：创建/导入候选是大纲管理核心功能，保留在大纲管理工作台；知识资产另新增《知识中心-知识资产-手册目录与文档资产-详细设计文档》，只负责手册与文档资产查看并通过 `handbookId` 跳转大纲治理。
- 风险调整：按确认意见移除历史责任信息无法匹配和待激活用户占位两条风险；平台不保存历史姓名或创建用户占位。
- Review 纠偏：补充大纲版本更新链路，所有更新基于已发布版本创建候选副本，保存变更集、基线和编辑版本号，经差异影响分析、评审和发布后替代旧版本；并发冲突返回结构化错误。
- Review 纠偏：知识点选择与文档生产模板联动，服务端推荐初始模板类型，用户可在候选阶段手动修改模板类型/版本并填写原因，运行任务冻结最终模板引用。

## 提示词-20260901-建立 AI 协同开发指导任务

- 决策：后续开发采用“全局架构和核心契约先冻结，按业务闭环逐模块进行详细设计与垂直切片开发，真实验证结果反哺设计”的方式，不要求所有模块详细设计一次性完成。
- 执行要求：统一使用版本化设计文档、模块关系、API/DTO、决策风险和验收证据作为 AI 共同上下文，不以历史聊天记录作为唯一依据。
- 已新增任务：[知识中心-平台-AI协同开发与设计基线-开发任务文档](.codex/知识中心建设平台/04-开发任务/知识中心-平台-AI协同开发与设计基线-开发任务文档.md)。

## 提示词-20260901-建立平台开发进展总览

- 决策：开发进展总览归档到 `08-进度与报告`，不放在 `04-开发任务`；04 目录继续保存单项任务卡，08 目录负责跨模块进度、依赖、阻塞和下一步。
- 编号：总览使用连续数字编号 `1、2、3……`，原任务卡编号保留在“来源任务编号”字段中，不替代稳定任务 ID。
- 口径：平台全量任务纳入首版总览；状态统一为“待开始/进行中/阻塞/已完成”；完成率只代表开发任务交付进度。
- 维护：采用确定性提取任务卡信息、人工补充风险和下一步的混合方式；新增 [知识中心-平台-开发进展总览-进展报告文档](.codex/知识中心建设平台/08-进度与报告/知识中心-平台-开发进展总览-进展报告文档.md)。

## 提示词-20260902-001：修复移动侧栏账号退出不可见

- 时间：2026-09-02 09:25:38
- 问题：知识中心管理在小于等于 900px 的响应式侧栏中，底部账号的“退出登录”按钮不可见。
- 根因：移动端样式使用 `.sidebar .sidebar-control { display: none; }` 隐藏侧栏控件，该选择器同时误隐藏了底部 `#logout`；后续只单独恢复了移动端关闭按钮。
- 修复：在响应式规则中明确恢复 `.sidebar-footer #logout` 的 grid 布局，保持顶部非必要控件的原隐藏行为不变。
- 验证：Chromium/Firefox 知识中心入口回归 22/22 通过；新增 768px 侧栏展开后退出按钮可见与 title 验证；语法和差异检查通过。

## 提示词-20260902-002：确认桌面侧栏与主内容双滚动

- 决策：桌面端左侧侧栏固定在浏览器视口，右侧主内容独立滚动；账号和退出功能仍属于左侧导航，不与右侧模块或侧滑框联动。
- 原实现缺陷：`.app-shell` 只设置 `min-height`，右侧长内容将整个文档撑高；侧栏 footer 随文档高度被推到页面底部，不满足全局退出操作应始终可见的职责。
- 修复：桌面 `.app-shell` 使用 `height: 100dvh; overflow: hidden`，`.main-shell` 使用 `overflow-y: auto`，`.sidebar` 占满视口高度；900px 以下恢复移动端单页滚动。
- 验证：新增桌面主内容独立滚动和侧栏位置不变的 E2E 断言；Chromium/Firefox 全部入口回归 24/24 通过。

## 提示词-20260902-003：大纲管理采用列表与详情分离布局

- 时间：2026-09-02 10:43:18
- 发起角色：用户
- 类型：设计决策 / 前端实现

### 原始提示词

> 以现有大纲管理页面为基础，使用方案一完成整改：大纲列表与大纲详情拆分为独立页面；列表负责查找和筛选，详情提供全宽治理工作区，其他阶段流程、事实源和验收约束保持不变。

### 目标与期望结果

解决原双栏页面中完整列表、手册上下文和阶段内容争夺宽度导致的拥挤，使用户先完成“找到手册”，再在独立详情中完成“治理大纲”；列表与详情仍消费同一真实后端事实。

### 关键理解、假设与待确认项

- 已确认：采用列表页到全宽详情页的方案，不实现可收缩主从栏、三栏工作台或布局切换。
- 已确认：沿用“目录 → 责任 → 模板 → 检查 → 发布”，统一使用“大纲版本”“目录条目”等业务术语。
- 已确认：不新增后端业务接口，不扩大为新的目录编辑器，不用前端状态伪造保存、确认或发布成功。
- 默认：搜索和当前进度使用 URL 查询参数恢复，滚动位置使用浏览历史状态恢复；详情阶段使用 `stage` 查询参数。

### 认知与逻辑检查

- 根因不是单个间距或字号，而是“查找”和“处理”两个主任务被强制放入同一视口；仅缩小控件会继续牺牲目录可读性。
- 列表/详情拆分会增加一次页面转换，因此必须同时设计筛选、滚动、阶段、深链和浏览器历史恢复，不能只改变 CSS 宽度。
- 手册不存在、无权限、接口失败、空列表和筛选无结果具有不同原因，不能统一显示模糊空态。

### 执行动作与证据

- 更新 `agent-runner/docs/modules/outline-management/overview/outline-management-overview-design.md`，冻结页面职责、正式路由、桌面/移动简图、恢复契约、异常状态和响应式验收。
- 更新 `agent-runner/docs/modules/outline-management/README.md`，同步当前前端信息架构和代码映射。
- 前端实现与真实 API、浏览器回归由本次标准开发任务继续完成；本记录不提前将其标记为已验证。

### 结果与残余问题

- 设计基线已更新，原常驻双栏方案已明确被替代。
- 前端已完成全宽列表、独立详情、五阶段深链、筛选与滚动恢复、手册信息抽屉和无效手册反馈；详情路径通过 HTML `base` 保证脚本与样式仍从知识中心根路径加载。
- 验证证据：真实管理员登录 API、大纲列表 API（7 条）和首条详情 API 成功；Jest 前端契约 8/8 通过；Playwright Chromium 6/6 及知识中心入口回归 12/12 通过，Firefox 专项 6/6 通过，覆盖 1440/1024/768/375 视口无页面级溢出、阶段刷新、筛选返回恢复、抽屉和无效 ID。

### 可复用经验

治理型后台先按核心任务划分页面，再分配空间；当列表用于“找”、详情用于“做”时，应优先分离页面并完整设计返回恢复，而不是把两个完整工作面持续压缩在同一屏。

## 提示词-20260902-004：知识资产工作台与管理权限收口

- 时间：2026-09-02 11:29:24
- 类型：产品决策 / 设计与实施
- 页面原则：知识资产只服务“查找手册、确认责任、判断阶段、进入专业模块”，使用中等信息密度的列表与详情工作台，不在首屏展示内部 ID、结构版本、概要和重复时间。
- 术语决策：“目录条目”统一替换为“章节与页面”；章节是目录组织节点，页面是可生产和验收的文章节点，二者不等于文档数。
- 状态决策：前端只展示后端投影的“待创建 / 完善大纲 / 大纲已发布 / 内容构建中 / 手册发布”，不根据其他字段自行推断。“待创建”在资产已存在的语境下表示“大纲待创建”。
- 权限决策：只有平台管理员可新增手册、修改名称和 A/B 角、归档与恢复；普通角色只读，后端对写请求二次鉴权。
- 删除决策：不物理删除手册，归档后从默认列表隐藏但保留大纲、文档、任务、责任和审计关系；运行中任务阻断归档。
- 数据与交互：手册查询使用服务端搜索和分页；页面级更新时间只在右上角展示一次，取服务端时间，缺失时明确不可用。
- 实施纠偏：阶段筛选、分页和对象选择均保留 URL 上下文；浏览器前进/后退会重读对应服务端页。对话框关闭/取消不能触发保存。归档确认展示真实可得的关联数量，缺失时不构造数字。

## 提示词-20260902-005：权限角色与增量构建概要设计

- 时间：2026-09-02 11:33:59 +0800
- 类型：产品决策 / 概要设计
- 原始决策：业务角色允许一人多角色；不默认禁止审核本人提交内容，但存在自审时至少还需一名其他审核人有效通过；当前增量构建由用户人工选择 PingCode 文档内容，自动变更候选作为后续待办，用于辅助或替代人工检索。
- 设计逻辑：授权不能由角色名称单独决定，采用“主体 → 角色 → 资源范围 → 责任绑定 → 动作与条件”分层；允许权限在有效范围内取并集，显式拒绝、资源状态和审核门禁优先。增量构建分为当前人工选材闭环和目标自动候选/GitLab MR/CI 闭环，目标能力不能写成已实现事实。
- 已完成：新增《知识中心-权限管理-业务角色与资源授权-概要设计文档》和《知识中心-文档生产-增量构建垂直业务-概要设计文档》；同步总体需求、总体架构、目录索引和正式决策记录。
- 审核不变量：同一自然人拥有多个角色仍只按一个稳定用户 ID 计票；提交人参与通过决定时，至少存在一个不同于提交人的有效审核人。空间可以收紧，不能放宽为提交人单独通过。
- 当前实施边界：PingCode 采用人工选择和不可变来源快照；真实 GitLab 分支、提交、MR 和 CI 尚未完成专项验收，没有真实证据不得标记成功。平台内发布与外部官方交付状态分离。
- 待办：详细设计动作字典、责任绑定、授权判断、有效审核票、变更集和人工外部证据契约；使用真实历史增量样本论证自动候选的召回、误报、证据追溯和人工节省时间。
- 验证：本轮文档范围差异检查无空白错误；旧“默认禁止自审”表述已清除；概要设计和上游版本引用已对齐。未执行代码或真实后端 API 验证，因为本轮仅修改需求、概要设计与决策文档，不宣称功能已实现。全仓检查仍受无关既有文件的尾随空格阻断。
- 事实源收口：资产工作台不再于启动阶段并行读取旧 `/assets/catalog` 投影，只使用 `/assets/handbooks` 分页接口，避免旧结构回包覆盖五阶段状态和对话框状态。

## 提示词-20260902-005：知识资产生命周期术语与按需详情

- 时间：2026-09-02 12:05:07
- 类型：设计纠偏 / 前端实施
- 事实边界：“已发布”表示形成正式交付版本；“已停用”表示资产退出日常使用但历史关系保留。两者不是同义状态，界面不得用“已发布”替代停用事实。
- 术语决策：最终阶段“手册发布”简化为“已发布”；用户操作统一为“停用/重新启用”。内部 `archive/restore` 路由和审计动作保留兼容，不作为界面文案。
- 渐进披露：移除主筛选中的“包含已归档”勾选框；已停用资产只通过平台管理员二级入口访问，不进入普通用户主任务和五阶段统计。
- 布局决策：资产页默认不选择第一本手册，列表占满宽度。点击手册名称或“查看”后才打开 `280px～340px` 弹性详情；内容区小于 `1100px` 时使用独立全宽详情。
- 状态恢复：关闭详情只移除 `handbookId`，保留 `lifecycle`、搜索、筛选和分页；浏览器前进、后退和刷新继续以 URL 为恢复依据。

## 提示词-20260902-006：知识中心局域网入口

- 时间：2026-09-02 14:29:18 +0800
- 类型：运行配置 / 局域网可达性
- 对外入口：隔离知识中心默认使用 `http://192.168.130.180:13510/knowledge-center/`，并允许通过 `KNOWLEDGE_CENTER_PUBLIC_HOST` 覆盖。
- 内外边界：前端监听 `0.0.0.0`；文档、认证等内部服务和脚本健康检查使用 `127.0.0.1`，不将内部 API 直接暴露到局域网。
- CAS 契约：认证服务的 `service` 地址使用局域网入口，确保登录回调返回用户实际访问的地址。
- 外部前提：当 `192.168.130.180` 属于 Windows 宿主机时，必须配置 TCP `13510` 转发和防火墙放行；该动作需要 Windows 管理员权限，不能由 WSL 内重启脚本代替。
- 当前状态：WSL 前端监听 `0.0.0.0:13510`，本机入口返回 `200`；`192.168.130.180:13510` 尚未连通，确认宿主机规则仍未配置。重启脚本已自动识别当前 WSL IP 并打印完整管理员 PowerShell 命令，不再绕行权限链路。
- 人工操作：在 Windows 管理员 PowerShell 执行脚本输出的 `netsh interface portproxy` 两条命令和 `New-NetFirewallRule`，然后执行 `Test-NetConnection -ComputerName 192.168.130.180 -Port 13510`。WSL IP 变化后重新执行第二条转发命令。

## 提示词-20260902-007：知识资产列表与操作栏去重

- 时间：2026-09-02 14:48:39 +0800
- 类型：前端交互纠偏 / 信息收敛
- 入口决策：手册名称作为唯一查看入口，删除与其行为完全相同的“查看”按钮。
- 展示边界：列表保留 A/B 角和章节/页面数用于横向比较；右侧改为“当前手册操作栏”，只保留名称、阶段和大纲/文档生产/审核入口，不重复列表事实。
- 权限收口：“管理”列仅平台管理员可见，只承载停用/重新启用；普通角色不渲染空列。
- 视觉语义补充：蓝色文字表示可点击，不限定为页面跳转；四个处理入口同级显示且不增加箭头。手册名称始终使用蓝色可点击样式，不可点击的事实文字不使用蓝色。
- 关闭控件：关闭操作栏是可恢复的界面操作，不是删除或停用，因此使用中性透明底的 `16px` Lucide `X`，不使用会误导为危险操作的浅红色常驻背景。
- 颜色纠偏：“手册资产”和“当前手册”不可点击，改用次级灰色；“新增手册”是可点击操作，改用平台蓝色。本次仅在知识资产模块设置显式样式，不改动其他模块的全局标签颜色。

## 提示词-20260902-008：新增手册聚焦式对话框

- 时间：2026-09-02 15:47:24 +0800
- 类型：前端设计确认 / 表单实施
- 方案决策：采用聚焦式单列对话框，最大宽度 `520px`，对话框圆角 `8px`、输入控件圆角 `6px`；避免把新增流程拆成多步或在宽抽屉中制造空白。
- 字段层级：手册名称、所属产品和 A 角负责人必填；B 角负责人可选；A/B 角作为同一责任信息组。概要说明属于低频可选信息，默认折叠并按需展开。
- 可见性决策：“对外可见”在新增手册对话框中直接展示，采用带说明和键盘焦点的紧凑开关；不重复加入资产列表首屏，以免削弱责任、阶段和章节/页面规模的比较效率。
- 文案与图标：主提交按钮从“创建”改为“创建手册”；右上角关闭统一使用 Lucide `X`，取消和关闭均不得触发表单提交。
- 稳定性纠偏：平台上下文与资产接口并行返回时不得全量重绘并销毁正在使用的弹窗；弹窗打开期间延迟资产视图刷新，关闭后再应用最新投影，保留用户输入和焦点连续性。

## 提示词-20260902-009：模块页面标题收敛

- 时间：2026-09-02 15:55:00 +0800
- 类型：前端信息架构纠偏 / 小范围实施
- 决策：右侧页面标题与左侧导航使用同一正式名称；删除“负责人工作台”“知识中心总览”“手册资产”眉标题及各模块口号式说明。
- 边界：工作台标题为“工作台”，知识资产标题为“知识资产”；资料清洗、大纲管理、模板管理、文档生产、审核与发布、平台管理同理。风险、空态和服务不可用提示仅保留在实际业务区块内，不放在页面顶部导语。
- 视觉适配：桌面端基础字号令牌调整为 `15.4px`，在浏览器 100% 缩放下接近原设计 110% 缩放的舒适度；移动端保持 `16px`，不使用页面级 `zoom`。
