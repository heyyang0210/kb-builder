# 批量生成方案

## 一、生成方式对比

### 方式1：单个知识点生成

**输入示例**：
```json
{
  "name": "B+树索引原理",
  "part": "第二部分：数据库核心原理",
  "chapter": "2.2 索引原理与实现",
  "type": "理论机制"
}
```

| 优点 | 缺点 |
|------|------|
| ✅ 质量最高，每个知识点充分展开 | ❌ 效率低，需要多次调用 |
| ✅ 可以针对每个知识点定制 | ❌ 需要人工逐个构造输入 |
| ✅ 便于质量控制和审阅 | ❌ 生成 200+ 文档需要 200+ 次调用 |
| ✅ 失败影响范围小 | ❌ 上下文切换成本高 |

**适用场景**：
- 关键知识点（需要高质量）
- 复杂知识点（需要详细展开）
- 首次生成（验证模板效果）

---

### 方式2：一次性批量生成（全部 200+ 知识点）

**输入示例**：
```
请根据数据库知识点大纲，生成所有 200+ 知识点的文档...
```

| 优点 | 缺点 |
|------|------|
| ✅ 一次调用，看似高效 | ❌ 超出上下文窗口限制 |
| ✅ 减少重复操作 | ❌ 质量严重下降，内容空洞 |
| | ❌ 容易遗漏知识点 |
| | ❌ 无法保证每个文档的完整性 |
| | ❌ 失败时全部重来 |

**结论**：❌ **不推荐**，质量无法保证

---

### 方式3：按章节分批生成（推荐）⭐

**输入示例**：
```
请生成「第二部分：数据库核心原理」章节的所有知识点文档，
包含以下 4 个知识点：
1. 2.1 存储引擎与物理存储
2. 2.2 索引原理与实现
3. 2.3 事务处理与并发控制
4. 2.4 日志与恢复
```

| 优点 | 缺点 |
|------|------|
| ✅ 质量与效率的平衡 | ❌ 需要分 8 次调用 |
| ✅ 每次 3-5 个知识点，上下文可控 | ❌ 需要人工协调 |
| ✅ 相关知识点可以相互引用 | |
| ✅ 便于质量控制 | |
| ✅ 失败影响范围可控 | |

**适用场景**：
- 日常批量生成
- 章节级别的文档生成
- 团队协作分工

---

## 二、推荐方案：按章节分批生成

### 2.1 分批策略

根据大纲结构，分为 8 个批次：

| 批次 | 章节 | 知识点数 | 预计文档数 | 难度 |
|------|------|---------|-----------|------|
| 1 | 第一部分：数据库基础入门 | 4 | 15-20 | ★ |
| 2 | 第二部分：数据库核心原理 | 4 | 15-20 | ★★ |
| 3 | 第三部分：数据库管理 | 5 | 20-25 | ★★ |
| 4 | 第四部分：SQL编程与开发 | 5 | 20-25 | ★★★ |
| 5 | 第五部分：性能优化 | 5 | 20-25 | ★★★ |
| 6 | 第六部分：高可用与集群 | 5 | 20-25 | ★★★ |
| 7 | 第七部分：数据库内核 | 5 | 20-25 | ★★★ |
| 8 | 第八部分：架构设计与实践 | 5 | 20-25 | ★★★ |

### 2.2 每批次生成流程

```mermaid
flowchart LR
    A["选择章节"] --> B["提取知识点"]
    B --> C["构造提示词"]
    C --> D["AI生成"]
    D --> E["质量检查"]
    E --> F["保存到output/"]
    F --> G["记录日志"]
```

### 2.3 质量保障

每批次生成后：
1. 检查文档数量是否符合预期
2. 抽查 2-3 篇文档的质量
3. 验证格式规范是否遵守
4. 确认输出路径正确

---

## 三、最终提示词模板

### 3.1 单批次生成提示词（推荐）

```markdown
# 任务：批量生成 YashanDB 知识文档

## 输入信息

**目标章节**：[章节名称，如"第二部分：数据库核心原理"]
**知识点列表**：
1. [知识点1名称] - [简要描述]
2. [知识点2名称] - [简要描述]
3. [知识点3名称] - [简要描述]
4. [知识点4名称] - [简要描述]

## 生成要求

### 1. 使用 Skill
请按照 `skills/00-通用生成-skill.md` 的流程执行：
- 自动判断每个知识点的类型
- 选择对应的模板（7套模板之一）
- 按照模板结构生成文档

### 2. 引用资料
- 优先引用 `references/oracle-kb/` 中的 Oracle 知识库文档
- 改写为 YashanDB 版本，标注差异
- 所有 SQL 示例必须可执行

### 3. 格式规范
- 遵守 `config/全局格式规范.md`
- 所有图表使用 Mermaid 语法
- 所有代码块标注语言类型
- 不出现客户名称和特定业务表名

### 4. 输出要求
- 每个知识点生成一个独立的 Markdown 文档
- 文件命名：`[序号]-[知识名称].md`
- 保存到：`output/[章节编号]-[章节名称]/` 目录
- 包含完整的 YAML 元数据头

### 5. 质量自检
每篇文档生成后，按 `config/质量验证标准.md` 进行自检：
- 结构完整性检查
- 格式正确性检查
- 确保无空白占位符残留

## 输出格式

请为每个知识点输出：

### 知识点1：[名称]
**文件路径**：`output/[章节编号]-[章节名称]/[序号]-[名称].md`
**使用模板**：[模板名称]
**知识类型**：[类型]

[完整的 Markdown 文档内容]

---

### 知识点2：[名称]
...

---

## 注意事项

1. 每个知识点独立生成，不要合并
2. 相关知识点之间可以相互引用
3. 如果某个知识点内容过多，可以适当拆分
4. 生成完成后，输出一个汇总报告，包含：
   - 生成的文档数量
   - 每个文档的文件路径
   - 使用的模板类型
   - 自检结果
```

---

### 3.2 单知识点生成提示词（高质量场景）

```markdown
# 任务：生成单个 YashanDB 知识文档

## 输入信息

```json
{
  "name": "[知识点名称]",
  "part": "[所属部分]",
  "chapter": "[所属章节]",
  "description": "[详细描述]",
  "type": "[知识类型，可选]",
  "target_db": "[目标对比数据库，兼容性类必填]",
  "references": {
    "oracle_ref": "[Oracle参考文档路径]",
    "test_cases": "[测试用例路径]"
  }
}
```

## 生成要求

[同上述"生成要求"部分]

## 输出

请生成完整的 Markdown 文档，包含所有必选章节。
```

---

## 四、实际执行建议

### 4.1 首次生成（验证阶段）

1. 选择 1 个知识点，使用**单知识点提示词**
2. 生成后仔细审阅，验证模板效果
3. 根据审阅结果调整 Skill 和模板
4. 确认效果后再批量生成

### 4.2 批量生成（生产阶段）

1. 按章节分批，每批 3-5 个知识点
2. 使用**单批次生成提示词**
3. 每批次生成后抽查 2-3 篇
4. 发现问题及时调整，再继续下一批

### 4.3 团队协作

| 角色 | 职责 |
|------|------|
| 协调人 | 分配章节、协调进度 |
| 生成人员 | 执行提示词、生成文档 |
| 审阅人员 | 审阅文档质量、提出修改意见 |
| 测试人员 | 双库验证、测试SQL |

---

## 五、总结

| 方式 | 推荐度 | 适用场景 |
|------|--------|---------|
| 单个知识点 | ⭐⭐⭐⭐⭐ | 关键知识点、首次验证 |
| 一次性全部 | ⭐ | ❌ 不推荐 |
| 按章节分批 | ⭐⭐⭐⭐ | 日常批量生成 |

**最终推荐**：**按章节分批生成**，每批 3-5 个知识点，平衡质量与效率。
