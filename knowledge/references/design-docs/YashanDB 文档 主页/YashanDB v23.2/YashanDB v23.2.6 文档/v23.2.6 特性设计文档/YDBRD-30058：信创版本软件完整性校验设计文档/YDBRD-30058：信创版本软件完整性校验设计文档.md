Created by 贺国锋, last modified on 九月 03, 2024

#   [YDBRD-30058：信创版本软件完整性校验设计文档](https://conf.yasdb.com/pages/viewpage.action?pageId=163006653)  

SR链接：     [https://pingcode.yasdb.com/pjm/items/66864266288e197820b64aa3](https://pingcode.yasdb.com/pjm/items/66864266288e197820b64aa3)    ?#YDBRD-30058 【安全】信创版本软件完整性校验

# 1. 总述

在软件发布之时，对yasdb和各种库文件进行完整性校验，产生签名数据和对应公钥。    
  软件启动时，自动检测签名是否正确，如果不正确则停止启动。

##   [2. ](https://conf.yasdb.com/pages/viewpage.action?pageId=138545264#4-%E7%89%B9%E6%80%A7)    功能列表

1、提供签名工具

2、软件包构建时生成签名和公钥

3、yasdb软件启动时进行签名效验

# 3. 规格与约束

1、签名数据和公钥保存在yasdb二进制中，仅对yasdb和lib库进行完整性效验。

# 4. 特性

## 4.1 新增签名工具yassign

     在tools下面提供yassign工具，输入参数为需要进行签名的文件或者路径，若为路径，则按照字母顺序，在签名时对路径下的文件进行排序。

     签名工具执行完毕后，在标准输出上输出签名和公钥。

     输出的签名和公钥使用AES128对称加密算法进行加密，加密密钥采用编译时的GIT版本号，进行50次SM3 Hash后生成。

  


  上车前需要从版本包中删除该工具。

## 4.2 对yasdb和lib进行sign签名

    在build构建过程中，适配build.py脚本，在install步骤结束后，根据install安装的路径，对install/lib目录和install/bin/yasdb文件进行数字签名，生成加密后的签名数据和签名公钥。

    签名数据和签名公钥保存在conf目录下的signature.ini中。

  


    排除三方库。

## 4.3 yasdb启动验证sign签名

    yasdb在启动时，从conf目录下的signature.ini中读取加密后的签名和公钥，然后利用相同的方式生成自己的AES128的解密密钥，对签名和公钥解密。

    然后利用解密获取的公钥，对签名数据进行验签。

    若效验通过，则正常启动，否则报错退出。

# 5.兼容性

# 6.未来规划

# 7.附录

  [https://sourceware.org/legacy-ml/binutils/2008-06/msg00216.html](https://sourceware.org/legacy-ml/binutils/2008-06/msg00216.html)  

  


## Comments:

|  [](null)  ,1、问下ext文件放什么的，不要放在conf下面，放在ext下面    
  2、非信创版本，看这个参数；信创版本，必须验证签名    
  3、用编译宏控制住,Posted by heguofeng at 八月 26, 2024 17:28|
|---|
|  [](null)  ,1、签名和公钥进行加密,2、每次编译增加对称加密的密钥,3、还是使用配置文件的方式，对称加解密,Posted by heguofeng at 八月 30, 2024 15:04|
|  [](null)  ,会议纪要：,1、yassign工具不能从版本发布，测试完成后上车前需要从发布包中去除,2、签名的lib库中排除第三方的lib库,3、确认下升级回退的场景是否有问题,Posted by heguofeng at 九月 03, 2024 10:24|
