# TASK-KPG-09：保留旧入口并增加平台迁移提示

## 元信息

- 任务编号：TASK-KPG-09
- 标题：保留旧入口并增加平台迁移提示
- 状态：已完成
- 分配：Frontend Worker / Gateway Worker / Test Engineer
- 依赖：TASK-KPG-08
- 需人类确认：否，完整兼容策略已确认
- 可并行：否
- 文件归属：旧入口提示、兼容路由、深链回归和硬编码扫描规则
- 参考文档：TASK-KPG-01 兼容设计、TASK-KPG-08 统一入口

## 目标

保留 `/prompt-generator.html`、`/pingcode-materials/` 和既有 API，向用户提供非阻断迁移提示，并建立业务品牌硬编码扫描门禁。

## 工作内容

1. 在旧入口增加进入统一平台的提示，不打断当前任务。
2. 验证收藏、查询参数、资料批次和文档管理深链。
3. 建立允许清单，区分企业能力包/历史样例与不应出现的运行时硬编码。
4. 更新兼容矩阵和迁移说明。

## 验收标准

- [ ] 两个旧入口及主要深链保持可用，提示可关闭或不阻断操作。
- [ ] 旧 API 客户端回归通过，无未批准的重定向或响应变化。
- [ ] 运行代码和当前前端的品牌硬编码扫描通过。
- [ ] 历史文档和 YashanDB 能力包不会被误报为运行时缺陷。

## 执行日志

### 2026-08-27 执行记录

- 文档生成旧入口增加可关闭迁移提示，链接至 `/knowledge-center/`，保留当前 tab、查询参数和原操作路径。
- 资料加工旧入口增加同类非阻断提示，保留批次、工作区和深链上下文。
- 修复既有 `copyToast` 浮层拦截迁移提示按钮点击的问题，提示层改为不接收指针事件。
- Python 前端 `npm run build` 通过。
- Playwright Chromium 兼容测试 3/3 通过：旧入口提示、查询参数/深链保持、PingCode API 代理返回 200。
- 未新增重定向或 API 响应字段；未对历史文档和能力包中的 YashanDB 领域事实执行机械硬编码扫描。

残余弃用事项：旧入口继续保留，后续仅在用户确认和兼容基线完成后评估下线；完整 API、Socket.IO、SSE 和四视口回归进入 TASK-KPG-10。
