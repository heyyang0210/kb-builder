# TASK-KPG-05：将文档生成器改为企业配置驱动

## 元信息

- 任务编号：TASK-KPG-05
- 标题：将文档生成器改为企业配置驱动
- 状态：已完成
- 分配：Agent Runner Backend / Frontend Worker / Test Engineer
- 依赖：TASK-KPG-04
- 需人类确认：否，改变生成业务规则或旧 API 时需要
- 可并行：可与 TASK-KPG-06 并行，但共享配置和网关文件不得同时修改
- 文件归属：`agent-runner` 生成链路、生成器前端、对应设计和测试
- 参考文档：YashanDB 企业能力包和生成工作流设计

## 目标

移除文档生成器运行时的 YashanDB 品牌、目标产品、MCP 描述、提示词标题、输出命名等硬编码，由企业能力包注入，同时保持生成行为与旧 API 兼容。

## 工作内容

1. 将运行文案和业务默认值迁入加载后的企业上下文。
2. 生成任务冻结企业能力包版本和配置指纹，保证结果可追溯。
3. 保持 Planner、Retriever、Generator、Validator 和文档管理契约不变。
4. 同步设计、中文界面和黄金样例测试。

## 验收标准

- [x] 生成器不再依赖未登记的 Agent、Prompt、Skill、模板和质量规则资源。
- [x] 既有生成 API、任务状态、输出结构和 YashanDB 聚焦样例不回归。
- [x] 普通 Workflow、直写任务和过程元数据记录企业配置指纹，不记录敏感配置。
- [x] 真实调用健康接口并创建、查询一条生成 Workflow 成功，模型调用已进入登记的 Planner Prompt。

## 执行日志

- 能力包补齐 Comparator Agent 和 7 类文档生成 Skill，共解析 29 个版本化资源。
- 新增进程内深度只读资源访问器；公开上下文保持脱敏，绝对路径只留在后端内部。
- 四类 Agent Prompt、生成专家品牌、Prompt Generator 资源校验、质量规则和直写模板/Skill 均改为能力包驱动。
- 普通 Workflow 与直写任务追加非敏感 profile 追溯；旧字段、旧 Skill/模板相对路径和 API 状态保持兼容。
- Node 聚焦测试 33/33、Python 契约与加载器 6/6 通过；Node 4198 真实健康、任务创建和状态查询返回 200。
- 外部模型调用已进入登记的 Planner Prompt，但未等待生成完成；真实黄金文档质量仍依赖可控模型/MCP 验收环境。
- 类型识别、手册输出目录映射和固定适用版本暂作为 YashanDB 兼容策略保留；当前契约缺少独立生成策略资源类型，已登记为后续扩展项。
