# TASK-KPG-07：增加平台运行上下文接口并接入统一网关

## 元信息

- 任务编号：TASK-KPG-07
- 标题：增加平台运行上下文接口并接入统一网关
- 状态：待开始
- 分配：Node Backend / Python Backend / Gateway Worker / Test Engineer
- 依赖：TASK-KPG-05、TASK-KPG-06
- 需人类确认：否，新增只读接口已在计划中确认
- 可并行：否
- 文件归属：两端运行上下文路由、统一网关代理、接口设计和契约测试
- 参考文档：TASK-KPG-03 的内部 DTO 与 TASK-KPG-01 的接口设计

## 目标

提供脱敏的平台运行上下文接口，使统一前端可读取平台名称、企业标识、领域版本、模块、连接器能力和配置指纹。

## 工作内容

1. 在生成服务和资料加工服务提供一致的只读投影。
2. 统一成功、配置不可用和服务不可用错误语义。
3. 在 3500 网关接入平台上下文和两模块健康状态。
4. 验证响应不包含凭证、绝对路径、模型密钥或完整内部配置。

## 验收标准

- [ ] 真实 HTTP 调用两端接口均成功，公共字段与配置指纹一致。
- [ ] 网关可聚合模块状态，单模块失败时返回可恢复的中文状态。
- [ ] 敏感信息和绝对路径扫描通过。
- [ ] 既有 API 路由和代理行为不回归。

## 执行日志

待执行时补充接口、HTTP 状态、响应摘要和安全测试。
