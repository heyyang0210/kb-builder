# 每日开发报告 — 2026-08-18

## 今日完成

- `TASK-RAG-KG-M2-LINEAGE-REWORK-01` 决策前 A 组修订（Planner / Backend Worker / Test Engineer / Doc Writer）
  - 非对象 evidence 单条隔离并生成稳定问题 ID。
  - normalized 路径、普通文件和 symlink 边界 fail-closed。
  - issues JSONL 纳入四文件自校验，并在原子替换前二次校验。
  - overlap 跨 resource、重复 chunk/evidence producer ID 被隔离或整体拒绝。
  - 100 组输入乱序结果稳定；并发 writer 恰好一个提交成功，失败方得到结构化冲突。
  - `tests.test_production_lineage`：34 项，32 passed、2 skipped；新增 PL-A05、PL-A06、PL-A11 组件断言。
  - Lineage/版本/回放/生产适配器组合回归：66 项，64 passed、2 skipped。
  - 完成目标快照只读审计：确认 2,027 个 JSON 权限错误信封已污染 chunk、candidate 和 graph edge；确认 55,324 条 legacy candidate 无可信 extractor 版本快照。
  - 完成供应商错误信封输入边界设计：冻结来源元数据 + 完整契约 + 批准指纹方案、三态检测接口、配置快照、质量问题、性能限制和 PL-A08—A10 验收规格；未修改生产代码。
- `TASK-RAG-KG-M2-LINEAGE-REWORK-01` G-LIN-01 Source/Schema 组件实现（Architect / Backend Worker / Test Engineer）
  - 新写仅输出 Schema `2.0`，Source occurrence 采用 `datasetId + resourceId + contentHash` 规范三元组哈希 ID。
  - Schema `1.0` 保持原规则只读验证和回放；歧义旧清单返回 `LEGACY_SOURCE_IDENTITY_AMBIGUOUS` 并阻断发布。
  - PL-B01—B06 完成；组合回归 73 项，71 passed、2 skipped，跳过项仅 PL-A18、PL-A23，等待 G-LIN-04。
  - Python 编译和目标文件 `git diff --check` 通过；全仓检查只发现无关既有 backup 文件尾空格。
- `TASK-RAG-KG-M2-LINEAGE-REWORK-01` G-LIN-02/2A 规则版本证据组件（Backend Worker / Test Engineer / Reporter）
  - Product Owner 确认 `2A+2C`；决策、选择理由、适用边界和回滚条件已写入设计与任务卡。
  - 实现 `KeywordRuleSnapshot`、`KeywordExtractorVersion.resolve`；新 producer 显式写入 `schemaVersion/extractorVersion/extractorSnapshotRef`，规则路径固定 `model=None/modelStatus=not_applicable`。
  - 新增 7 项聚焦测试；Lineage/TrainingService 组合回归 166 项，164 passed、2 skipped。
  - 历史只读审计基线为 `RECOVERABLE=0`、`ISOLATE=55,324`；不从当前配置、源码或 Git 时间推断历史版本。
- `TASK-RAG-KG-M2-LIN-2C-TST-01 / BE-01` 冻结输入规则重建内核（Planner / Test Engineer / Backend Worker）
  - Planner 拆分 2C 内核、公共 API 和真实隔离验收任务；拆分时 API-A 仍处于显式确认门禁，随后已按当日常设推荐方案授权选定。
  - Test Engineer 新增 T01—T09 红灯测试；Backend Worker 实现 `KeywordRuleRebuild`，覆盖冻结 manifest/artifact 校验、稳定 `rebuildKey`、按 key reservation/CAS、锁外执行、原子独立 run 和规则-only contract。
  - 2C 专项 `tests.test_keyword_rule_rebuild` 9/9 通过；与既有组件组合回归 175 项，173 passed、2 skipped、0 failed；未调用公共 API，未写入目标生产批次。
  - 后端全量 discover 收集 417 项，仍有 2 项范围外失败及一个 asyncio `ResourceWarning`；失败与既有 `RISK-TSR-003` 一致，已更新风险计数，不作为 2C 聚焦回归通过的替代证据。

## 今日进行中

- M2 生产 Lineage 修订任务仍为 `in_progress / partially-unblocked`；G-LIN-02/2C 内核已完成，`sourceDatasetId` 公共触发、生产 governance package 和 publish 验证尚未实现，未迁移物理路径。
- PL-A18、PL-A23 等待 G-LIN-04 冻结偏移单位和 formal offset 表示。

## 风险与阻塞

- G-LIN-01 和 G-LIN-02/2A 组件已完成；G-LIN-02/2C 业务恢复路径未完成。G-LIN-02-API/API-A、G-LIN-03/3A 和 G-LIN-04/4A 已选定，风险已由“决策阻塞”转为“待实施与验证”。
- G2-03 推荐架构已选定，但 JWT 外部参数、依赖审批和隔离测试身份仍未就绪，RG-20/23/25 和 G2.5 继续阻塞。
- 已确认的错误信封污染、重复 Source ID 与 legacy keyword 版本缺失均为 P0；组件回归不能替代真实训练出口和发布 API 验收。

## 下一步计划

- 按已确认的 API-A，先同步设计、公共契约和伪代码，再接入真实冻结 dataset 触发；当前内核测试已完成。
- 按已确认的 G-LIN-03/3A、G-LIN-04/4A 接生产治理包和偏移契约，再完成隔离 API、旧产物摘要不变、零模型调用和发布重验。
- 完成 keyword/formal 真实 FastAPI 回归、治理包篡改/CAS/force 阻断和生产脱敏样本回放。
- 身份参数齐备并批准 JWT/JWKS 依赖后实施 RG-20，再执行 RG-23 越权回归。

## 需要人类关注

- 用户建立常设决策授权：后续待确认方案默认采用有证据支持的推荐方案，但每次必须记录候选、选择、理由、风险、回滚和边界。据此已选择 G-LIN-02-API/API-A、G-LIN-03/3A、G-LIN-04/4A 和 G2-03 推荐架构。
- 常设授权不提供未知外部事实，也不等同于生产写入、发布或删除许可。安全线仍需提供 issuer、audience、JWKS URI、网关信任边界、claims 契约和隔离测试身份；参数不齐时保持 fail-closed。
