# 知识中心-知识资产-GitLab手册映射与协作审阅-风险清单文档

> 状态：持续跟踪
> 版本：0.1.0
> 更新时间：2026-09-02
> 关联设计：`../02-概要设计/知识中心-知识资产-GitLab手册映射与协作审阅-概要设计文档.md`

## 1. 当前结论

GitLab 地址和 CAS 登录入口可达，但当前开发机依赖已有 Git 凭证，知识中心尚无 OAuth 授权和真实连接能力。PingCode 未提供实例地址和代表性对象，无法验证 API 与 OAuth。以下风险未解除前，真实 GitLab/PingCode 连接器保持关闭，不以本地凭证、模拟结果或人工链接冒充正式接入。

## 2. 风险清单

| 编号 | 风险 | 影响 | 当前控制 | 责任角色 | 解除条件 | 状态 |
|---|---|---|---|---|---|---|
| RISK-KC-GL-001 | GitLab/PingCode OAuth Application 尚未注册 | 知识中心不能代表用户读取私有内容或创建 MR | CAS 与外部 API 授权分离；禁止复用浏览器 Cookie 和开发机凭证 | 外部平台管理员、平台管理员 | 两个平台分别完成 OAuth 注册、回调和最小权限验证 | 阻塞真实接入 |
| RISK-KC-GL-002 | 知识中心尚无正式 HTTPS 域名和证书 | OAuth 授权码可能通过不安全回调传输 | 局域网 HTTP 仅用于开发，不作为生产回调 | 部署管理员、安全负责人 | HTTPS 域名、证书、回调白名单和续期方案验证通过 | 阻塞真实接入 |
| RISK-KC-GL-003 | PingCode 实例、项目、SR/任务样本和 API 文档缺失 | 无法冻结 OAuth、分页、字段和附件契约 | 概要设计只冻结只读边界，不声称接口已完成 | PingCode 管理员、产品负责人 | 提供脱敏实例与样本，在隔离环境完成读取验证 | 待确认 |
| RISK-KC-GL-004 | 40 本资产与仓库内容不是一对一关系 | 错误映射会造成责任、版本和 MR 目标错误 | 只生成候选，管理员逐项确认中英文路径和分支 | 平台管理员、手册 A 角 | 40 行盘点全部确认并形成映射配置版本 | 待确认 |
| RISK-KC-GL-005 | 中英文目录可能不对称 | 语言切换出现错误内容或遗漏 | 分别保存路径；缺失时不跨语言回退 | 手册 A/B 角、文档作者 | 启用分支逐项完成路径存在性和 `_index.md` 对账 | 待验证 |
| RISK-KC-GL-006 | 版本命名规则可能误纳入实验分支 | 用户读取或提交到错误目标版本 | 正则只发现候选，管理员启用；`master` 独立显示 | 平台管理员、发布负责人 | 正式分支清单和默认分支完成确认 | 待确认 |
| RISK-KC-GL-007 | 阅读分支最新内容与任务基线发生漂移 | 基于旧内容生成补丁或覆盖并发修改 | 任务冻结 `baseCommitSha`，提交前比较目标 `HEAD` | 连接器、文档作者 | 基线冲突、刷新差异和人工接管测试通过 | 待验证 |
| RISK-KC-GL-008 | 现有文档生成器的 HTML 写入缺少统一安全边界 | 仓库 Markdown/HTML 可能造成脚本或不安全资源执行 | 不直接复制渲染路径；要求共享清理、链接校验和资源代理 | 前端、后端、安全负责人 | 恶意 Markdown、链接、图片和 HTML 安全测试通过 | 阻塞阅读器上线 |
| RISK-KC-GL-009 | 分支更新导致审阅文本锚点失效或误定位 | 批注可能指向错误内容 | 锚点绑定提交、路径、原文和上下文；不唯一时标记位置变化 | 审核模块、前端 | 漂移、重复文本、文件移动和删除样本验收通过 | 待验证 |
| RISK-KC-GL-010 | 一个任务创建多个 MR 时出现部分成功 | 重试可能重复分支、提交或 MR | 每个分支/分组独立幂等；成功结果保留，失败项单独重试 | 连接器、长任务模块 | 部分失败、超时、重复请求和恢复测试通过 | 待验证 |
| RISK-KC-GL-011 | 用户 OAuth 权限与知识中心责任权限不一致 | 越权或用户误以为平台能授予 GitLab 权限 | 两侧权限都必须通过；平台不创建或扩大 GitLab 权限 | 权限模块、连接器 | 跨用户、撤销、过期、无写权限和资源范围测试通过 | 阻塞写入启用 |
| RISK-KC-GL-012 | 正式仓库直接用于首次写入验证 | 错误分支或 MR 影响真实资料流程 | 真实写入默认关闭，先使用隔离仓库和测试身份 | GitLab 管理员、测试负责人 | 隔离仓库端到端验收和正式启用决策完成 | 阻塞正式写入 |
| RISK-KC-GL-013 | 平台发布与 GitLab/CI 状态被混为一个结果 | 外部失败被伪装成功或错误回滚平台版本 | 两类状态独立，CI 只读取真实远端证据 | 审核发布、连接器 | 状态投影和失败路径端到端测试通过 | 待验证 |

## 3. 必须由人工完成的前置事项

1. GitLab 管理员注册 OAuth Application，配置正式 HTTPS 回调地址和经评审的最小权限。
2. PingCode 管理员提供实例地址、项目范围、代表性 SR/任务和 API 文档，并确认是否支持 OAuth。
3. 部署管理员提供正式 HTTPS 域名、证书和密钥托管能力。
4. GitLab 管理员提供隔离测试仓库与测试身份，不使用正式 `cod-doc/yasdoc` 试错。
5. 平台管理员和各手册 A 角确认 40 本资产的中英文内容范围、启用分支和默认分支。

## 4. 验证证据要求

- 外部可达性、OAuth 授权、撤销和过期记录不得包含令牌正文。
- 映射验收保留仓库、真实分支、提交 SHA、路径、确认人和配置版本。
- 内容阅读验收覆盖目录顺序、Markdown、PNG、相对链接、缓存和安全降级。
- 审阅验收覆盖固定提交、文本重复、内容移动、文件删除和建议采纳权限。
- 写入验收覆盖基线冲突、分支重名、提交失败、MR 失败、CI 失败、部分成功和幂等重试。
- 没有真实外部证据时，状态必须保持“未配置”“待验证”或“失败”，不得显示成功。
