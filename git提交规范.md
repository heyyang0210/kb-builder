# Git 提交规范

本规范适用于仓库内所有代码、文档、配置、脚本、Skill、提示词和设计产物。提交信息采用 [Conventional Commits](https://www.conventionalcommits.org/)，当前以人工检查为准；仓库尚未启用 commitlint 或 Git hooks 自动门禁。

## 1. 格式

```text
<type>(<scope>): <中文摘要>
```

`scope` 可省略。除拼写修正等微小变更外，提交必须包含正文，说明具体修改和验证结果：

```text
<type>(<scope>): <中文摘要>

修改内容：
- 具体修改及其业务或技术影响

验证结果：
- 已执行的检查及结果；未运行测试时说明原因

BREAKING CHANGE: 不兼容变更说明
```

## 2. 类型

| 类型 | 用途 |
|---|---|
| `feat` | 新增用户可见功能或能力 |
| `fix` | 修复错误或异常行为 |
| `docs` | 修改说明、需求、设计、提示词或其他文档 |
| `refactor` | 不改变外部行为的代码或结构重构 |
| `test` | 新增或调整测试 |
| `chore` | 构建、依赖、工具和日常维护 |
| `ci` | 持续集成、发布或自动化流程 |
| `perf` | 性能优化 |
| `revert` | 回滚已有提交 |

## 3. 各类型正文要求

正文用于让评审者只阅读 `git log` 就能理解变更，不应只重复标题，也不要求机械罗列所有文件名。

| 类型 | 正文必须说明 |
|---|---|
| `docs` | 修改了哪些文档或文档类别；新增、修订或废止的主要内容；关键结论、边界或引用变化 |
| `feat` | 新增了什么能力；主要实现或交互；影响的用户、接口、数据或配置 |
| `fix` | 可观察的问题与根因；采用的修复方式；回归影响和防复发验证 |
| `refactor` | 调整了哪些结构或职责；为什么调整；哪些外部行为必须保持不变 |
| `test` | 覆盖了哪些场景和风险；新增或调整的测试层级；验证结果 |
| `perf` | 优化对象和瓶颈；采用的方式；优化前后的测量口径或数据 |
| `chore` / `ci` | 修改的工具、依赖或流程；影响范围；执行或迁移要求 |
| `revert` | 回滚的提交；回滚原因；恢复到的行为和后续处理 |

正文使用以下通用结构；无对应内容的段落可以省略：

```text
修改内容：
- ...
- ...

影响范围：
- ...

验证结果：
- ...
```

`docs` 提交建议直接写出核心文档名称或文档类别，例如“总体需求、概要设计、前端详细设计”，并概括各自形成的主要约束。`feat`、`fix` 和 `refactor` 不应只写“修改后端和前端”，而要描述对外能力、根因或职责变化。

## 4. 作用域

作用域使用稳定、可检索的模块名；无法归属于单一模块时省略。当前常用作用域包括：

```text
knowledge-center   知识中心需求、设计与前端 Demo
agent-runner       Agent Runner 服务与工作流
pingcode           PingCode 数据处理与连接器
frontend           通用前端或页面展示
retrieval          检索与知识加工
llm                大模型客户端与模型配置
skills             Skill 定义与运行约束
prompts            提示词、规则与生成配置
docs               通用项目文档
scripts            脚本和自动化工具
config             共享配置
roles              角色、任务路由与治理契约
```

目录新增模块时，可使用其稳定目录名；不要使用临时任务名、日期或个人姓名作为作用域。

## 5. 摘要与提交原则

- 摘要使用中文、动词开头（如“新增”“修复”“更新”“移除”），不加句号，建议不超过 72 个字符。
- 一个提交只处理一个主题；相关文件应保持在同一提交，不相关修改不要混入。
- 除微小修改外正文至少包含“修改内容”；有测试或检查时增加“验证结果”。
- 文档或代码变更应在同一提交中同步更新对应设计、测试和目录说明。
- 破坏性变更在类型后加 `!`，并在正文或脚注写明 `BREAKING CHANGE:`、影响范围和迁移方式。
- 不提交密码、令牌、API Key、私有地址、临时输出、构建产物或与本次主题无关的文件。
- 不确定是否应提交的生成文件或本地配置，先确认其是否属于仓库的事实来源。

## 6. 提交前检查

根据变更范围执行必要检查，至少确认：

```bash
git status
git diff --check
git diff --stat
```

代码、前端或后端变更还应运行对应目录的语法检查和测试；文档变更应检查 Markdown 链接、示例路径和引用文件是否有效。涉及后端接口或端到端流程时，按仓库 `AGENTS.md` 要求真实调用 API 验证。

## 7. 示例

```text
feat(knowledge-center): 增加手册完整度总览

修改内容：
- 新增结构、内容、责任和质量门禁四类完整度投影
- 增加按缺口下钻到责任模块的手册详情入口

验证结果：
- 桌面端和移动端关键视口无横向溢出
```

```text
docs(roles): 更新任务路由契约

修改内容：
- 修订角色职责和最小任务路由文档
- 明确直接处理、标准开发与治理任务的适用边界
- 补充人工审批边界和升级条件

验证结果：
- Markdown 引用路径和差异空白检查通过
```

详细开发、测试和目录约束见根目录 `AGENTS.md`；本文件是仓库 Git 提交信息的唯一规范入口。

---

*最后更新：2026-08-27*
