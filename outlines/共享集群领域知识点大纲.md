# 共享集群（Shared‑Cluster）数据库知识点大纲

## 一、共享集群架构基础

### 1.1 集群架构演进与定位
- 1.1.1 单机架构的瓶颈：计算能力、存储容量、可用性上限
- 1.1.2 主备与双活架构的局限性与共享集群的差异化优势
- 1.1.3 共享集群的核心特征：多节点共享存储、统一数据视图、透明应用访问
- 1.1.4 共享集群与传统分布式数据库（分库分表/存算分离）的边界与适用场景

### 1.2 硬件与存储层组成
- 1.2.1 共享存储子系统：磁盘阵列、SAN/NAS、分布式块存储的接入方式
- 1.2.2 节点间互连网络（集群互联）：高速低延迟网络（如RDMA、InfiniBand）的作用
- 1.2.3 网络分区与冗余设计：多路径、多网卡绑定与容错机制
- 1.2.4 共享存储的访问仲裁与锁机制（基于SCSI或定制协议）

### 1.3 集群软件栈与组件
- 1.3.1 集群管理器（Cluster Manager）：节点生命期管理、资源分配、状态广播
- 1.3.2 全局服务组件：全局锁管理器、全局缓存管理器、全局事务管理器
- 1.3.3 集群心跳与监控组件：节点存活检测、网络延迟监测
- 1.3.4 配置文件与集群状态库（如集中式配置存储）


## 二、全局缓存一致性与数据共享

### 2.1 全局缓存管理（Global Cache Management）
- 2.1.1 缓存融合机制（Cache Fusion）：不同节点间数据块的传递与协同
- 2.1.2 缓存粒度的选择：数据页、行、块等不同层级的共享策略
- 2.1.3 缓存状态转换模型（如保持模式、共享模式、独占模式）
- 2.1.4 缓存一致性协议：基于广播的主动推送 vs 基于请求的按需拉取

### 2.2 数据块（资源）的全局所有权管理
- 2.2.1 数据资源的“所有者”概念：哪个节点持有最新版本
- 2.2.2 所有权的获取、转移与释放流程（如锁升级/降级）
- 2.2.3 跨节点数据块的传送开销与网络带宽管理
- 2.2.4 缓存融合中的本地缓存与远程缓存的取舍

### 2.3 全局读写一致性与可见性
- 2.3.1 多节点同时读取同一数据块时的版本一致保障
- 2.3.2 多节点同时更新同一数据块时的冲突检测与解决
- 2.3.3 全局快照与读一致性的实现（基于SCN/版本号/时间戳）
- 2.3.4 全局跨节点事务的可见性规则


## 三、全局资源协调与锁管理

### 3.1 全局锁管理器（Global Lock Manager）
- 3.1.1 全局锁与本地锁的层次关系：两级锁体系
- 3.1.2 全局锁的类型：全局行锁、全局表锁、全局字典锁
- 3.1.3 全局锁的申请、授予、转换与释放流程
- 3.1.4 全局锁的排队机制（FIFO/优先级）与超时处理

### 3.2 锁粒度与并发控制
- 3.2.1 锁的粒度策略：精细锁 vs 粗粒度锁的权衡（吞吐 vs 争用）
- 3.2.2 全局死锁的检测与解决方法（集中式检测/分布式超时重试）
- 3.2.3 锁的持有时间与释放策略，预防锁长时间占用导致的“锁漂移”

### 3.3 全局事务协调
- 3.3.1 全局事务ID（GXID）的生成与分配机制
- 3.3.2 全局事务提交协议（如两阶段提交 / 并行提交优化）
- 3.3.3 跨节点事务的原子性与持久性保障
- 3.3.4 跨节点事务的异常处理（节点宕机后的恢复机制）


## 四、节点管理与高可用

### 4.1 节点生命周期管理
- 4.1.1 节点加入集群：注册、状态初始化、全局资源同步
- 4.1.2 节点正常退出：资源释放、状态同步、负载迁移
- 4.1.3 节点故障检测：基于心跳的超时判定与多级健康检查
- 4.1.4 节点隔离与驱逐策略（防脑裂的“仲裁”机制）

### 4.2 集群脑裂与防分裂
- 4.2.1 脑裂（Split‑Brain）的发生条件与危害
- 4.2.2 仲裁机制：Quorum（多数派）原则与第三方仲裁节点
- 4.2.3 投票权分配与权重策略（根据节点负载/角色加权）
- 4.2.4 脑裂后的自动恢复与数据一致性修复

### 4.3 故障转移与高可用切换
- 4.3.1 故障检测的时延与切换时间的权衡
- 4.3.2 自动故障转移（Failover）流程：资源接管、日志恢复、会话重连
- 4.3.3 手动切换（Switchover）与计划内维护流程
- 4.3.4 无缝应用连接（如连接池感知/客户端重连机制）


## 五、负载均衡与集群管理

### 5.1 集群内负载均衡策略
- 5.1.1 连接层负载均衡：基于客户端驱动 vs 中间件（如代理）的分发
- 5.1.2 查询路由策略：基于会话亲和性、资源利用率的动态调度
- 5.1.3 节点间工作负载的主动迁移（如热点数据节点平滑分流）
- 5.1.4 混合负载（OLTP+OLAP）在集群节点上的差异化部署

### 5.2 资源管理与调度
- 5.2.1 全局资源池划分（CPU、内存、I/O带宽）与节点独立分配
- 5.2.2 动态资源调整：根据实时负载自动调整节点资源配额
- 5.2.3 集群级工作负载监控与历史趋势分析
- 5.2.4 节点过载保护与服务质量（QoS）降级策略

### 5.3 集群运维操作
- 5.3.1 在线扩容（添加新节点）：数据再平衡与全局状态更新
- 5.3.2 在线缩容（移除节点）：负载转移与资源回收
- 5.3.3 集群配置变更的在线生效（如网络参数、锁超时阈值）
- 5.3.4 补丁升级与滚动更新策略（避免全集群停机）


## 六、集群环境下的性能监控与调优

### 6.1 共享集群特有性能指标
- 6.1.1 全局缓存命中率与跨节点数据传送流量（Global Cache Transfer）
- 6.1.2 全局锁请求频率、等待时间与争用率
- 6.1.3 节点间网络延迟、丢包率与重传率
- 6.1.4 全局事务提交延迟与协调时间

### 6.2 性能监控与诊断方法
- 6.2.1 集群范围的等待事件分类（如全局锁等待、跨节点I/O等待）
- 6.2.2 节点级与集群级统计信息的聚合与对比分析
- 6.2.3 全局执行计划的获取与执行代价分析
- 6.2.4 实时集群健康检查与告警阈值设定

### 6.3 集群调优常见方向
- 6.3.1 缓存融合网络带宽与延迟的优化（如使用更高速的互联协议）
- 6.3.2 全局锁粒度与持有时间的调整，减少跨节点锁争用
- 6.3.3 数据分片（分区）的放置策略，尽量将热点数据集中到单个节点
- 6.3.4 会话亲和性（Session Affinity）策略，减少跨节点缓存切换次数
- 6.3.5 全局事务提交批处理与异步提交的合理使用


## 七、数据高可用与灾难恢复

### 7.1 集群级别的数据保护
- 7.1.1 共享存储的高可用（如RAID、镜像、多副本存储）
- 7.1.2 节点故障时的未提交事务回滚与数据文件一致性
- 7.1.3 日志（Redo）的多节点同步与可恢复性

### 7.2 集群级别的备份与恢复
- 7.2.1 全集群一致性备份（冻结全局活动）的实现
- 7.2.2 基于时间点的集群恢复（PITR）与全局日志应用
- 7.2.3 单节点异常恢复而整体集群不中断的处理流程

### 7.3 灾难恢复与容灾
- 7.3.1 跨站点集群部署（双活/主备数据中心）
- 7.3.2 异地容灾的日志传输与数据复制策略
- 7.3.3 全局一致性的跨站点切换流程
- 7.3.4 容灾演练的步骤与注意事项


## 八、从单机/主备向共享集群的迁移实践

### 8.1 迁移前的架构评估
- 8.1.1 源架构的负载特征与业务依赖分析
- 8.1.2 共享集群能力与现有业务的匹配度评估（是否真正需要多节点并发）
- 8.1.3 网络环境与基础设施的预检（如集群互联带宽是否达标）

### 8.2 迁移实施策略
- 8.2.1 数据迁移方案：全量导出导入、逻辑复制、跨集群同步等
- 8.2.2 应用改造要点：连接字符串调整、事务隔离级别的适配、全局事务支持
- 8.2.3 并行迁移与数据一致性校验的“割接窗口”设计
- 8.2.4 灰度切换与回退方案（失败时快速恢复）

### 8.3 迁移后性能基准测试
- 8.3.1 迁移前的单机/主备性能基线采集（响应时间、吞吐量、资源占用）
- 8.3.2 迁移后在共享集群上执行同等负载，对比关键指标（全局缓存命中率、跨节点流量）
- 8.3.3 集群多维监控与瓶颈定位，针对性调整缓存融合策略与锁参数


## 九、进阶实践：从架构认知到集群调优专家

### 9.1 集群故障排查的思维框架
- **9.1.1 四类集群故障的快速分类**：
  - 网络类：心跳断连、网络延迟剧增、包丢失
  - 缓存类：全局缓存争用、跨节点传输阻塞
  - 锁类：全局死锁、锁超时、锁持有时间过长
  - 节点类：节点假死、节点倒挂、资源耗尽
- **9.1.2 从“异常现象”到“根因定位”的推理链**（示例）
  - 现象：应用响应超时，集群节点CPU正常 → 检查等待事件发现“全局缓存等待”高 → 分析跨节点传输量巨大 → 定位热点数据被多个节点频繁更新 → 优化分区策略将热点集中在单节点

### 9.2 综合实战案例
- **案例1：集群节点间网络延迟导致性能崩溃**
  - 现象：集群整体吞吐量骤降，各节点出现大量“网络延迟等待”
  - 排查：通过监控发现节点间互连网络丢包率升至 5%，重传次数上升
  - 根源：网络交换机故障，导致多路径冗余失效
  - 优化：启用备用链路，并降低缓存融合的数据块大小（减少单次传输量）
  - 结果：性能恢复，吞吐量回归到基线水平

- **案例2：全局锁竞争引发“雪崩式”锁等待**
  - 现象：某一热点表被高并发更新，导致全局锁争用严重，所有更新语句排队
  - 分析：该表未做合理分区，所有节点更新同一个数据块，全局锁升级为独占模式
  - 优化：按业务键对该表进行分区，将不同范围的数据分配到不同节点，减少跨节点锁请求
  - 结果：全局锁等待时间降低 90%，TPS 提升 300%

- **案例3：集群脑裂导致数据不一致**
  - 现象：某节点因网络闪断被集群管理器“隔离”，但该节点依然存活并继续写数据，导致同一数据存在两个版本
  - 排查：心跳超时阈值设置过小，误判节点离线
  - 优化：调整心跳超时时间（增加容忍窗口），并启用“多数派仲裁”机制
  - 恢复：通过全局日志比对，回滚被隔离节点写入的脏数据
  - 结果：集群重新恢复正常，数据一致性得到保障

- **案例4：全局事务频繁挂起，应用层超时**
  - 现象：大量跨节点事务提交超时，部分节点内存持续增长
  - 分析：协调者与参与者通信频繁，且参与者事务日志写入磁盘过慢
  - 优化：将事务日志从HDD迁移到SSD，并批量化提交（收集多个事务后统一提交）
  - 结果：全局事务提交时间从 200ms 降至 30ms

### 9.3 专家级能力培养
- **9.3.1 技术能力矩阵**：
  - 集群架构的全局视图能力（理解所有组件相互作用）
  - 跨节点性能分析能力（区分节点自身问题与集群整体问题）
  - 数据分布设计能力（合理设计分区与分布，减少跨节点交互）
  - 集群网络调优能力（链路聚合、TCP参数调整、QoS设置）
- **9.3.2 软技能与协作**：
  - 跨部门沟通（与应用开发、网络运维、存储工程师协作）
  - 集群升级与变更的风险预判和回退方案制定
  - 制定集群容量规划（根据业务增长趋势提前扩容）
- **9.3.3 持续优化闭环（PDCA）**：
  - Plan：建立集群的性能基线（全局缓存命中率、全局锁等待时间、网络流量）
  - Do：定期执行集群健康检查，调整参数与分区策略
  - Check：对比基线数据，验证优化效果，识别新瓶颈
  - Act：将验证有效的方法固化到运维脚本与文档中，形成标准操作流程
- **9.3.4 主动防御机制**：
  - 建立集群告警规则（如跨节点流量超过阈值、全局锁等待超出基线）
  - 定期进行故障演练（模拟节点宕机、网络分区），验证高可用切换速度
  - 每次应用上线前进行集群兼容性预演，评估对全局缓存和锁的影响


> **使用说明**：本大纲适用于共享集群数据库的架构学习、运维诊断、迁移适配及性能调优等场景。建议按照章节顺序逐步深入，重点关注**第六章（集群调优）**、**第八章（迁移实践）** 与**第九章（进阶案例）** 的实际落地应用。如需对任意章节进行深度扩展（如补充具体参数计算公式、细化监控指标解读、提供故障排查步骤模板），可在后续交互中提出。