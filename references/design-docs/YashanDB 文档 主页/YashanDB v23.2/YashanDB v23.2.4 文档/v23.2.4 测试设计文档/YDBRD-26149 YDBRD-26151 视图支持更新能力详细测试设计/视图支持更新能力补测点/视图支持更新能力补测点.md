Created by 刘清萍, last modified on 八月 16, 2024

嵌套view + 静态改写 + 两表主键

开发文档参考：    [Rewriter - YashanDB - SICS-CoD Confluence (yasdb.com)](https://conf.yasdb.com/display/YAS/Rewriter)  

外场问题单链接：    [【外场问题】YDBRD-31198 回溯改进 - YashanDB - SICS-CoD Confluence (yasdb.com)](https://conf.yasdb.com/pages/viewpage.action?pageId=162994205)  

|补测点|场景|备注|
|---|---|---|
|优化器改写(单表）|in/not in|  
|
|  
|exists/ not exists|  
|
|  
|any/all/some|  
|
|from 子查询上拉|filter下推到窗口函数|  `select`         `* `      `from`         `(`      `select`            `rank() over (`      `order`         `by`         `c1) `      `as`         `r1 `      `from`         `ydbrd24171_lsc_01 `      `where`         `c1 < 10) `      `where`         `r1 < 5;`  |
|  
|谓词下推支持集合操作子查询|  `select`         `* `      `from`         `(`      `select`         `* `      `from`         `tb_ydbrd_union_8782_heap_t1 t1 `      `union`         `select`         `* `      `from`         `tb_ydbrd_union_8782_heap_t2 t1) `      `where`         `a1 = 1;`  |
|覆盖多层嵌套后改写|支持filter上拉功能|  `select`         `* `      `from`         `(`      `select`         `* `      `from`         `test_ydbrd26109_heap_01 `      `where`         `id < 6)tb1 `      `left`         `join`         `test_ydbrd26109_heap_02 tb2 `      `on`         `tb1.id = tb2.id `      `where`         `tb2.id < 10;`  |
|  
|exists/ not exists|  
|
|  
|any/all/some|  
|
|select 后bool表达式|  
|  
|
|grouping sets|  
|  
|
|join|三表或者四表|  
|
|子查询|子查询,- 普通子查询
- 关联子查询
- 标量子查询
- 非标量子查询
- join on 子查询
- having 子查询
- where子查询
|  
|
|  
|  
|  
|
|  
|  
|  
|
|  
|  
|  
|
|  
|  
|  
|
