Created by 范瑜, last modified on 一月 26, 2024

# **1.概述**

SR：    [[YDBRD-22298] 【yasldr】支持切分不落地方案 - SICS-CoD Jira (yasdb.com)](https://jira.yasdb.com/browse/YDBRD-22298?filter=-1)  

开发设计：    [yasldr分区表导入性能优化 ](https://conf.yasdb.com/pages/viewpage.action?pageId=133562660)  

需求背景：  在分区表场景下，随着分区数量的上升，yasldr导入性能急剧恶化。由于分布式默认采用分区表实现，因此在分布式场景下，导入性能恶化的现象尤其显著，因此需要针对分区表导入进行性能提升。

需求目标：分布式性能优化， dn带备机， yasldr导入单个节点200MB/s

交付版本：23.2

交付形态：分布式

# **2.需求分析**

## **2.1 功能点分析**

### **2.1.1 优化场景**

（1）优化场景：  在分区表场景下，随着分区数量的上升，yasldr导入性能急剧恶化。

（2）原因分析：yasldr在调用C驱动的接口发送数据时，发送的一批数据中包含了多个分区的数据，而在驱动层会针对每个分区的数据进行单独组包。这样就会导致在分区数量上升时，这批数据将会被拆分为更多个小包发送，导致性能恶化降低。

（3）优化方案：  通过提前将数据按照分区进行组织，在调用C驱动的接口发送数据时，提供的就是一个分区的数据，避免拆分为小包发送。

|**术语**|**描述**|**借鉴业界**|**参考**|
|:---|:---|:---|:---|
|controller|主线程， 作为控制线程，负责命令行参数解析效验、load data语法解析效验、csv文件切分、reader和sender线程组创建和管理|否|不涉及|
|reader|reader线程，负责csv文件的读取、逐行解码、分区计算，将数据转换为行结构挂载到分区数据的行链表上|否|不涉及|
|sender|sender线程，负责分区数据的行列转换，数据发送，以及服务端返回的错误消息处理|否|不涉及|


### **2.1.2 对外接口**

|**接口**|**接口表现**|**接口说明**|
|:---|:---|:---|
|命令行参数|CSV_CHUNK_SIZE|用来控制文件划分的Chunk大小，即待导入的数据文件按照CSV_CHUNK_SIZE切分后提供给reader线程读取，默认128MB，取值范围[16M-1024M]，单位为MB|
|命令行参数|SENDERS|控制发送线程的数量。默认情况下，发送线程数量为ROUND(DOP，DECODER_THREAD_TIMES + 1)，取值范围[1,128]。若设置了SENDER_COUNT，则以设置值为准，若超过DOP，则报错。|


### **2.2.2 新增统计项**

|统计项|统计项子类|含义|
|---|---|---|
|YashanDB Loader execution statistics|Readers|线程的数量|
|  
|Senders|线程的数量|
|  
|Parses|解析耗时|
|  
|Prepare|准备执行环境的耗时|
|  
|TimeUint|单位: 微秒|
|Reader Execution Statistics|id|线程编号|
|  
|io_cnt|io次数|
|  
|io_cost|io耗时|
|  
|avg_cost|io平均耗时|
|  
|lines|解码的行数|
|  
|max_len|行最大长度|
|  
|min_len|行最小长度|
|  
|avg_len|行平均长度|
|  
|fetch_cost|获取一行数据的耗时|
|  
|decode_cost|解码耗时|
|  
|locate_cost|分区计算耗时|
|  
|to_row_cost|转为行结构耗时|
|  
|wait_cost|等待分区可用耗时|
|  
|enque_cost|送入发送队列耗时|
|  
|elapsed|线程总体的运行耗时|
|Sender Execution Statistics|id|线程编号|
|  
|send_cnt|发送次数|
|  
|send_cost|发送耗时|
|  
|max_cost|单次最大发送耗时|
|  
|commit_cnt|线程执行提交的次数|
|  
|commit_cost|提交的耗时|
|  
|deque_cost|从发送队列获取数据的耗时|
|  
|to_col_cost|行列转换的耗时|
|  
|elapsed|线程总体的耗时|
|Node Send Statistics|id|线程编号|
|  
|lines|节点的行数|
|  
|send_cnt|发送的次数|
|  
|send_cost|发送耗时|
|  
|max_cost|单次最大发送耗时|
|  
|commit_cnt|提交的次数|
|  
|commit_cost|提交耗时|
|  
|address|节点地址信息|


## **2.2 应用场景**

（1）分区多下场景下导入性能

## **2.3 规格约束**

（1）不同的部署模型和不同的分区数量，在最优性能的参数选择上可能不同，具体的参数选择设置需要进行实测。

（2）目前针对每一个分区都创建4个分区slot的方式，因此在分区数量过大的情况下，可能导致资源不足。

# **3. 详细测试设计**

## **3.1 测试设计方法**

因为本sr主要对yasldr进行重构，所以本次主要从以下方面进行测试

（1）执行历史自动化用例，主要有以下方面：

- 默认配置执行上车工程
- 调整新增对外参数CSV_CHUNK_SIZE和  SENDERS进行测试， 主要调整成最大值、中间值、最小值进行组合
- 在asan版本上执行，检查是否有内存泄露
- 执行原有性能ci工程，性能不劣化


（2） 对此次重构进行分析，新增功能测试点

（3）性能另外进行分析和设计

### 3.1.1 yasldr导入流程测试分析

对导入流程进行分析，此次测试需要注意以下方面：

（1）一个分区会创建4个slot，需考虑分区数量，因考虑ci执行用例时长，历史自动化用例没有上百万或者上千万分区的用例

（2）新增了CSV_CHUNK_SIZE、  SENDERS参数，需对参数进行验证，另外CSV_CHUNK_SIZE需要与withembedded参数进行组合

（3）新增统计项，因统计项是动态变化的，历史自动化用例都是模糊校验结果且数据量较少，根据统计项，需要注意以下方面

- 长时间导入， 导入完成后查看统计项信息
- 分布式多个节点（32dn），分布表和复制表


### 3.1.2 功能测试

针对新增配置、统计项、导入流程的相关分析，增加的测试点如下：

|输入条件1|输入条件2|有效类|无效类|
|---|---|---|---|
|CSV_CHUNK_SIZE|校验规则：,取值范围：[16M-1024M],默认值：128MB|16、1024、128、重复设置|15、-15、1025、数值边界值、科学计数法、,字符串数字、非数字、空格、空串|
|  
|结合  with embedded参数验证|前置条件：,（1）csv在CSV_CHUNK_SIZE位置处字段值存在换行、存在多字节字符,（2）csv文件大小大于CSV_CHUNK_SIZE,测试场景：,（1）拆分,（2）导入|  
|
|SENDERS,  
|校验规则：,取值范围：[1,128],默认值：,- 若设置了SENDER_COUNT，则以设置值为准，若超过DOP，则报错。
- 未设置，默认值为  ROUND(DOP,1/(DECODER_THREAD_TIMES + 1))
|（1）设置senders：1、16、128、重复设置,（2）未设置senders,  
|（1）设置senders：,- 0、-1、129、超过数值边界值、科学计数法、字符串数字、非数字、空格、空串
- senders超过dop
,  
|
|统计项|  
|前置条件：,（1）单机,（2）分布式：单个dn、32dn,测试场景：,（1）heap、tac、lsc、分布表、复制表长时间正常导入，导入完成后查看统计信息，各个统计项统计值正确,（2）heap、tac、lsc、分布表、复制表长时间带有bad记录导入，导入完成后查看统计信息，各个统计项统计值正确,（3）上千万分区导入，导入完成后查看统计信息，各个统计项统计值正确,（4）上千万分区拆分，拆分完成后查看统计信息，各个统计项统计值正确|  
|
|其它|  
|（1）分区多情况下导入， 统计项处已覆盖该场景,（2）命令行帮助信息,（3）资料,  
|  
|


  


## **3.2 详细测试设计**

1. *使用章节1的测试方法设计详细的功能测试点，可借用xmind的方式*
1. *梳理该特性是否涉DFX测试，并在详细设计中描述具体测试点，可借用xmind的方式*


|**系统级DFX分类**|**是否涉及**|**测试点**|
|---|---|---|
|CT|否|  
|
|KT|否|  
|
|长稳|否|  
|
|一致性|否|  
|
|三方测试工具    
  (sqltest，sqlancer)|否|  
|
|安全|否|  
|
|DFR|否|  
|
|HA|否|  
|
|压力|否|  
|
|性能|否|  
|
|可维护性|否|  
|


  


# **4. 测试用例**

（1）此次是优化重构，需要将历史自动化执行通过，历史用例即为冒烟用例

（2）新增用例文本用例如下

  


# **5. 测试框架设计**

使用导入导出工程

# **6. 测试环境说明**

*测试环境的相关说明，包括但不限于操作系统，环境配置，辅助测试工具等*

# **7. 工作量评估**

工作量：3  *人天*

|工作内容|工作量|
|---|---|
|测试设计和评审|0.5人天|
|执行ci工程和分析|1.5人天|
|新增测试点执行|1人天|
|上车|此测试设计主要是功能测试,该sr涉及到性能，性能达标后才能合入， 因此不在此处预估上车工作量|


计划测试完成时间：2024/02/02

## Comments:

|  [](null)  ,测试设计评审意见：,1、假设senders=1, 有32dn, 则有32*1个连接    
  2、统计信息只改了客户端， 没有改服务端    
  3、原有性能ci测试场景不劣化，该sr就可以合入,Posted by fanyu at 一月 29, 2024 15:15|
|---|
