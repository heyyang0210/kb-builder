Created by 何阳, last modified on 一月 04, 2024

JIRA：    [YDBRD-21540](https://jira.yasdb.com/browse/YDBRD-21540?src=confmacro)    -  窗口函数支持DN上执行-优化器  完成

## 1. Overview（概述）

window function 用于SQL分析，为了加快执行速度，减少网络数据交互，可以把窗口函数的执行放到数据节点上执行。

窗口函数与聚集函数不同的是，聚集函数一组只能返回一行，而窗口函数每组可以返回多行，组内每一行都是基于窗口的逻辑计算的结果。因此，在执行含有   WINDOW FUNCTION   的 SQL 时 (格式一般为   OVER(...)  ），都会在生成执行计划的时候分配一个   WINDOW FUNCTION   算子。

窗口函数可以对数据库数据进行实时分析处理。

## 2. Features（功能特性）

分布式下支持窗口函数的下推执行。

## 3. Interfaces（接口）

窗口函数的对外使用方式没有变化。

常用的窗口函数

1. 专用窗口函数：rank，dense_rank，first_value，lag，last_value，lead，row_number
1. 聚集函数：sum，avg，count，max，min等


## 4. Limitations（功能限制）

1. 窗口函数的分区键为常量值时，不下推
1. **窗口移动需要配合order by子句一起使用**  ，如果没有order by，数据没有做排序，滑动窗口计算出来的数据会出现随机的问题。
1. 不支持的特性，partkey为子查询，聚集函数


## 5. Detail Design（详细设计）

*方案设计的详细说明，包括但不限于方案选型、方案所依赖的技术/第三方组件的原理或背景介绍、关键技术点、方案折衷的考量、可靠性分析、兼容性分析。可以根据需要增删小节。*

### 5.1 Architecture（架构）

*说明方案的总体架构，优先考虑通过架构图进行描述。*

### 5.2 Data Structures & Flow（数据结构与流程）

*设计主要数据结构、工作流程、序列图等。*

typedef     struct     StExtraProp   {    
        ObjectArray  *      projExprs  ;   // 投影表达式数组    
        CodUint16        projFlags  ;    
        CodUint8         unused  [  2  ];    
        RescanType       rescan  ;      // 物化信息，RS_NONE表示不需要物化，RS_RESCAN需要物化    
        DstbDesc  *        dstbDesc  ;    // 分布式信息    
        DstbDesc  *        paralDesc  ;   // 并行信息    
        SortDesc  *        sort  ;        // 排序信息    
        ObjectArray  *     group  ;       // item 为Expr*    
        // CTE    
  }   ExtraProp  ;

  


1. 在requireSortedWinFunc增加一个DN执行的路径
1. 在cost上增加计算代价的相关代码，每个算子都需要增加
1. partition by列的优化，重复列，相关列，keyCost列，常量列，子查询列 --- 需要增加一个函数(分区列优化)，得到dstAttrs列 --- WindowDecl->partExprs 为窗口函数的分区键
1. --- 如果partition 列有多张表的列，dstbKeys是怎样的 --- oriProp->dstbDesc
1. --- 怎么从Expr得到ColumnAttr --- 函数colAttrFromExprNode 
1. order by列，order by字段没有做变更 --- WindowDecl→sortColumns


```
CodResult requireSortedWinFunc(CboOptimizer* cboOpt, CboOperator* op, ExtraProp* oriProp, ObjectArray* outProps)
{
    OpPhysSortedWinFunc* sortedWinFunc = PHYS_SORTED_WINFUNC(op);

    COD_CALL(makeSingleWinFuncProp(cboOpt, sortedWinFunc-&gt;srcDecl, oriProp, outProps, COD_TRUE));

    if (!isColumnStored(op)) {
        return COD_SUCCESS;
    }

    // 构造下推的路径，需要优化相同分区键
    COD_CALL(makeHashWinFuncProp(cboOpt, sortedWinFunc-&gt;srcDecl, oriProp, outProps, COD_TRUE));
    return COD_SUCCESS;
}

```

  


注意点：

1. 确保序列化和反序列化字段都有做执行
1. 分区键边界值
1. 将order by列做下检查


  


## 6. Testcases（自测用例）

*设计开发人员自测用例（文字描述）。*

create     table   class(id   int  , class   char  (  10  ), score   char  (  3  ));    
  insert     into   class   values  (  1  ,   '1'  ,   '90'  );    
  insert     into   class   values  (  2  ,   '1'  ,   '88'  );    
  insert     into   class   values  (  3  ,   '1'  ,   '87'  );    
  insert     into   class   values  (  4  ,   '2'  ,   '85'  );    
  insert     into   class   values  (  5  ,   '2'  ,   '99'  );    
  ​    
  --- 分布键的超集，预期下推    
  explain   select   rank() over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  explain   select   rank() over(partition   by   class,id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  explain   select   dense_rank() over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  explain   select   lag(score,   1  ) over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  explain   select   first_value(score) over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  explain   select   avg(score) over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  explain   select     count  (score) over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  explain   select     count  (score) over(partition   by   id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  --- partkey不同的数据类型，需要注意lob，blob，json新增类型    
  ​    
  --- partkey为常量，预期不下推    
  explain   select   rank() over(partition   by     1     order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  --- partkey为重复列,窗口函数分区键为分布键的超集时，下推，否则根据cost来判断是否下推    
  explain   select   rank() over(partition   by   id,id   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  explain   select   rank() over(partition   by   class,class   order     by   score   desc  )   from   class   order     by   score   desc   ;    
    
  --- 数据量比较大的情况下，下推 or 不下推的执行时间，调整下cost部分的代码    
  ​    
  --- 不支持项    
  --- partKey 为子查询    
  explain   select   class.*,  rank() over(partition   by   (  select     1     from   dual)   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  --- partKey 是聚集函数    
  explain   select   class.*,  rank() over(partition   by     count  (id)   order     by   score   desc  )   from   class   order     by   score   desc   ;    
  ​    
  ​    
  --- order by不同属性的用例    
  --- order by为子查询    
  --- order by为常量    
  --- order by为分布键，非分布键

  


## 7. Workload（工作量）

*评估代码量KLOC、工作量（人天）。*

* *

*2人周*

## 8. TODO（遗留问题）

*说明本方案遗留的问题或下一步需要解决的问题。*

## Comments:

|  [](null)  ,参与人员：,徐晓锋 孔柯煜 许秋莹 何阳 廖增康 吴煜,时间：2023年11月28日 11:00 ~ 12:00,会议纪要：,1. 不带order by列也是可以的，但是执行结果不稳定，建议二层CI用例使用order by字段
,  
,Posted by heyang at 一月 04, 2024 14:49|
|---|
